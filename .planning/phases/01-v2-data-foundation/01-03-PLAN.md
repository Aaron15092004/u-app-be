---
phase: 01-v2-data-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - backend/src/api/campaigns/campaigns.routes.ts
  - backend/src/api/campaigns/campaigns.controller.ts
  - backend/src/api/campaigns/campaigns.validation.ts
  - backend/src/api/recommendations/recommendations.routes.ts
  - backend/src/api/recommendations/recommendations.controller.ts
  - backend/src/api/recommendations/recommendations.service.ts
  - backend/src/api/recommendations/recommendations.validation.ts
  - backend/src/api/ratings/ratings.routes.ts
  - backend/src/api/ratings/ratings.controller.ts
  - backend/src/api/ratings/ratings.validation.ts
  - backend/src/api/media-assets/media-assets.routes.ts
  - backend/src/api/media-assets/media-assets.controller.ts
  - backend/src/api/media-assets/media-assets.validation.ts
  - backend/src/api/food/food.routes.ts
  - backend/src/api/food/food.controller.ts
  - backend/src/api/food/food.validation.ts
  - backend/src/api/admin/admin.routes.ts
  - backend/src/app.ts
autonomous: true
requirements:
  - CODE-11
requirements_addressed:
  - CODE-11
must_haves:
  truths:
    - "D-02: Campaign redeem validation accepts code input suitable for HTTPS redeem links without implementing QR export."
    - "D-04/D-05: Entitlement status scaffold exposes finite quota metadata shape without enabling full scan bypass workflow."
    - "D-06/D-07: Barcode validation requires string barcode params and minimum nutrition fields before save contracts."
    - "D-08/D-09: Recommendation routes expose static backend nut-milk rules and preference selection contract."
    - "D-11: Media asset admin scaffolds expose asset metadata only; no MediaBatch workflow exists."
    - "D-12/D-13: Rating routes separate prompt state/status from internal AppRating submission."
  artifacts:
    - path: "backend/src/api/campaigns/campaigns.validation.ts"
      provides: "Redeem and entitlement request/response validation contracts"
    - path: "backend/src/api/food/food.validation.ts"
      provides: "Barcode string and minimum nutrition validation"
    - path: "backend/src/app.ts"
      provides: "Mounted user scaffolds for campaigns, recommendations, ratings, and media routes"
  key_links:
    - from: "backend/src/app.ts"
      to: "backend/src/api/campaigns/campaigns.routes.ts"
      via: "/api/campaigns mount"
      pattern: "app.use\\('/api/campaigns'"
    - from: "backend/src/api/admin/admin.routes.ts"
      to: "backend/src/api/media-assets/media-assets.routes.ts"
      via: "admin guard router mount"
      pattern: "media-assets"
---

<objective>
Create API validation and route scaffolds for v2.0 domains without implementing full workflows.

Purpose: Later phases need stable request/response contracts and protected mount points for campaigns, entitlements, barcode, recommendations, ratings, and media. Phase 1 should make routes discoverable and typecheckable while preventing accidental workflow claims.

Output: Zod validation modules, controller/service placeholders, route mounts, and 501/minimal contract responses for domains that are implemented in later phases.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-v2-data-foundation/01-CONTEXT.md
@.planning/phases/01-v2-data-foundation/01-RESEARCH.md
@backend/src/api/food/food.routes.ts
@backend/src/api/food/food.validation.ts
@backend/src/api/admin/admin.routes.ts
@backend/src/app.ts
@backend/src/api/recommendations/nut-milk.rules.ts

<interfaces>
Existing route patterns:
- User food routes apply `authenticate` per route.
- Admin routes call `router.use(authenticate, requireAdmin)` once, then define all admin endpoints below that guard.
- `app.ts` mounts routers before `errorMiddleware`.
- Validation modules use Zod objects exported near the route/controller code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user-facing route scaffolds and validation</name>
  <files>backend/src/api/campaigns/campaigns.routes.ts, backend/src/api/campaigns/campaigns.controller.ts, backend/src/api/campaigns/campaigns.validation.ts, backend/src/api/recommendations/recommendations.routes.ts, backend/src/api/recommendations/recommendations.controller.ts, backend/src/api/recommendations/recommendations.service.ts, backend/src/api/recommendations/recommendations.validation.ts, backend/src/api/ratings/ratings.routes.ts, backend/src/api/ratings/ratings.controller.ts, backend/src/api/ratings/ratings.validation.ts, backend/src/app.ts</files>
  <action>Create authenticated user route scaffolds for `/api/campaigns/redeem`, `/api/campaigns/me/entitlements`, `/api/recommendations/nut-milk`, `/api/recommendations/nut-milk/selection`, `/api/ratings/status`, and `/api/ratings`. Validation must include redeem code string input, entitlement response quota policy shape, nut-milk need signals/selection shape, rating stars 1-5 with optional comment/trigger/appVersion/platform, and prompt status shape. Controllers may return 501 for workflows scheduled later, except recommendation GET may return static rules from `nut-milk.rules.ts`; do not implement campaign generation, redemption mutation, entitlement bypass, feedback cooldown workflow, or native store review logic in this phase.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>User-facing contract routes mount and typecheck; full workflows remain explicitly unimplemented.</done>
</task>

<task type="auto">
  <name>Task 2: Add barcode and admin media/campaign/rating scaffolds</name>
  <files>backend/src/api/food/food.routes.ts, backend/src/api/food/food.controller.ts, backend/src/api/food/food.validation.ts, backend/src/api/media-assets/media-assets.routes.ts, backend/src/api/media-assets/media-assets.controller.ts, backend/src/api/media-assets/media-assets.validation.ts, backend/src/api/admin/admin.routes.ts</files>
  <action>Add `/api/food/items/barcode/:barcode` route behind `authenticate`, with param validation that treats barcodes as 6-18 digit strings and never as numbers per D-07. Add a validation export for minimum barcode-save nutrition requiring product name, kcal, protein, carbs, and fat per D-06; do not save barcode logs or call Open Food Facts in Phase 1. Add admin route scaffolds under existing `/api/admin` guard for campaigns, campaign code generation/list/export contract, ratings list, and media assets list/upload/update/delete contract. Media scaffolds must use `MediaAsset` metadata terms and not introduce `MediaBatch` per D-11. Return 501/minimal contract responses for later workflows.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>Barcode and admin scaffolds exist, remain guarded, and do not implement full Phase 2/3/5/6 workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> user API | Authenticated mobile requests cross into new scaffold controllers. |
| Admin web -> admin API | Admin-only campaign/media/rating endpoints cross privileged boundary. |
| Barcode input -> backend | User-supplied barcode strings must be validated before lookup/save workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Spoofing | User routes | mitigate | Apply `authenticate` to all user scaffolds. |
| T-01-03-02 | Elevation of Privilege | Admin scaffolds | mitigate | Mount admin campaign/media/rating scaffolds only below existing `router.use(authenticate, requireAdmin)`. |
| T-01-03-03 | Tampering | Barcode params | mitigate | Zod validates barcodes as strings; no numeric conversion per D-07. |
| T-01-03-04 | Information Disclosure | Redeem code endpoints | mitigate | Placeholder controllers must not log or echo persisted raw-code material; only validate input and return generic scaffold responses. |
</threat_model>

<verification>
Run `cd backend && npm run typecheck`. Run `rg -n "router.use\\(authenticate, requireAdmin\\)|campaigns|media-assets|ratings" backend/src/api/admin/admin.routes.ts` to confirm admin scaffolds sit behind the router-level guard.
</verification>

<success_criteria>
- Phase 1 roadmap success criterion 4 is satisfied.
- Scaffolds cover campaigns/entitlements, barcode, recommendations, ratings, and media without full workflows.
- D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-11, D-12, and D-13 are represented in validation/routes.
</success_criteria>

<output>
Create `.planning/phases/01-v2-data-foundation/01-03-SUMMARY.md` when done.
</output>
