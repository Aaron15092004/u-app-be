---
phase: 01-v2-data-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - backend/src/services/redeem-code.service.test.ts
  - backend/src/models/RedeemCode.test.ts
  - backend/src/models/v2-data-foundation.test.ts
  - backend/src/api/campaigns/campaigns.integration.test.ts
  - backend/src/api/food/food.integration.test.ts
  - backend/src/api/admin/admin.integration.test.ts
  - backend/package.json
autonomous: true
requirements:
  - CODE-11
requirements_addressed:
  - CODE-11
must_haves:
  truths:
    - "CODE-11/D-01: Tests prove code hashing is normalized and raw reusable code fields are absent from RedeemCode."
    - "D-03/D-04/D-05: Tests prove entitlement schema stores activeUntil, finite high quota policy, and scan-attempt entitlement metadata."
    - "D-06/D-07: Tests prove barcode validation rejects missing macros and keeps barcodes as strings."
    - "D-08/D-09: Tests prove NutMilkPreference is separate and static backend rules exist outside DB seed data."
    - "D-10/D-11: Tests prove Exercise imageUrl remains and imageAssetId/MediaAsset metadata exist without MediaBatch."
    - "D-12/D-13: Tests prove FeedbackPromptState and AppRating are separate internal feedback contracts."
  artifacts:
    - path: "backend/src/services/redeem-code.service.test.ts"
      provides: "Hash utility tests"
    - path: "backend/src/models/RedeemCode.test.ts"
      provides: "CODE-11 schema/index tests"
    - path: "backend/src/models/v2-data-foundation.test.ts"
      provides: "Cross-model foundation tests"
    - path: "backend/src/api/campaigns/campaigns.integration.test.ts"
      provides: "Campaign scaffold auth/validation tests"
  key_links:
    - from: "backend/src/api/campaigns/campaigns.integration.test.ts"
      to: "backend/src/app.ts"
      via: "supertest mounted route check"
      pattern: "/api/campaigns"
    - from: "backend/src/models/RedeemCode.test.ts"
      to: "backend/src/models/RedeemCode.ts"
      via: "unique codeHash and absence of plaintext fields"
      pattern: "codeHash"
---

<objective>
Add automated verification for the v2.0 data foundation and CODE-11 security contract.

Purpose: Phase 1 is foundational; later phases should not rediscover that a schema, index, route guard, or DTO contract is missing. These tests lock the decisions before implementation proceeds.

Output: Backend unit/model/integration tests and package scripts for fast Phase 1 verification.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-v2-data-foundation/01-CONTEXT.md
@.planning/phases/01-v2-data-foundation/01-RESEARCH.md
@backend/package.json
@backend/src/api/food/food.integration.test.ts
@backend/src/api/admin/admin.integration.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test CODE-11 hashing and RedeemCode storage contract</name>
  <files>backend/src/services/redeem-code.service.test.ts, backend/src/models/RedeemCode.test.ts, backend/package.json</files>
  <behavior>
    - Equivalent code forms with case, spaces, and hyphens normalize to the same HMAC hash.
    - Hash output is not equal to raw or normalized input and changes when pepper changes.
    - RedeemCode documents persist `codeHash` and metadata only, with no raw reusable code field.
    - Duplicate `codeHash` violates the unique index in mongodb-memory-server.
  </behavior>
  <action>Add node:test tests for `redeem-code.service.ts` and `RedeemCode.ts`. Use existing `--env-file=.env.test --require tsx/cjs --test` pattern and mongodb-memory-server conventions from current integration tests. Add `test:v2-foundation` package script that runs the new service/model tests. The tests must fail if fields named `code`, `rawCode`, or `plainCode` exist as persisted schema paths on `RedeemCode`.</action>
  <verify>
    <automated>cd backend && npm run test:v2-foundation</automated>
  </verify>
  <done>CODE-11 hashing and hash-only persistence are locked by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test schema, validation, and route scaffold contracts</name>
  <files>backend/src/models/v2-data-foundation.test.ts, backend/src/api/campaigns/campaigns.integration.test.ts, backend/src/api/food/food.integration.test.ts, backend/src/api/admin/admin.integration.test.ts, backend/package.json</files>
  <behavior>
    - Model tests assert required indexes/fields for Campaign, UserScanEntitlement, FoodItem barcode strings, Exercise imageAssetId, MediaAsset, NutMilkPreference, FeedbackPromptState, and AppRating.
    - Campaign scaffold tests assert unauthenticated users are rejected and authenticated requests hit validation/501 contract responses.
    - Food tests assert barcode param validation accepts string digits with leading zeros and rejects invalid/missing-minimum nutrition inputs.
    - Admin tests assert campaign/media/rating scaffolds remain behind `requireAdmin`.
  </behavior>
  <action>Create `v2-data-foundation.test.ts` for cross-model schema/index assertions and add focused route scaffold tests to existing food/admin suites or new campaign suite as appropriate. Extend `test:v2-foundation` to include all new Phase 1 tests. Do not test full campaign redemption, QR export, barcode provider fallback, recommendation UI, media assignment, or rating prompt workflows because those are later phases.</action>
  <verify>
    <automated>cd backend && npm run test:v2-foundation</automated>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd backend && npm run test:food</automated>
    <automated>cd backend && npm run test:admin</automated>
  </verify>
  <done>Phase 1 data foundation contracts are covered by fast automated tests and existing affected suites still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner -> in-memory DB | Contract tests create and index documents in isolated MongoDB. |
| API scaffold tests -> app | Supertest validates authentication/authorization and validation behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Repudiation | CODE-11 contract | mitigate | Tests prove hash-only persistence and unique codeHash index. |
| T-01-05-02 | Elevation of Privilege | Admin scaffolds | mitigate | Integration tests require non-admin rejection for admin campaign/media/rating routes. |
| T-01-05-03 | Tampering | Barcode contract | mitigate | Tests assert string barcode handling and minimum nutrition validation. |
</threat_model>

<verification>
Run the full Phase 1 gate:
- `cd backend && npm run typecheck`
- `cd backend && npm run test:v2-foundation`
- `cd backend && npm run test:food`
- `cd backend && npm run test:admin`
- `cd mobile && npx tsc --noEmit`
- `cd admin && npx tsc --noEmit`

Decision coverage gate:
- `rg -n "D-0[1-9]|D-1[0-3]|CODE-11" .planning/phases/01-v2-data-foundation/01-*-PLAN.md`
- `rg -n "rawCode|plainCode|code: String|code: \\{ type: String" backend/src/models/RedeemCode.ts` must not report persisted schema paths; response-only export DTOs in client/admin files are allowed only when explicitly named as transient/export response data.
</verification>

<success_criteria>
- Phase 1 success criteria from ROADMAP are all covered by implementation and tests.
- CODE-11 is proven by hash utility tests, model/index tests, and route scaffold tests.
- D-01 through D-13 remain visible in plan objectives/truths/must-haves and are enforced by test expectations where executable.
</success_criteria>

<source_audit>
GOAL: Establish v2.0 backend contracts and data foundations so later phases can build without schema churn or security shortcuts — covered by Plans 01-05.
REQ CODE-11: Backend stores redeem codes hashed at rest and redeems them atomically so the same code cannot be reused — covered by Plans 01, 02, and 05.
RESEARCH: package additions, model shapes, route scaffolds, validation, and tests — covered by Plans 01-05; `archiver` explicitly excluded because ZIP export is conditional.
CONTEXT D-01 through D-13: covered across must_haves truths in Plans 01-05; no deferred ideas exist.
</source_audit>

<output>
Create `.planning/phases/01-v2-data-foundation/01-05-SUMMARY.md` when done.
</output>
