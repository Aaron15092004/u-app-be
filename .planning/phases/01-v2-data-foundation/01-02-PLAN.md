---
phase: 01-v2-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/models/Campaign.ts
  - backend/src/models/RedeemCode.ts
  - backend/src/models/UserScanEntitlement.ts
  - backend/src/models/NutMilkPreference.ts
  - backend/src/models/MediaAsset.ts
  - backend/src/models/AppRating.ts
  - backend/src/models/FeedbackPromptState.ts
  - backend/src/models/FoodScanAttempt.ts
  - backend/src/models/FoodItem.ts
  - backend/src/models/Exercise.ts
  - backend/src/api/recommendations/nut-milk.rules.ts
autonomous: true
requirements:
  - CODE-11
requirements_addressed:
  - CODE-11
must_haves:
  truths:
    - "D-01/CODE-11: RedeemCode schema has `codeHash` and metadata only; raw reusable code fields are absent."
    - "D-03: Campaign and entitlement models store redemption-duration semantics through `entitlementDurationDays` and `activeUntil`."
    - "D-04/D-05: UserScanEntitlement and FoodScanAttempt carry finite high-quota policy and entitlement audit metadata."
    - "D-07: FoodItem stores barcodes as string arrays with leading-zero preservation."
    - "D-08/D-09: NutMilkPreference is separate and recommendation rules are static backend config."
    - "D-10/D-11: Exercise keeps imageUrl and adds imageAssetId; MediaAsset has source/batchId/status/basic metadata with no MediaBatch model."
    - "D-12/D-13: FeedbackPromptState is separate and AppRating models internal feedback before native store-review prompts."
  artifacts:
    - path: "backend/src/models/RedeemCode.ts"
      provides: "Hash-only code lookup model"
      contains: "codeHash"
      excludes: "rawCode, plainCode, code: String"
    - path: "backend/src/models/UserScanEntitlement.ts"
      provides: "Finite high-quota entitlement records"
      contains: "quotaPolicy"
    - path: "backend/src/models/FoodItem.ts"
      provides: "Barcode string fields"
      contains: "barcodes"
    - path: "backend/src/models/Exercise.ts"
      provides: "Media asset ref while preserving imageUrl"
      contains: "imageAssetId"
  key_links:
    - from: "backend/src/models/RedeemCode.ts"
      to: "backend/src/services/redeem-code.service.ts"
      via: "codeHash generated by service utility"
      pattern: "codeHash"
    - from: "backend/src/models/FoodScanAttempt.ts"
      to: "backend/src/models/UserScanEntitlement.ts"
      via: "optional entitlementId"
      pattern: "entitlementId"
---

<objective>
Create the v2.0 Mongoose model and index foundation.

Purpose: Later phases need stable backend contracts for campaigns, hashed redeem codes, scan entitlements, barcode food lookup, nut-milk preferences, media assets, and feedback without changing schema shape mid-feature.

Output: New Mongoose models, backward-compatible schema extensions, and static nut-milk rule config covering D-01 through D-13 where data shape is the source of truth.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-v2-data-foundation/01-CONTEXT.md
@.planning/phases/01-v2-data-foundation/01-RESEARCH.md
@backend/src/models/FoodScanAttempt.ts
@backend/src/models/FoodItem.ts
@backend/src/models/Exercise.ts
@backend/src/models/User.ts
@backend/src/services/redeem-code.service.ts

<interfaces>
Existing model patterns:
- Models import `mongoose, { Document, Schema }` and export `mongoose.model<IName>('Name', Schema)`.
- Schema indexes are declared near the schema definition.
- Existing `FoodItem` text index uses `{ default_language: 'none' }`; preserve this for Vietnamese text search.
- Existing `Exercise.imageUrl` is `string | null`; it must remain compatible.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add campaign, code, and entitlement models</name>
  <files>backend/src/models/Campaign.ts, backend/src/models/RedeemCode.ts, backend/src/models/UserScanEntitlement.ts, backend/src/models/FoodScanAttempt.ts</files>
  <action>Create `Campaign`, `RedeemCode`, and `UserScanEntitlement` models using existing Mongoose conventions. `RedeemCode` must include `campaignId`, `batchId`, `codeHash`, `codePrefix`, `codeLength`, `status`, `expiresAt`, `redeemedBy`, `redeemedAt`, `redemptionSource`, and `createdBy`; it must not define `code`, `rawCode`, `plainCode`, or any reusable plaintext field per D-01/CODE-11. Add unique index on `codeHash`, campaign/status/date indexes, batch export index, and redeemedBy support index. `Campaign` must include active window, `entitlementDurationDays` per D-03, finite `highQuotaDailyLimit` per D-04, status, counts, and createdBy. `UserScanEntitlement` must include `activeUntil`, campaign/code/redemption links, `quotaPolicy.mode = high_daily_quota`, `quotaPolicy.dailyLimit`, and unique `redeemCodeId` per D-05/CODE-11. Extend `FoodScanAttempt` with optional `source`, `entitlementId`, and `quotaMode` while preserving existing TTL and user/date count indexes.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>Campaign/code/entitlement models and scan-attempt metadata exist with indexes needed for future atomic redemption and entitlement-backed scan auditing.</done>
</task>

<task type="auto">
  <name>Task 2: Add barcode, recommendation, media, and feedback model contracts</name>
  <files>backend/src/models/FoodItem.ts, backend/src/models/Exercise.ts, backend/src/models/NutMilkPreference.ts, backend/src/models/MediaAsset.ts, backend/src/models/AppRating.ts, backend/src/models/FeedbackPromptState.ts, backend/src/api/recommendations/nut-milk.rules.ts</files>
  <action>Extend `FoodItem` with optional/defaulted `barcodes: string[]`, `brand`, `servingSizeG`, `packageSize`, `barcodeSource`, and `barcodeLastVerifiedAt`; barcodes must be strings, never numbers, per D-07. Keep existing search compatible and include `brand` in the Vietnamese text index without removing `default_language: 'none'`. Extend `Exercise` with optional `imageAssetId` while keeping `imageUrl` unchanged per D-10. Add `NutMilkPreference` as a separate model with user, selected/recommended flavor ids, optional need signals, source BMI category/value, selectedAt, and history-friendly timestamps per D-08. Add static backend `nut-milk.rules.ts` constants for the five roadmap flavors; do not seed rules into DB and do not create admin-editable rules per D-09. Add `MediaAsset` with source, batchId, status, publicId/url, dimensions/bytes/mimeType, assignedExerciseId, uploadedBy, and metadata; do not create `MediaBatch` per D-11. Add `AppRating` for internal stars/comment/trigger/appVersion/platform/deviceInfo per D-13, and `FeedbackPromptState` as a separate prompt-state model per D-12.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>Barcode, recommendation, media, and feedback data contracts exist while preserving existing mobile/admin compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database schema | Persisted v2.0 documents become source of truth for later workflows. |
| Mobile/admin clients -> API | Existing clients must not break from optional schema additions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | `RedeemCode` | mitigate | Hash-only fields; no raw reusable code fields; unique `codeHash` index per CODE-11/D-01. |
| T-01-02-02 | Tampering | `UserScanEntitlement` | mitigate | Unique `redeemCodeId`, activeUntil, and finite quota policy prepare atomic single-use redemption per CODE-11. |
| T-01-02-03 | Denial of Service | Scan quota data | mitigate | Store high daily quota metadata, not unlimited bypass flags, per D-04/D-05. |
| T-01-02-04 | Information Disclosure | Rating/media/admin data | mitigate | Models only; admin route guards are planned in Plan 03 before exposure. |
</threat_model>

<verification>
Run `cd backend && npm run typecheck`. Inspect schema definitions with `rg -n "rawCode|plainCode|code: \\{ type: String|code: String" backend/src/models/RedeemCode.ts` and confirm no raw reusable code field exists.
</verification>

<success_criteria>
- Phase 1 roadmap success criteria 1, 2, and 3 are satisfied at the model layer.
- D-01, D-03, D-04, D-05, D-07, D-08, D-09, D-10, D-11, D-12, and D-13 are encoded in schemas/config.
- Existing `FoodItem`, `Exercise`, and `FoodScanAttempt` behavior remains backward-compatible.
</success_criteria>

<output>
Create `.planning/phases/01-v2-data-foundation/01-02-SUMMARY.md` when done.
</output>
