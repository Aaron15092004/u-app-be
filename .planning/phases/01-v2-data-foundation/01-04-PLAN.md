---
phase: 01-v2-data-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - mobile/src/lib/api/types.ts
  - mobile/src/lib/api/v2-contracts.api.ts
  - admin/src/features/exercises/useExercises.ts
  - admin/src/features/v2-contracts/types.ts
autonomous: true
requirements:
  - CODE-11
requirements_addressed:
  - CODE-11
must_haves:
  truths:
    - "D-01/CODE-11: Client/admin types expose code metadata and hash-backed ids, not persisted raw redeem code fields."
    - "D-02: Redeem/QR contracts use HTTPS redeem URL payload names."
    - "D-03/D-04/D-05: Entitlement DTOs include activeUntil, redemption linkage, and finite high_daily_quota metadata."
    - "D-06/D-07: Barcode DTOs keep barcode as string and require minimum nutrition for save-ready status."
    - "D-08/D-09: Nut-milk preference/rule DTOs are backend-sourced and separate from user profile."
    - "D-10/D-11: Exercise type preserves imageUrl and adds optional imageAssetId/media asset metadata."
    - "D-12/D-13: Rating and prompt-state DTOs separate internal feedback from optional native store review eligibility."
  artifacts:
    - path: "mobile/src/lib/api/types.ts"
      provides: "Mobile v2 DTOs for later phases"
    - path: "mobile/src/lib/api/v2-contracts.api.ts"
      provides: "Typed endpoint helper stubs for mounted backend scaffolds"
    - path: "admin/src/features/v2-contracts/types.ts"
      provides: "Admin DTOs for campaigns, codes, ratings, media assets"
    - path: "admin/src/features/exercises/useExercises.ts"
      provides: "Exercise imageAssetId compatibility"
  key_links:
    - from: "mobile/src/lib/api/v2-contracts.api.ts"
      to: "/api/campaigns/me/entitlements"
      via: "typed API helper"
      pattern: "entitlements"
    - from: "admin/src/features/exercises/useExercises.ts"
      to: "backend/src/models/Exercise.ts"
      via: "optional imageAssetId"
      pattern: "imageAssetId"
---

<objective>
Create client/admin type contract scaffolding for the backend data foundation.

Purpose: Later mobile and admin phases should consume stable DTOs from Phase 1 instead of inventing per-screen shapes. This plan preserves existing compatibility while adding v2.0 contract surfaces.

Output: Mobile API types/helper stubs and admin DTO types covering campaign, entitlement, barcode, recommendation, media, and rating contracts.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-v2-data-foundation/01-CONTEXT.md
@mobile/src/lib/api/types.ts
@admin/src/features/exercises/useExercises.ts

<interfaces>
Existing client/admin patterns:
- Mobile shared DTOs live in `mobile/src/lib/api/types.ts`.
- Mobile API helpers use the existing shared API client pattern in adjacent `*.api.ts` files.
- Admin TanStack Query feature hooks define local DTOs beside hooks; new broad v2 contracts can live under `admin/src/features/v2-contracts/types.ts`.
- Existing `Exercise.imageUrl` stays optional/null-compatible; add `imageAssetId?: string | null` only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile v2 DTOs and API scaffold helpers</name>
  <files>mobile/src/lib/api/types.ts, mobile/src/lib/api/v2-contracts.api.ts</files>
  <action>Extend mobile shared types with v2 DTOs for campaign redeem requests, HTTPS redeem payload URL, entitlement status with `activeUntil`, `campaignId`, `redeemCodeId`, `quotaPolicy.mode = high_daily_quota`, barcode lookup result with barcode string/provenance/minimum nutrition readiness, nut-milk rules and separate preference, app rating submission, prompt status, and optional native store-review eligibility. Do not add UI screens or workflow logic. Add `v2-contracts.api.ts` with typed helper functions targeting the mounted scaffold endpoints; these helpers may call endpoints that return 501 until later phases, but their TypeScript signatures must match the Phase 1 backend contracts.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Mobile can import typed v2 contracts without breaking existing food/exercise/profile types.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin v2 DTOs and exercise media compatibility</name>
  <files>admin/src/features/exercises/useExercises.ts, admin/src/features/v2-contracts/types.ts</files>
  <action>Add optional `imageAssetId?: string | null` to the existing admin `Exercise` type while keeping `imageUrl?: string | null` unchanged per D-10. Create admin v2 contract types for Campaign, RedeemCode metadata, UserScanEntitlement, MediaAsset, AppRating, FeedbackPromptState, barcode product metadata, and NutMilkPreference. Admin code DTOs must include `codeHash`/prefix/status metadata but no raw reusable code persistence field per D-01/CODE-11; generation/export response types may have transient `rawCode` or `redeemUrl` only when named as response-only export data.</action>
  <verify>
    <automated>cd admin && npx tsc --noEmit</automated>
  </verify>
  <done>Admin contracts are available for later feature phases and existing exercise admin hooks remain compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> mobile/admin clients | Backend contract data becomes typed client input. |
| Admin export response -> admin client | Transient raw code values may appear only in explicit generation/export response DTOs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | Client code DTOs | mitigate | Persisted DTOs expose codeHash/prefix/status only; transient rawCode appears only in response-only export types per D-01. |
| T-01-04-02 | Tampering | Barcode DTOs | mitigate | Barcode is typed as string and save-ready status depends on required macro fields per D-06/D-07. |
| T-01-04-03 | Information Disclosure | Rating DTOs | mitigate | Separate internal feedback and store-review eligibility so public review is not implied or forced per D-13. |
</threat_model>

<verification>
Run `cd mobile && npx tsc --noEmit` and `cd admin && npx tsc --noEmit`. Run `rg -n "imageAssetId" mobile/src/lib/api/types.ts admin/src/features/exercises/useExercises.ts admin/src/features/v2-contracts/types.ts`.
</verification>

<success_criteria>
- Client/admin contract scaffolding exists without adding full UI workflows.
- All D-01 through D-13 decisions have a corresponding DTO surface where relevant.
- Exercise image compatibility and barcode string handling are preserved in client types.
</success_criteria>

<output>
Create `.planning/phases/01-v2-data-foundation/01-04-SUMMARY.md` when done.
</output>
