---
phase: 02-mvp-fast-release-pack
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
files_modified:
  - backend/src/api/food/barcode-provider.service.ts
  - backend/src/api/food/food.service.ts
  - backend/src/api/food/food.controller.ts
  - backend/src/api/food/food.validation.ts
  - backend/src/api/recommendations/recommendations.service.ts
  - backend/src/api/recommendations/recommendations.controller.ts
  - backend/src/api/recommendations/recommendations.validation.ts
  - mobile/src/lib/api/v2-contracts.api.ts
  - mobile/src/lib/api/types.ts
  - mobile/src/app/(tabs)/profile/index.tsx
  - mobile/src/app/(tabs)/food/index.tsx
  - mobile/src/app/(food)/scan.tsx
  - mobile/src/app/(food)/result.tsx
autonomous: true
requirements:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
  - BAR-05
  - BAR-06
  - BAR-07
  - MILK-01
  - MILK-02
  - MILK-03
  - MILK-04
  - MILK-05
  - MILK-06
requirements_addressed:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
  - BAR-05
  - BAR-06
  - BAR-07
  - MILK-01
  - MILK-02
  - MILK-03
  - MILK-04
  - MILK-05
  - MILK-06
must_haves:
  truths:
    - "D-13: Barcode MVP uses an external provider such as Open Food Facts and does not rely on local FoodItem barcode coverage."
    - "D-14: Barcode values remain strings and preserve leading zeros end-to-end."
    - "D-15/D-16: Save-ready external barcode results can be logged after basic serving/weight adjustment and preserve source/provenance metadata."
    - "D-17/D-18/D-19: Ủ milk MVP is Profile-based, shows suitable options, saves one preferred flavor, and uses non-medical product-preference copy."
    - "D-23: This plan does not implement exercise image/media operations."
  artifacts:
    - path: "backend/src/api/food/barcode-provider.service.ts"
      provides: "External barcode lookup and normalization"
    - path: "mobile/src/app/(food)/scan.tsx"
      provides: "Barcode mode entry point"
    - path: "mobile/src/app/(tabs)/profile/index.tsx"
      provides: "Profile-based Ủ milk preference UI"
  key_links:
    - from: "backend/src/api/food/food.controller.ts"
      to: "backend/src/api/food/barcode-provider.service.ts"
      via: "barcode lookup route"
      pattern: "getFoodItemByBarcode"
    - from: "mobile/src/app/(tabs)/profile/index.tsx"
      to: "backend/src/api/recommendations/recommendations.controller.ts"
      via: "recommendation and selection APIs"
      pattern: "selectNutMilkFlavor"
---

<objective>
Implement the barcode external-provider MVP and Profile-based Ủ milk preference MVP.

Purpose: Phase 2 needs food scanning to be more complete and product guidance to be visible, but without relying on unavailable local barcode data or moving milk UI into the BMI screen.

Output: backend external barcode lookup/normalization, mobile barcode scan/log path, backend milk preference persistence, and Profile milk selection UI.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-fast-release-pack/02-CONTEXT.md
@.planning/phases/02-mvp-fast-release-pack/02-RESEARCH.md
@backend/src/api/food/food.controller.ts
@backend/src/api/food/food.service.ts
@backend/src/api/food/food.validation.ts
@backend/src/models/FoodItem.ts
@backend/src/api/recommendations/nut-milk.rules.ts
@backend/src/models/NutMilkPreference.ts
@mobile/src/app/(food)/scan.tsx
@mobile/src/app/(food)/result.tsx
@mobile/src/app/(tabs)/profile/index.tsx
@mobile/src/lib/api/v2-contracts.api.ts

<interfaces>
- Open Food Facts `/api/v2/product/{barcode}.json` is the default external candidate.
- Backend must own external provider calls; mobile calls only app backend.
- Barcode result save-ready requires product name plus calories, protein, carbs, and fat.
- Milk copy must avoid treatment/medical claims.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement external barcode lookup and save-ready normalization</name>
  <files>backend/src/api/food/barcode-provider.service.ts, backend/src/api/food/food.service.ts, backend/src/api/food/food.controller.ts, backend/src/api/food/food.validation.ts, mobile/src/lib/api/types.ts, mobile/src/lib/api/v2-contracts.api.ts</files>
  <behavior>
    - Barcode lookup calls external provider when requested and does not depend on local FoodItem data for MVP.
    - Barcode string is validated as 6-18 digits and never coerced to number.
    - Response includes source/provenance, product identity, brand/package fields where available, normalized calories/protein/carbs/fat, `isSaveReady`, and `missingFields`.
    - External provider errors/timeouts produce friendly fallback response rather than crashing.
  </behavior>
  <action>Add a backend provider service with fetch timeout and normalization from Open Food Facts-style responses. Wire `GET /api/food/items/barcode/:barcode` to return the normalized contract. Keep any cache persistence minimal or omit it if it risks schedule; provenance in the response is mandatory.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Backend returns useful external barcode results without local barcode dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile barcode mode and log flow</name>
  <files>mobile/src/app/(tabs)/food/index.tsx, mobile/src/app/(food)/scan.tsx, mobile/src/app/(food)/result.tsx, mobile/src/lib/api/v2-contracts.api.ts, mobile/src/lib/api/food.api.ts, mobile/src/lib/api/types.ts</files>
  <behavior>
    - User can open barcode mode from the food scan flow.
    - User can enter/scan a barcode through available camera/input patterns; if camera barcode scanning is too risky, typed barcode fallback must exist for MVP.
    - Save-ready barcode result can be logged after basic serving/weight adjustment.
    - Missing/incomplete results guide user to manual search or AI image scan fallback.
  </behavior>
  <action>Add barcode mode UI in the food flow. Prefer existing camera infrastructure if available; otherwise implement typed barcode input as MVP fallback while preserving the route/API contract for scanner polish in Phase 3. Convert save-ready barcode result into the existing food log shape and call the existing save food log API with source/provenance where supported.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>User can use barcode-assisted logging without dead ends.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Profile-based Ủ milk preference</name>
  <files>backend/src/api/recommendations/recommendations.service.ts, backend/src/api/recommendations/recommendations.controller.ts, backend/src/api/recommendations/recommendations.validation.ts, mobile/src/app/(tabs)/profile/index.tsx, mobile/src/lib/api/v2-contracts.api.ts, mobile/src/lib/api/types.ts</files>
  <behavior>
    - Backend returns deterministic flavor options from existing static rules.
    - Backend persists one selected flavor per user through `NutMilkPreference`.
    - Profile shows suitable options and current saved selection.
    - Copy is product-preference wording with a concise non-medical disclaimer.
  </behavior>
  <action>Replace recommendation selection scaffold with real persistence. Add read/update behavior needed by Profile. In mobile Profile, render a compact milk preference section with suitable options and save action. Do not integrate into BMI screen in Phase 2.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>User can choose and persist one Ủ milk flavor from Profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Backend -> external barcode provider | External data can be missing, slow, or inconsistent. |
| User barcode input -> backend | Digit strings must be validated and preserved. |
| Product copy -> user | Milk recommendation must not become medical advice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04-01 | Tampering | Barcode data | mitigate | Normalize and mark provenance; require macro completeness before save-ready. |
| T-02-04-02 | Denial of Service | External provider | mitigate | Use timeout/friendly fallback and avoid blocking the whole food flow. |
| T-02-04-03 | Information Disclosure | Provider calls | mitigate | Send only barcode to external provider; no user token or PII. |
| T-02-04-04 | Safety | Milk copy | mitigate | Non-medical wording and disclaimer. |
</threat_model>

<verification>
- `cd backend && npm run typecheck`
- `cd mobile && npx tsc --noEmit`
- Backend tests in Plan 05 should mock external provider success, incomplete, missing, and timeout cases.
- Manual smoke: barcode save-ready result -> food log, incomplete result -> fallback; Profile milk preference save persists.
</verification>

<success_criteria>
- External barcode lookup has real MVP value.
- User can log save-ready barcode result.
- Profile milk preference works and persists.
- No BMI-screen integration or exercise media scope creep.
</success_criteria>

<source_audit>
REQ: BAR-01 through BAR-07 and MILK-01 through MILK-06.
CONTEXT: D-13 through D-19 and D-23.
</source_audit>

<output>
Create `.planning/phases/02-mvp-fast-release-pack/02-04-SUMMARY.md` when done.
</output>
