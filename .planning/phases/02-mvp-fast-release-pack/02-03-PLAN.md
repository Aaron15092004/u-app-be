---
phase: 02-mvp-fast-release-pack
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - mobile/src/lib/api/v2-contracts.api.ts
  - mobile/src/lib/api/types.ts
  - mobile/src/app/(tabs)/profile/index.tsx
  - mobile/src/app/(food)/scan.tsx
  - mobile/src/components/ui/RedeemCodeCard.tsx
  - mobile/src/components/ui/ScanEntitlementBadge.tsx
  - mobile/src/components/ui/AppRatingPrompt.tsx
autonomous: true
requirements:
  - CODE-06
  - CODE-08
  - CODE-09
  - CODE-10
  - RATE-01
  - RATE-02
  - RATE-03
requirements_addressed:
  - CODE-06
  - CODE-08
  - CODE-09
  - CODE-10
  - RATE-01
  - RATE-02
  - RATE-03
must_haves:
  truths:
    - "D-06: User can enter redeem codes from both Profile and the food-scan quota/exhausted state."
    - "D-09/D-20: Internal rating prompt appears after successful redeem, not first launch or error states."
    - "D-10/D-11: Food scan UI shows active entitlement badge and 30/day quota state returned by backend."
    - "D-21: Mobile does not trigger native store review in Phase 2."
    - "D-23: No exercise image/mobile media workflow is added."
  artifacts:
    - path: "mobile/src/components/ui/RedeemCodeCard.tsx"
      provides: "Reusable manual redeem form"
    - path: "mobile/src/components/ui/ScanEntitlementBadge.tsx"
      provides: "Active package/expiry display"
    - path: "mobile/src/components/ui/AppRatingPrompt.tsx"
      provides: "Internal star/comment feedback prompt"
  key_links:
    - from: "mobile/src/app/(tabs)/profile/index.tsx"
      to: "mobile/src/lib/api/v2-contracts.api.ts"
      via: "redeem and entitlement APIs"
      pattern: "redeemCampaignCodeApi"
    - from: "mobile/src/app/(food)/scan.tsx"
      to: "mobile/src/components/ui/ScanEntitlementBadge.tsx"
      via: "quota display"
      pattern: "activeUntil"
---

<objective>
Implement the mobile redeem, entitlement status, and post-redeem internal rating MVP.

Purpose: users need to activate bottle codes from obvious places, see their scan benefit, and give lightweight feedback after a successful activation.

Output: Profile and food-scan manual redeem entry points, entitlement badge/status display, and internal star/comment prompt after redeem.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-fast-release-pack/02-CONTEXT.md
@.planning/phases/02-mvp-fast-release-pack/02-RESEARCH.md
@.planning/phases/02-mvp-fast-release-pack/02-01-PLAN.md
@mobile/src/lib/api/v2-contracts.api.ts
@mobile/src/lib/api/types.ts
@mobile/src/app/(tabs)/profile/index.tsx
@mobile/src/app/(food)/scan.tsx
@mobile/src/components/ui/PrimaryButton.tsx

<interfaces>
- Reuse existing `v2-contracts.api.ts` helpers where possible.
- Keep copy Vietnamese and concise.
- No QR scanner/deep-link handling in Phase 2.
- No native store review prompt in Phase 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable redeem form and entitlement badge</name>
  <files>mobile/src/components/ui/RedeemCodeCard.tsx, mobile/src/components/ui/ScanEntitlementBadge.tsx, mobile/src/lib/api/v2-contracts.api.ts, mobile/src/lib/api/types.ts</files>
  <behavior>
    - Manual code input normalizes user-friendly spaces/hyphens visually but sends raw input to backend validation.
    - Success/invalid/already used/expired/revoked/unauthenticated messages are shown clearly in Vietnamese from backend response.
    - Active entitlement badge shows package active state and expiry date in Vietnam-friendly display.
  </behavior>
  <action>Create `RedeemCodeCard` and `ScanEntitlementBadge` components. Ensure API response types include quota mode, limit, activeUntil, and redemption status fields returned by Plan 01. Keep components stable on loading/error states and avoid layout shifts.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Reusable redeem/status UI components are available for Profile and food scan.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Profile and food scan redeem/status flows with rating prompt</name>
  <files>mobile/src/app/(tabs)/profile/index.tsx, mobile/src/app/(food)/scan.tsx, mobile/src/components/ui/AppRatingPrompt.tsx</files>
  <behavior>
    - Profile shows manual redeem entry and current entitlement status.
    - Food scan screen shows entitlement badge and a redeem entry when scan quota blocks the user.
    - Successful redeem opens internal star/comment prompt.
    - Rating prompt supports submit and dismiss/cooldown behavior using backend rating APIs.
  </behavior>
  <action>Integrate the new components into Profile and food scan. On successful redeem, invalidate entitlement/food scan related queries and present `AppRatingPrompt`. Submit rating through `submitAppRatingApi` with trigger `manual` or a redeem-specific mapping supported by backend validation; if backend keeps only Phase 1 trigger enum, use `manual` and include redeem context in comment/device metadata as supported. Do not call `expo-store-review`.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>User can redeem, see active scan package, and submit/dismiss internal feedback after redeem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mobile input -> backend | User-entered redeem codes may be invalid or malicious. |
| Mobile feedback -> backend | Rating/comment is user-generated content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03-01 | Spoofing | Entitlement UI | mitigate | UI only displays backend status; no client-side entitlement decisions. |
| T-02-03-02 | Information Disclosure | Redeem code input | mitigate | Do not log code values in client console or store persistent local copies. |
| T-02-03-03 | Repudiation | Rating prompt | mitigate | Submit rating through authenticated API with backend context. |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit`
- Manual smoke: Profile redeem success/error, food scan quota state redeem, active badge display, rating submit/dismiss after redeem.
</verification>

<success_criteria>
- User-facing redeem flow exists in both required entry points.
- Active package benefit is visible in food scan.
- Rating prompt is internal and tied to successful redeem.
</success_criteria>

<source_audit>
REQ: CODE-06/08/09/10 and RATE-01/02/03.
CONTEXT: D-06, D-09, D-10, D-11, D-20, D-21, D-23.
</source_audit>

<output>
Create `.planning/phases/02-mvp-fast-release-pack/02-03-SUMMARY.md` when done.
</output>
