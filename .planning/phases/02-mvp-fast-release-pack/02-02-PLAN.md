---
phase: 02-mvp-fast-release-pack
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - admin/src/App.tsx
  - admin/src/components/layout/AppShell.tsx
  - admin/src/features/campaigns/useCampaigns.ts
  - admin/src/features/ratings/useRatings.ts
  - admin/src/features/v2-contracts/types.ts
  - admin/src/pages/CampaignsPage.tsx
  - admin/src/pages/RatingsPage.tsx
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - RATE-04
requirements_addressed:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - RATE-04
must_haves:
  truths:
    - "D-02/D-04: Admin UI is minimal but backed by real APIs for campaign creation, bulk code generation, CSV export, and revoke actions."
    - "D-05/D-07: Admin export UI downloads CSV with raw code and HTTPS redeem URL; it does not offer QR image/ZIP export."
    - "D-22: Admin rating view is a basic internal feedback table, not analytics or native store-review management."
    - "D-23: Admin navigation/pages do not add exercise image operations in Phase 2."
  artifacts:
    - path: "admin/src/pages/CampaignsPage.tsx"
      provides: "Campaign/code MVP admin workflow"
    - path: "admin/src/pages/RatingsPage.tsx"
      provides: "Basic internal feedback view"
    - path: "admin/src/features/campaigns/useCampaigns.ts"
      provides: "TanStack Query hooks for Phase 2 admin campaign APIs"
  key_links:
    - from: "admin/src/App.tsx"
      to: "admin/src/pages/CampaignsPage.tsx"
      via: "admin route"
      pattern: "campaign"
    - from: "admin/src/components/layout/AppShell.tsx"
      to: "admin/src/pages/RatingsPage.tsx"
      via: "admin navigation"
      pattern: "ratings"
---

<objective>
Create minimal admin UI for Phase 2 campaign operations and internal ratings.

Purpose: the company needs an operations screen to create campaigns, generate codes, export CSV for bottle printing, revoke bad codes/campaigns, and review post-redeem feedback without waiting for advanced analytics.

Output: admin campaign/code page, ratings page, hooks, route/navigation entries, and DTO updates.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-fast-release-pack/02-CONTEXT.md
@.planning/phases/02-mvp-fast-release-pack/02-RESEARCH.md
@.planning/phases/02-mvp-fast-release-pack/02-01-PLAN.md
@admin/src/App.tsx
@admin/src/components/layout/AppShell.tsx
@admin/src/components/data-table/DataTable.tsx
@admin/src/features/v2-contracts/types.ts
@admin/src/pages/ExercisesPage.tsx

<interfaces>
- Use existing admin auth/API client patterns.
- Keep UI dense and operational; no landing/marketing layout.
- CSV export should be a direct download from backend response.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build admin campaign/code MVP page</name>
  <files>admin/src/features/campaigns/useCampaigns.ts, admin/src/pages/CampaignsPage.tsx, admin/src/features/v2-contracts/types.ts</files>
  <behavior>
    - Admin can create campaign with required MVP fields.
    - Admin can generate a batch of codes for a campaign.
    - Admin can download CSV export after generation or from campaign/code controls.
    - Admin can see campaign/code status enough for operations.
    - Admin can revoke campaign/code in MVP.
  </behavior>
  <action>Add campaign API hooks and a `CampaignsPage` following existing DataTable/dialog/form conventions. Keep form fields aligned with backend validation. Use toast feedback and query invalidation. Implement CSV download by reading blob/text response and triggering browser download. Avoid QR image/ZIP UI.</action>
  <verify>
    <automated>cd admin && npx tsc --noEmit</automated>
  </verify>
  <done>Admin can run first bottle-code operations from a real UI.</done>
</task>

<task type="auto">
  <name>Task 2: Build basic admin ratings page</name>
  <files>admin/src/features/ratings/useRatings.ts, admin/src/pages/RatingsPage.tsx, admin/src/App.tsx, admin/src/components/layout/AppShell.tsx, admin/src/features/v2-contracts/types.ts</files>
  <behavior>
    - Admin can view stored internal ratings with user, stars, comment, trigger, created date, and app version when available.
    - View is table/list only; no analytics, sentiment, or native store review controls.
    - Campaigns and ratings are discoverable from admin navigation.
  </behavior>
  <action>Add ratings API hook and a basic table page. Add admin routes and nav links for Campaigns and Ratings. Keep advanced filters out of scope; optional lightweight search/date filtering is allowed only if backend already exposes it without extra complexity.</action>
  <verify>
    <automated>cd admin && npx tsc --noEmit</automated>
  </verify>
  <done>Admin can inspect internal feedback entries after redeem-triggered prompts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Admin browser -> campaign APIs | Admin actions create/revoke/export sensitive codes. |
| Admin export -> local file | CSV contains raw redeem codes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-02-01 | Information Disclosure | CSV download | mitigate | UI labels export as sensitive and does not persist raw CSV client-side beyond browser download. |
| T-02-02-02 | Elevation of Privilege | Admin routes | mitigate | Reuse protected admin shell/API client; backend remains authoritative. |
| T-02-02-03 | Tampering | Revoke actions | mitigate | Confirm destructive revoke actions in UI and rely on backend auth/status checks. |
</threat_model>

<verification>
- `cd admin && npx tsc --noEmit`
- Manual smoke after backend is running: create campaign, generate codes, download CSV, revoke, view ratings table.
</verification>

<success_criteria>
- Campaign/code CSV workflow is usable by admin.
- Ratings are visible to admin.
- No exercise media UI is added.
</success_criteria>

<source_audit>
REQ: CODE-01/02/03/04/05 and RATE-04.
CONTEXT: D-02, D-04, D-05, D-07, D-22, D-23.
</source_audit>

<output>
Create `.planning/phases/02-mvp-fast-release-pack/02-02-SUMMARY.md` when done.
</output>
