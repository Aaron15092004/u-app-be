---
phase: 02-mvp-fast-release-pack
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - backend/src/api/campaigns/campaigns.integration.test.ts
  - backend/src/api/food/food.integration.test.ts
  - backend/src/api/recommendations/recommendations.integration.test.ts
  - backend/src/api/ratings/ratings.integration.test.ts
  - backend/src/api/admin/admin.integration.test.ts
  - backend/package.json
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - CODE-06
  - CODE-08
  - CODE-09
  - CODE-10
  - CODE-12
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
  - BAR-05
  - BAR-06
  - BAR-07
  - MILK-01
  - MILK-02
  - MILK-03
  - MILK-04
  - MILK-05
  - MILK-06
  - RATE-01
  - RATE-02
  - RATE-03
  - RATE-04
requirements_addressed:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - CODE-06
  - CODE-08
  - CODE-09
  - CODE-10
  - CODE-12
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
  - BAR-05
  - BAR-06
  - BAR-07
  - MILK-01
  - MILK-02
  - MILK-03
  - MILK-04
  - MILK-05
  - MILK-06
  - RATE-01
  - RATE-02
  - RATE-03
  - RATE-04
must_haves:
  truths:
    - "D-01 through D-12: Campaign, redemption, CSV export, entitlement status, and 30/day quota are covered by automated backend tests."
    - "D-03: The selected MVP workflows ship together in Phase 2; only explicitly deferred polish/advanced items are excluded."
    - "D-13 through D-16: External barcode lookup is tested with mocked provider responses and preserves string/provenance/save-ready behavior."
    - "D-17 through D-19: Profile-based milk preference persistence and non-medical recommendation contract are tested."
    - "D-20 through D-22: Internal post-redeem rating and basic admin feedback view are tested; native store review remains absent."
    - "D-23: Verification asserts exercise media files/routes were not added to Phase 2 scope."
  artifacts:
    - path: "backend/package.json"
      provides: "`test:v2-mvp` script"
    - path: "backend/src/api/campaigns/campaigns.integration.test.ts"
      provides: "Campaign/redeem/quota tests"
    - path: "backend/src/api/food/food.integration.test.ts"
      provides: "Barcode/external provider and entitlement scan tests"
  key_links:
    - from: "backend/package.json"
      to: "backend/src/api/*/*.integration.test.ts"
      via: "test:v2-mvp"
      pattern: "test:v2-mvp"
---

<objective>
Add the Phase 2 MVP verification gate and close contract gaps before execution is considered done.

Purpose: this phase changes money-adjacent code, AI cost controls, external data lookup, and user-visible flows. The MVP needs focused tests that prove decisions from `02-CONTEXT.md` survived implementation.

Output: backend integration/unit tests, a fast Phase 2 test script, and final compile gates for backend/mobile/admin.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-fast-release-pack/02-CONTEXT.md
@.planning/phases/02-mvp-fast-release-pack/02-RESEARCH.md
@.planning/phases/02-mvp-fast-release-pack/02-01-PLAN.md
@.planning/phases/02-mvp-fast-release-pack/02-02-PLAN.md
@.planning/phases/02-mvp-fast-release-pack/02-03-PLAN.md
@.planning/phases/02-mvp-fast-release-pack/02-04-PLAN.md
@backend/package.json
@backend/src/api/campaigns/campaigns.integration.test.ts
@backend/src/api/food/food.integration.test.ts
@backend/src/api/admin/admin.integration.test.ts

<interfaces>
- Use node:test pattern already present in backend.
- Mock external barcode provider responses; do not rely on network in tests.
- Mobile/admin verification is TypeScript compile unless executor adds focused component tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend tests for campaign, redeem, quota, rating, and milk</name>
  <files>backend/src/api/campaigns/campaigns.integration.test.ts, backend/src/api/recommendations/recommendations.integration.test.ts, backend/src/api/ratings/ratings.integration.test.ts, backend/src/api/admin/admin.integration.test.ts, backend/package.json</files>
  <behavior>
    - Campaign create/generate/export works for admins only.
    - CSV export includes raw code and HTTPS redeem URL but raw code is not persisted.
    - Manual redeem creates entitlement and rejects duplicate/expired/revoked/invalid codes with Vietnamese messages.
    - Entitlement status returns activeUntil and quota policy.
    - Post-redeem internal rating persists and admin can view ratings.
    - Milk preference selection persists and can be read.
  </behavior>
  <action>Add integration tests around the Phase 2 backend APIs. Extend or create `test:v2-mvp` script in backend package.json. Keep test fixtures isolated and avoid real external calls.</action>
  <verify>
    <automated>cd backend && npm run test:v2-mvp</automated>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>Money/redeem/rating/milk backend contracts are protected by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add barcode and entitlement scan verification</name>
  <files>backend/src/api/food/food.integration.test.ts, backend/package.json</files>
  <behavior>
    - External barcode provider success response normalizes save-ready product data.
    - Missing/incomplete provider response returns `isSaveReady: false` and missingFields.
    - Provider timeout/error returns friendly fallback response.
    - Barcode strings with leading zeros remain strings.
    - Active entitlement user gets 30/day scan limit; standard user keeps normal limit.
    - Scan attempts record entitlement metadata when applicable.
  </behavior>
  <action>Add tests with mocked provider/fetch behavior and scan quota fixtures. Ensure no test depends on live Open Food Facts network availability.</action>
  <verify>
    <automated>cd backend && npm run test:v2-mvp</automated>
    <automated>cd backend && npm run test:food</automated>
  </verify>
  <done>Barcode and scan quota decisions are executable and regression-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Run cross-app compile and scope guard checks</name>
  <files>backend/package.json</files>
  <behavior>
    - Backend, mobile, and admin compile.
    - No Phase 2 plan added exercise media operations.
    - Native store review is not invoked in Phase 2 UI.
    - QR image/ZIP export and in-app QR scanner remain deferred.
  </behavior>
  <action>Run final verification commands and fix narrow contract/type issues that arise from Phase 2 implementation. Add a small script or document checks in the summary if no script is needed.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd backend && npm run test:v2-mvp</automated>
    <automated>cd backend && npm run test:food</automated>
    <automated>cd backend && npm run test:admin</automated>
    <automated>cd mobile && npx tsc --noEmit</automated>
    <automated>cd admin && npx tsc --noEmit</automated>
    <automated>rg -n "Exercise Media Operations|MediaAsset|imageAssetId|expo-store-review|QRCode|archiver" backend/src mobile/src admin/src .planning/phases/02-mvp-fast-release-pack/02-*-SUMMARY.md</automated>
  </verify>
  <done>Phase 2 has a repeatable verification gate and no known scope regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tests -> implementation | Tests must catch contract drift in monetization and quota paths. |
| Mocked external provider -> barcode normalizer | Provider fixtures simulate untrusted external data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-05-01 | Repudiation | Redemption | mitigate | Tests prove single-use and status transitions. |
| T-02-05-02 | Denial of Service | External provider | mitigate | Timeout/error tests prove graceful fallback. |
| T-02-05-03 | Information Disclosure | Raw codes | mitigate | Tests assert no persisted raw-code fields after generation/export. |
| T-02-05-04 | Scope Creep | Media/native review/QR polish | mitigate | Scope guard checks and summary callouts. |
</threat_model>

<verification>
Run the full Phase 2 gate:
- `cd backend && npm run typecheck`
- `cd backend && npm run test:v2-mvp`
- `cd backend && npm run test:food`
- `cd backend && npm run test:admin`
- `cd mobile && npx tsc --noEmit`
- `cd admin && npx tsc --noEmit`

Scope guard:
- Confirm no exercise media workflow was implemented in Phase 2.
- Confirm native store review prompt, QR image/ZIP export, and in-app QR scanner are not implemented.
</verification>

<success_criteria>
- All Phase 2 MVP backend contracts have automated tests.
- Mobile/admin/backend compile together.
- Discuss-phase decisions D-01 through D-23 are visible in plan summaries and tests where executable.
- Phase can proceed to execute-phase without hidden exercise media scope.
</success_criteria>

<source_audit>
GOAL: fastest usable commercial MVP — verified across backend, mobile, and admin.
REQ: all Phase 2 mapped requirements are represented. CODE-07 QR scanner and RATE-05 native prompt are deferred to Phase 3 by ROADMAP/REQUIREMENTS and context.
CONTEXT: D-01 through D-23.
</source_audit>

<output>
Create `.planning/phases/02-mvp-fast-release-pack/02-05-SUMMARY.md` when done.
</output>
