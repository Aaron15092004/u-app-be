---
phase: 03-core-health-tracking
plan: 06
type: execute
wave: 3
depends_on: [03-05]
files_modified:
  - mobile/src/components/ui/ExerciseCard.tsx
  - mobile/src/components/ui/CategoryFilterChip.tsx
  - mobile/src/components/ui/WeeklyStatCard.tsx
  - mobile/src/components/ui/DailyChallengeCard.tsx
  - mobile/src/app/(tabs)/exercises/_layout.tsx
  - mobile/src/app/(tabs)/exercises/index.tsx
  - mobile/src/app/(tabs)/exercises/[id]/_layout.tsx
  - mobile/src/app/(tabs)/exercises/[id]/index.tsx
autonomous: true
requirements: [WO-01, WO-02, WO-03, WO-04, WO-05, WO-06, WO-11]
tags: [mobile, workout, exercises, screens, components, tanstack-query, vertical-slice]

must_haves:
  truths:
    - "User can open the Tập luyện tab and see a vertically scrolling list of all exercises from the backend (WO-01, WO-02, WO-11)"
    - "User can tap one of 5 category chips (Tất cả / Yoga / Cardio / Tạ / Giãn cơ) and the list refetches with that filter applied (WO-01)"
    - "Active chip background is bg-primary + white text; inactive chips have bg-surface + border + text-text (UI-SPEC §3.1)"
    - "Each exercise card shows: category icon placeholder (since imageUrl=null per D-42), Vietnamese name, Vietnamese difficulty label (Dễ/Trung bình/Khó), '{N} phút', '{N} kcal' (WO-02)"
    - "Weekly stats row above the list shows 4 stat cards: Ngày tập / Bài tập / kcal / Phút using the GET /api/workouts/stats/weekly response (WO-03)"
    - "Daily challenge card shows 'Mục tiêu hôm nay: Đốt 300 kcal' with a progress bar fill = todayKcal / targetKcal (WO-04)"
    - "Tapping an exercise card navigates to /(tabs)/exercises/[id] showing description + steps list + 'Bắt đầu tập' sticky button (WO-05)"
    - "Difficulty enum mapping: 'easy' → 'Dễ' + green, 'medium' → 'Trung bình' + orange, 'hard' → 'Khó' + red (UI-SPEC §3.1 difficulty tag)"
    - "Category enum to display name mapping: 'yoga' → 'Yoga', 'cardio' → 'Cardio', 'weights' → 'Tạ', 'stretching' → 'Giãn cơ'"
    - "All screens use SafeAreaView + ScrollView (or FlatList for the exercise list)"
  artifacts:
    - path: "mobile/src/components/ui/ExerciseCard.tsx"
      provides: "Pressable row card rendering thumbnail/icon, name, difficulty tag, duration, kcal"
      exports: ["default"]
    - path: "mobile/src/components/ui/CategoryFilterChip.tsx"
      provides: "Active/inactive pill toggle for category filter"
      exports: ["default"]
    - path: "mobile/src/components/ui/WeeklyStatCard.tsx"
      provides: "Display-only card with label + value (+ optional unit)"
      exports: ["default"]
    - path: "mobile/src/components/ui/DailyChallengeCard.tsx"
      provides: "Display-only card with header + progress bar"
      exports: ["default"]
    - path: "mobile/src/app/(tabs)/exercises/index.tsx"
      provides: "Exercise List screen with stats row + chips + FlatList"
    - path: "mobile/src/app/(tabs)/exercises/[id]/index.tsx"
      provides: "Exercise Detail screen with description + steps + sticky 'Bắt đầu tập' button"
    - path: "mobile/src/app/(tabs)/exercises/_layout.tsx"
      provides: "Stack layout for the exercises route group (headerShown: false)"
    - path: "mobile/src/app/(tabs)/exercises/[id]/_layout.tsx"
      provides: "Stack layout for the per-id route group (headerShown: false)"
  key_links:
    - from: "mobile/src/app/(tabs)/exercises/index.tsx"
      to: "mobile/src/lib/api/exercises.api.ts + mobile/src/lib/api/workouts.api.ts"
      via: "useQuery(['exercises', category]) and useQuery(['workouts','stats','weekly'])"
      pattern: "listExercisesApi|getWeeklyStatsApi"
    - from: "mobile/src/app/(tabs)/exercises/[id]/index.tsx"
      to: "mobile/src/lib/api/exercises.api.ts"
      via: "useQuery(['exercise', id])"
      pattern: "getExerciseApi"
    - from: "mobile/src/app/(tabs)/exercises/[id]/index.tsx"
      to: "mobile/src/app/(tabs)/exercises/[id]/timer.tsx (Plan 07)"
      via: "router.push from 'Bắt đầu tập' onPress"
      pattern: "router\\.push.*timer"
---

<objective>
Mobile workout vertical slice — Part 1 (browse + detail). After this plan, a logged-in user opens the Tập luyện tab, sees their week stats, browses 100 Vietnamese exercises filtered by category, taps one, and lands on its detail screen with a Start button (the Start button navigates to /timer which Plan 07 implements).

Purpose: Delivers WO-01–WO-06 and WO-11 backend-to-pixel. A real user can complete the entire pre-workout journey after this plan.

Output: 4 new presentational components + 2 new screens + 2 stack layout files. Wired to backend endpoints from Plans 03-01 and 03-02 via API clients from Plan 03-05.

Note on Expo Router file structure: With the nested route `(tabs)/exercises/[id]/timer.tsx` (Plan 07), Expo Router requires the `[id]` segment to be a directory, not a file. This plan creates `(tabs)/exercises/[id]/index.tsx` as the detail screen (so the path renders correctly). The folder structure is set up here so Plan 07 can drop `timer.tsx` and `complete.tsx` into the same `[id]/` folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-health-tracking/03-CONTEXT.md
@.planning/phases/03-core-health-tracking/03-UI-SPEC.md
@.planning/phases/03-core-health-tracking/03-PATTERNS.md
@.planning/phases/03-core-health-tracking/03-05-PLAN.md

@mobile/src/lib/api/types.ts
@mobile/src/lib/api/client.ts
@mobile/src/lib/api/exercises.api.ts
@mobile/src/lib/api/workouts.api.ts
@mobile/src/components/ui/PrimaryButton.tsx
@mobile/src/components/ui/GoalCard.tsx
@mobile/src/components/ui/ScreenHeader.tsx
@mobile/src/providers/AuthProvider.tsx
@mobile/src/constants/colors.ts
@mobile/src/app/(tabs)/_layout.tsx
@mobile/src/app/(tabs)/index.tsx
@mobile/src/app/(onboarding)/screen-3.tsx

<interfaces>
<!-- Component prop contracts (kept simple; consumed only by this plan + Plan 07 indirectly). -->

ExerciseCard props:
  { exercise: IExercise; onPress: () => void }

CategoryFilterChip props:
  { label: string; active: boolean; onPress: () => void }

WeeklyStatCard props:
  { label: string; value: string | number; unit?: string }

DailyChallengeCard props:
  { targetKcal: number; currentKcal: number; title?: string }   // title defaults to 'Mục tiêu hôm nay: Đốt 300 kcal'

<!-- Display-name maps (used by Exercise List screen + ExerciseCard) -->

CATEGORY_DISPLAY: Record<ICategory, string> = {
  yoga: 'Yoga', cardio: 'Cardio', weights: 'Tạ', stretching: 'Giãn cơ'
}
CATEGORY_ICON: Record<ICategory, IoniconsName> = {
  yoga: 'body-outline', cardio: 'bicycle-outline', weights: 'barbell-outline', stretching: 'fitness-outline'
}
DIFFICULTY_DISPLAY: Record<IDifficulty, string> = {
  easy: 'Dễ', medium: 'Trung bình', hard: 'Khó'
}
DIFFICULTY_COLOR: Record<IDifficulty, string> = {
  easy: '#4CAF50', medium: '#FFA726', hard: '#EF5350'
}

FILTER_CHIPS: Array<{ id: ICategory | 'all'; label: string }> = [
  { id: 'all', label: 'Tất cả' },
  { id: 'yoga', label: 'Yoga' },
  { id: 'cardio', label: 'Cardio' },
  { id: 'weights', label: 'Tạ' },
  { id: 'stretching', label: 'Giãn cơ' },
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 presentational components (ExerciseCard, CategoryFilterChip, WeeklyStatCard, DailyChallengeCard)</name>
  <files>mobile/src/components/ui/ExerciseCard.tsx, mobile/src/components/ui/CategoryFilterChip.tsx, mobile/src/components/ui/WeeklyStatCard.tsx, mobile/src/components/ui/DailyChallengeCard.tsx</files>
  <read_first>
    mobile/src/components/ui/GoalCard.tsx (StyleSheet + Pressable + Ionicons pattern — analog for ExerciseCard and CategoryFilterChip)
    mobile/src/components/ui/PrimaryButton.tsx (Pressable interaction state pattern — opacity 0.85 pressed, 0.5 disabled, accessibilityRole='button')
    mobile/src/lib/api/types.ts (IExercise, ICategory, IDifficulty type definitions)
    .planning/phases/03-core-health-tracking/03-UI-SPEC.md §3.1 (Exercise card anatomy, category filter chips, weekly stats cards, daily challenge card)
    mobile/src/constants/colors.ts (PRIMARY, ACCENT, BACKGROUND, SURFACE, TEXT, TEXT_SECONDARY, DIFFICULTY_EASY, DIFFICULTY_MEDIUM, DIFFICULTY_HARD constants — added in Plan 03-05)
  </read_first>
  <behavior>
    No automated test — `tsc --noEmit` is the gate. Visual verification happens in Task 2 via the Exercise List screen.

    Per-component spec:

    ExerciseCard: Pressable row 80dp thumbnail + content column (name, difficulty pill, duration text, kcal text). Thumbnail is `bg-surface, border-gray-200, rounded-lg, 80x80` containing the category Ionicon at 32dp `text-text-secondary` color (since imageUrl is always null per D-42). Difficulty pill is a small View with `paddingHorizontal: 8, paddingVertical: 2, borderRadius: 12` and `backgroundColor: DIFFICULTY_COLOR[exercise.difficulty]` (with 20% opacity overlay or alpha-blended hex) + text `color: DIFFICULTY_COLOR[exercise.difficulty]` and `fontWeight: '700' size:12`. Below the pill row: duration text `"{durationMinutes} phút"` + kcal text `"{caloriesBurned} kcal"` both at fontSize 14, text-secondary. Pressed state: opacity 0.85.

    CategoryFilterChip: Pressable pill. When active: `bg-primary` (#4CAF50) + white text. Inactive: `bg-surface` (#FFFFFF) + border 1px #D1D5DB + text-text (#212121). Padding `paddingHorizontal: 16, paddingVertical: 8`, `borderRadius: 20`. Label font: bold (700), size 14.

    WeeklyStatCard: Plain View card. `bg-surface, borderRadius: 16, paddingVertical: 12, paddingHorizontal: 8, flex: 1, alignItems: 'center'`. Two stacked Text nodes: value (font-bold 700, size 20, color: text) and label (size 12, color: text-secondary). Optional `unit` rendered alongside value at smaller size.

    DailyChallengeCard: View card. `bg-surface, borderRadius: 16, padding: 16, marginVertical: 12`. Header text (title or default 'Mục tiêu hôm nay: Đốt 300 kcal'): bold 14. Progress bar below: outer View `height: 8, borderRadius: 4, backgroundColor: '#E0E0E0', overflow: 'hidden'`; inner View `height: '100%', backgroundColor: PRIMARY, width: '${Math.min(100, Math.round((currentKcal/targetKcal)*100))}%'`. Below progress bar: small text "{currentKcal} / {targetKcal} kcal" right-aligned, text-secondary.
  </behavior>
  <action>
    Create the 4 files at `mobile/src/components/ui/`. Use StyleSheet (matching existing PrimaryButton/GoalCard style — NativeWind className would also compile but the existing codebase favors StyleSheet for primitives; keep consistent).

    Each component:
    - Functional component, default export, returns React.JSX.Element.
    - Import Ionicons from '@expo/vector-icons' (for ExerciseCard).
    - Import named color constants from '../../constants/colors' (e.g. PRIMARY, TEXT, TEXT_SECONDARY, DIFFICULTY_EASY, DIFFICULTY_MEDIUM, DIFFICULTY_HARD).
    - For ExerciseCard import `IExercise, ICategory, IDifficulty` types from '../../lib/api/types'.

    ExerciseCard: At top of file, define the const maps CATEGORY_ICON, DIFFICULTY_DISPLAY, DIFFICULTY_COLOR from `<interfaces>`. Use `Pressable` with `accessibilityRole="button"` and `accessibilityLabel={`Bài tập: ${exercise.name}, độ khó ${DIFFICULTY_DISPLAY[exercise.difficulty]}, ${exercise.durationMinutes} phút, ${exercise.caloriesBurned} kcal`}`.

    CategoryFilterChip: Use `Pressable` with `accessibilityRole="button"`, `accessibilityState={{ selected: active }}`, accessibilityLabel = `Lọc theo ${label}`.

    WeeklyStatCard: Plain `View` (non-interactive).

    DailyChallengeCard: Plain `View` (non-interactive). Compute percent inline; clamp 0..100.

    Do NOT inline-import the IExerciseStep type (not needed here — only the parent IExercise).
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 4 component files exist at `mobile/src/components/ui/`.
    - `grep -n "^export default" mobile/src/components/ui/ExerciseCard.tsx mobile/src/components/ui/CategoryFilterChip.tsx mobile/src/components/ui/WeeklyStatCard.tsx mobile/src/components/ui/DailyChallengeCard.tsx` matches once per file (4 total).
    - `grep -n "DIFFICULTY_DISPLAY\|'Dễ'\|'Trung bình'\|'Khó'" mobile/src/components/ui/ExerciseCard.tsx` matches (Vietnamese difficulty labels embedded).
    - `grep -n "Tất cả\|Yoga\|Cardio\|Tạ\|Giãn cơ" mobile/src/components/ui/CategoryFilterChip.tsx` — at least no requirement here (chip receives label as prop); but `grep -nE "accessibilityRole.*button" mobile/src/components/ui/CategoryFilterChip.tsx` MUST match.
    - `grep -n "Mục tiêu hôm nay: Đốt 300 kcal" mobile/src/components/ui/DailyChallengeCard.tsx` matches the default title.
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Exercise List screen + stack layouts</name>
  <files>mobile/src/app/(tabs)/exercises/_layout.tsx, mobile/src/app/(tabs)/exercises/index.tsx, mobile/src/app/(tabs)/exercises/[id]/_layout.tsx</files>
  <read_first>
    mobile/src/app/(onboarding)/_layout.tsx (Expo Router Stack layout pattern)
    mobile/src/app/(tabs)/index.tsx (SafeAreaView + StyleSheet pattern for tabs screens)
    mobile/src/lib/api/exercises.api.ts (listExercisesApi signature)
    mobile/src/lib/api/workouts.api.ts (getWeeklyStatsApi signature)
    mobile/src/providers/AuthProvider.tsx (useAuth hook — not needed for this screen since apiClient injects token automatically; cite for awareness)
    .planning/phases/03-core-health-tracking/03-UI-SPEC.md §3.1 (Exercise List screen layout, states, copywriting — Vietnamese empty/error strings)
    .planning/phases/03-core-health-tracking/03-PATTERNS.md (TanStack Query useQuery patterns, FlatList structure)
    mobile/src/components/ui/ExerciseCard.tsx (just created — prop shape)
    mobile/src/components/ui/CategoryFilterChip.tsx (just created)
    mobile/src/components/ui/WeeklyStatCard.tsx (just created)
    mobile/src/components/ui/DailyChallengeCard.tsx (just created)
  </read_first>
  <behavior>
    Manual smoke (Plan 07 will exercise the navigation handoff; here we only verify the list renders). Acceptance is via type compile + grep gates that prove the wiring.
  </behavior>
  <action>
    `mobile/src/app/(tabs)/exercises/_layout.tsx`:
    ```typescript
    import { Stack } from 'expo-router';
    import React from 'react';
    export default function ExercisesLayout(): React.JSX.Element {
      return <Stack screenOptions={{ headerShown: false }} />;
    }
    ```

    `mobile/src/app/(tabs)/exercises/[id]/_layout.tsx`: identical to above (separate file because Expo Router treats `[id]/` as its own route group when nested screens exist).

    `mobile/src/app/(tabs)/exercises/index.tsx` (Exercise List screen):
    - Imports: React, useState; SafeAreaView, ScrollView (horizontal for chips), FlatList, View, Text, ActivityIndicator, StyleSheet from 'react-native'; useRouter from 'expo-router'; useQuery from '@tanstack/react-query'.
    - Imports: listExercisesApi from '../../../lib/api/exercises.api', getWeeklyStatsApi from '../../../lib/api/workouts.api'; IExercise, ICategory from '../../../lib/api/types'.
    - Imports: ExerciseCard, CategoryFilterChip, WeeklyStatCard, DailyChallengeCard from '../../../components/ui/*'.
    - Imports: BACKGROUND, TEXT, TEXT_SECONDARY from '../../../constants/colors'.

    Component body:
    ```typescript
    const router = useRouter();
    const [activeFilter, setActiveFilter] = useState<ICategory | 'all'>('all');

    const FILTER_CHIPS: Array<{ id: ICategory | 'all'; label: string }> = [
      { id: 'all', label: 'Tất cả' },
      { id: 'yoga', label: 'Yoga' },
      { id: 'cardio', label: 'Cardio' },
      { id: 'weights', label: 'Tạ' },
      { id: 'stretching', label: 'Giãn cơ' },
    ];

    const { data: exercises, isLoading: exercisesLoading, error: exercisesError, refetch: refetchExercises } = useQuery({
      queryKey: ['exercises', activeFilter],
      queryFn: () => listExercisesApi(activeFilter === 'all' ? undefined : activeFilter),
    });

    const { data: stats } = useQuery({
      queryKey: ['workouts', 'stats', 'weekly'],
      queryFn: getWeeklyStatsApi,
    });
    ```

    Render structure (SafeAreaView with `flex: 1, backgroundColor: BACKGROUND`):
    1. Heading "Tập luyện" — 28dp bold, paddingHorizontal 16, marginTop 16.
    2. Weekly stats row: `View flexDirection: 'row', gap: 12, paddingHorizontal 16, marginTop 16`. Four `<WeeklyStatCard>` instances:
       - label="Ngày tập", value={stats?.days ?? 0}
       - label="Bài tập", value={stats?.exercises ?? 0}
       - label="kcal", value={stats?.kcal ?? 0}
       - label="Phút", value={stats?.minutes ?? 0}
    3. `<DailyChallengeCard targetKcal={stats?.targetKcal ?? 300} currentKcal={stats?.todayKcal ?? 0} />` — marginHorizontal 16.
    4. Category chips row: horizontal `<ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: 8, paddingHorizontal: 16, paddingVertical: 8 }}>` with `FILTER_CHIPS.map(chip => <CategoryFilterChip key={chip.id} label={chip.label} active={activeFilter === chip.id} onPress={() => setActiveFilter(chip.id)} />)`.
    5. Exercise list:
       - If `exercisesLoading`: render 3 skeleton placeholder Views (`bg-#E0E0E0` 100dp tall, 16 horizontal margin, 8 vertical margin, borderRadius 16).
       - If `exercisesError`: View centered with Text "Không thể tải danh sách bài tập. Kiểm tra kết nối và thử lại." + a Pressable "Thử lại" that calls `refetchExercises()`.
       - If `exercises.length === 0`: Text "Không có bài tập nào trong danh mục này." centered, text-secondary.
       - Else: FlatList with `data={exercises}, keyExtractor={(item) => item._id}, renderItem={({ item }) => <ExerciseCard exercise={item} onPress={() => router.push(`/(tabs)/exercises/${item._id}`)} />, ItemSeparatorComponent={() => <View style={{ height: 12 }} />}, contentContainerStyle={{ paddingHorizontal: 16, paddingBottom: 32 }}, scrollEnabled={false}` (the outer SafeAreaView/ScrollView handles scrolling; FlatList in non-scroll mode is acceptable for ~100 items per WO-11).

    Wrap content 2–5 in an outer `ScrollView` so the whole screen scrolls. Use `nestedScrollEnabled` if necessary, but with `FlatList scrollEnabled={false}` the outer ScrollView is sufficient.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist: `(tabs)/exercises/_layout.tsx`, `(tabs)/exercises/index.tsx`, `(tabs)/exercises/[id]/_layout.tsx`.
    - `grep -n "Tập luyện" mobile/src/app/\(tabs\)/exercises/index.tsx` matches (Vietnamese heading).
    - `grep -nE "(Ngày tập|Bài tập|kcal|Phút)" mobile/src/app/\(tabs\)/exercises/index.tsx` matches all 4 stat labels.
    - `grep -nE "(Tất cả|Yoga|Cardio|Tạ|Giãn cơ)" mobile/src/app/\(tabs\)/exercises/index.tsx` matches all 5 chip labels.
    - `grep -n "listExercisesApi\|getWeeklyStatsApi" mobile/src/app/\(tabs\)/exercises/index.tsx` matches both API imports.
    - `grep -n "router\.push" mobile/src/app/\(tabs\)/exercises/index.tsx` matches the card-press navigation.
    - `grep -n "Không có bài tập nào trong danh mục này\|Không thể tải danh sách bài tập" mobile/src/app/\(tabs\)/exercises/index.tsx` matches empty + error states (UI-SPEC §3.1 Copywriting).
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Exercise Detail screen — description, steps list, sticky "Bắt đầu tập" button</name>
  <files>mobile/src/app/(tabs)/exercises/[id]/index.tsx</files>
  <read_first>
    mobile/src/app/(onboarding)/screen-3.tsx (SafeAreaView + ScrollView + sticky bottom button shell)
    mobile/src/components/ui/ScreenHeader.tsx (back chevron + title pattern — used here with showBack=true)
    mobile/src/components/ui/PrimaryButton.tsx (52dp tall PrimaryButton — sticky "Bắt đầu tập")
    mobile/src/lib/api/exercises.api.ts (getExerciseApi signature)
    mobile/src/lib/api/types.ts (IExercise, IExerciseStep types)
    .planning/phases/03-core-health-tracking/03-UI-SPEC.md §3.2 (Exercise Detail screen layout — thumbnail area, metadata row, sections, sticky bottom button)
  </read_first>
  <behavior>
    Type-only verification gate; navigation handoff to /timer is implemented by Plan 07.
  </behavior>
  <action>
    `mobile/src/app/(tabs)/exercises/[id]/index.tsx`:
    - Imports: React; SafeAreaView, ScrollView, View, Text, ActivityIndicator, StyleSheet from 'react-native'; useLocalSearchParams, useRouter from 'expo-router'; Ionicons from '@expo/vector-icons'; useQuery from '@tanstack/react-query'.
    - Imports: getExerciseApi from '../../../../lib/api/exercises.api'; PrimaryButton from '../../../../components/ui/PrimaryButton'; ScreenHeader from '../../../../components/ui/ScreenHeader'; BACKGROUND, SURFACE, TEXT, TEXT_SECONDARY, PRIMARY, DIFFICULTY_EASY, DIFFICULTY_MEDIUM, DIFFICULTY_HARD from '../../../../constants/colors'.

    Component body:
    ```typescript
    const { id } = useLocalSearchParams<{ id: string }>();
    const router = useRouter();
    const { data: exercise, isLoading, error } = useQuery({
      queryKey: ['exercise', id],
      queryFn: () => getExerciseApi(id),
      enabled: !!id,
    });

    const handleStart = (): void => {
      router.push(`/(tabs)/exercises/${id}/timer`);   // Plan 07 implements this route
    };
    ```

    Use the same CATEGORY_ICON map as in Task 1 (or re-import from a shared constants file if extracted; inline is acceptable for this slice).

    Render structure (SafeAreaView flex: 1 bg-BACKGROUND):
    - `<ScreenHeader showBack title="" />` at the very top (back chevron only; the title is not strictly needed because the exercise name appears below).
    - Wrap the rest in `<View flex: 1>` then `<ScrollView contentContainerStyle={{ paddingBottom: 96 }}>` (the 96 padding accommodates the sticky button).
    - Thumbnail area: `<View height: 200, bg-surface, alignItems: 'center', justifyContent: 'center'>` with the category Ionicon at size 64, color TEXT_SECONDARY (since imageUrl=null per D-42).
    - Exercise name: `<Text style={{ fontSize: 28, fontWeight: '700', color: TEXT, paddingHorizontal: 16, marginTop: 16 }}>{exercise?.name}</Text>`.
    - Metadata row: difficulty pill + `"{durationMinutes} phút"` + `"{caloriesBurned} kcal"` — horizontal row with gap 8, paddingHorizontal 16, marginTop 8.
    - Section "Mô tả": `<Text fontSize: 20 fontWeight: '700'>Mô tả</Text>` then `<Text fontSize: 16 lineHeight: 24>{exercise?.description}</Text>` — both with paddingHorizontal 16, marginTop 24.
    - Section "Các động tác": same heading style, then `{exercise?.steps.map((step) => (<View key={step.order} ...>)} <Text fontWeight: '700'>{step.order}. {step.instruction}</Text> <Text fontSize: 14 color: TEXT_SECONDARY>{step.durationSeconds ?? 0} giây</Text></View>))` — paddingHorizontal 16, gap 12.
    - Optional section "Lưu ý": render only if a hardcoded note exists — for v1, omit (no `notes` field in IExercise; skip the section).
    - Loading state: ActivityIndicator centered, large.
    - Error state: Text "Không thể tải bài tập." centered.

    Sticky bottom button: outside the ScrollView, `<View style={{ position: 'absolute', bottom: 0, left: 0, right: 0, padding: 16, backgroundColor: BACKGROUND }}><PrimaryButton label="Bắt đầu tập" onPress={handleStart} /></View>`. Use SafeAreaView wrapping if there are notch concerns — `<SafeAreaView edges={['bottom']}>` from `react-native-safe-area-context` IF that package is available; otherwise plain View with bottom padding (the apex Phone's home indicator area is typically handled by the parent SafeAreaView via the screen flex layout).

    NOTE: the navigation target `/(tabs)/exercises/${id}/timer` is created by Plan 07. If Plan 07 has not landed yet at integration time, the executor verifies the route is reachable by typing it in (Expo Router warns "No route" but does not crash). This is acceptable mid-wave.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `mobile/src/app/(tabs)/exercises/[id]/index.tsx` exists.
    - `grep -n "useLocalSearchParams" mobile/src/app/\(tabs\)/exercises/\[id\]/index.tsx` matches.
    - `grep -n "getExerciseApi" mobile/src/app/\(tabs\)/exercises/\[id\]/index.tsx` matches.
    - `grep -n "Bắt đầu tập" mobile/src/app/\(tabs\)/exercises/\[id\]/index.tsx` matches the CTA label.
    - `grep -n "Mô tả\|Các động tác" mobile/src/app/\(tabs\)/exercises/\[id\]/index.tsx` matches both section headings.
    - `grep -n "/timer" mobile/src/app/\(tabs\)/exercises/\[id\]/index.tsx` matches the navigation push target (Plan 07 handoff).
    - `grep -n "step.order\|step.instruction" mobile/src/app/\(tabs\)/exercises/\[id\]/index.tsx` matches (steps list iterates the schema fields).
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → Backend GET /api/exercises | Bearer token auto-injected by apiClient interceptor |
| User input (search/filter) → query params | Mobile sends `category` string from a fixed enum — no free-text input |
| Display of backend strings → UI text nodes | exercise.name / description / steps are Vietnamese content from seed (no user-generated content) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-31 | Information Disclosure | Anonymous browsing of exercise library | mitigate | All API calls go through apiClient with auth interceptor; backend rejects with 401 if no token. AuthProvider routing (Phase 2) prevents unauthenticated users from reaching `(tabs)` group. |
| T-03-32 | Tampering | Filter chip values causing unexpected enum | mitigate | TypeScript narrowing — `activeFilter` is `ICategory \| 'all'`; passed as union-typed argument to listExercisesApi which only sends `category` when not 'all' |
| T-03-33 | Information Disclosure | Cached exercise data across users | mitigate | TanStack Query's queryClient is cleared on logout (already wired in AuthProvider Phase 2 — `queryClient.clear()` in logout) |
| T-03-34 | Spoofing | XSS via exercise.description | accept | React Native Text renders strings as text-only — no HTML interpretation; no XSS surface. (Seed data is server-controlled anyway.) |
| T-03-SC | Tampering | npm install of new dependencies | accept | No new dependencies — `@tanstack/react-query`, `expo-router`, `@expo/vector-icons` all from Phase 1/2 |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0.
- All 4 components exported with `default`.
- Exercise List screen pulls from `/api/exercises` and `/api/workouts/stats/weekly`; screen renders empty/error/loading states with Vietnamese copy.
- Exercise Detail screen pulls from `/api/exercises/:id`; sticky "Bắt đầu tập" button navigates to `/(tabs)/exercises/[id]/timer` (Plan 07 target).
- `grep -nrE "(Tập luyện|Bắt đầu tập|Tất cả|Yoga|Cardio|Tạ|Giãn cơ)" mobile/src/app/\(tabs\)/exercises/` matches.
</verification>

<success_criteria>
1. Tập luyện tab opens to Exercise List with weekly stats + daily challenge + 5 category chips + scrollable exercise list (WO-01, WO-02, WO-03, WO-04, WO-11).
2. Category chip toggle refetches list (TanStack Query queryKey changes).
3. Exercise card tap → Exercise Detail screen showing description + steps + "Bắt đầu tập" (WO-05).
4. "Bắt đầu tập" routes to /timer — Plan 07 picks up from there (WO-06).
5. All copy in Vietnamese (D-41 — exercise names are Vietnamese from seed; UI strings are Vietnamese in code).
6. `tsc --noEmit` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-core-health-tracking/03-06-SUMMARY.md` when done. Include: component prop signatures (4 components), screen file paths, the exact router.push target string used by both screens, the loading/empty/error copy strings, and a brief note confirming visual smoke test passed on iOS or Android device.
</output>
