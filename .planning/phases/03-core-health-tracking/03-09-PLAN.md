---
phase: 03-core-health-tracking
plan: 09
type: execute
wave: 3
depends_on: [03-05]
files_modified:
  - mobile/src/components/ui/BMIResultCard.tsx
  - mobile/src/components/ui/BMIScaleBar.tsx
  - mobile/src/components/ui/BMIChart.tsx
  - mobile/src/app/(tabs)/bmi/_layout.tsx
  - mobile/src/app/(tabs)/bmi/index.tsx
  - mobile/package.json
  - mobile/babel.config.js
autonomous: true
requirements: [BMI-01, BMI-02, BMI-03, BMI-04, BMI-05, BMI-06]
tags: [mobile, bmi, screens, components, sliders, victory-native, skia, tanstack-query, vertical-slice]

must_haves:
  truths:
    - "User opens the BMI tab and sees: heading 'Phân tích BMI', BMI result card with score + Vietnamese category label, BMI scale bar with current-position dot, height/weight sliders, 'Lưu số đo' button, advice card, 30-day bar chart"
    - "Sliders are initialized from useAuth().user.profile.heightCm (default 170) and useAuth().user.profile.weightKg (default 65)"
    - "Height slider range 100-220 cm; weight slider range 30-200 kg (D-56)"
    - "Slider drag recalculates BMI CLIENT-SIDE (no API call) and updates score / category / scale bar dot / advice text instantly (BMI-04, D-53)"
    - "BMI computation matches backend: weightKg / (heightCm/100)^2, rounded to 1 decimal place"
    - "BMI category mapping: <18.5 underweight ('Thiếu cân'), 18.5-24.9 normal ('Bình thường'), 25-29.9 overweight ('Thừa cân'), >=30 obese ('Béo phì') — Vietnamese display labels per BMI-01"
    - "'Lưu số đo' button triggers PATCH /api/bmi (D-53); on success: queryClient invalidates BMI history, shows toast 'Đã lưu!'; on error: FormErrorText 'Không thể lưu chỉ số BMI. Kiểm tra kết nối và thử lại.'"
    - "Advice card content changes based on BMI category — uses exact Vietnamese strings from UI-SPEC Copywriting (BMI-05)"
    - "30-day bar chart (BMI-06) uses victory-native v40 (pre-installed in mobile/package.json); bar color #4CAF50; reads from GET /api/bmi/history; empty-state copy 'Chưa có dữ liệu BMI. Nhập số đo và nhấn Lưu số đo để bắt đầu theo dõi.'"
    - "Reanimated plugin entry exists in babel.config.js (required for victory-native + Skia animations)"
  artifacts:
    - path: "mobile/src/components/ui/BMIResultCard.tsx"
      provides: "Card with BMI score + Vietnamese category label + embedded BMIScaleBar"
      exports: ["default"]
    - path: "mobile/src/components/ui/BMIScaleBar.tsx"
      provides: "Horizontal 4-segment bar (underweight/normal/overweight/obese colors) + position dot"
      exports: ["default"]
    - path: "mobile/src/components/ui/BMIChart.tsx"
      provides: "30-day bar chart using victory-native CartesianChart + Bar"
      exports: ["default"]
    - path: "mobile/src/app/(tabs)/bmi/index.tsx"
      provides: "Phân tích BMI screen — sliders, save, history chart, advice"
    - path: "mobile/src/app/(tabs)/bmi/_layout.tsx"
      provides: "Stack layout (headerShown: false)"
  key_links:
    - from: "mobile/src/app/(tabs)/bmi/index.tsx"
      to: "mobile/src/lib/api/bmi.api.ts (Plan 05)"
      via: "useQuery for /history; useMutation for PATCH /"
      pattern: "saveBMIApi|getBMIHistoryApi"
    - from: "mobile/src/components/ui/BMIChart.tsx"
      to: "victory-native + @shopify/react-native-skia"
      via: "CartesianChart + Bar imports"
      pattern: "from 'victory-native'"
    - from: "mobile/src/app/(tabs)/bmi/index.tsx"
      to: "@react-native-community/slider"
      via: "Slider component for height + weight inputs"
      pattern: "from '@react-native-community/slider'"

user_setup:
  - service: react-native-community-slider
    why: "Native slider component for height/weight inputs — React Native deprecated its built-in Slider; @react-native-community/slider is the replacement and is NOT currently in mobile/package.json"
    install_steps:
      - "cd mobile && npx expo install @react-native-community/slider"
    notes: "Use npx expo install (not npm install) so the right version is picked for Expo SDK 54. The package autolinks in Expo's prebuild; works in Expo Go out of the box."
---

<objective>
Mobile BMI vertical slice. After this plan, the BMI tab is fully functional: live BMI recalculation from sliders, atomic save to backend, 30-day bar chart, Vietnamese advice based on category.

Purpose: Delivers BMI-01 to BMI-06 backend-to-pixel. Wraps up Phase 3 — all 25 requirements covered after this plan ships.

Output: 3 new presentational components + 1 new screen + 1 stack layout file + 1 new mobile dependency (@react-native-community/slider) + babel config verification.

Chart library choice (LOCKED): victory-native@40.2.1 is already installed in mobile/package.json (verified during planning). @shopify/react-native-skia@1.12.4 (its peer dep) is also installed. No additional install needed for charting. Out of UI-SPEC's "gifted-charts OR victory-native" options, planner selects victory-native.

Slider library: React Native's deprecated Slider component is no longer in the core API. @react-native-community/slider IS REQUIRED and must be installed via npx expo install. Documented in user_setup frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-health-tracking/03-CONTEXT.md
@.planning/phases/03-core-health-tracking/03-UI-SPEC.md
@.planning/phases/03-core-health-tracking/03-PATTERNS.md
@.planning/phases/03-core-health-tracking/03-05-PLAN.md

@mobile/src/lib/api/types.ts
@mobile/src/lib/api/bmi.api.ts
@mobile/src/providers/AuthProvider.tsx
@mobile/src/components/ui/PrimaryButton.tsx
@mobile/src/components/ui/GoalCard.tsx
@mobile/src/components/ui/FormErrorText.tsx
@mobile/src/components/ui/ScreenHeader.tsx
@mobile/src/constants/colors.ts
@mobile/package.json
@mobile/app.json
@mobile/babel.config.js

<interfaces>
BMIResultCard props:
  { bmi: number; category: IBMICategory }

BMIScaleBar props:
  { bmi: number }

BMIChart props:
  { records: IBMIHistoryEntry[] }

Vietnamese category labels (CATEGORY_VI):
  underweight -> 'Thiếu cân'
  normal -> 'Bình thường'
  overweight -> 'Thừa cân'
  obese -> 'Béo phì'

Vietnamese advice copy (CATEGORY_ADVICE) per UI-SPEC Copywriting BMI module:
  underweight -> 'Hãy tăng cường dinh dưỡng và tập thể dục để đạt cân nặng khỏe mạnh.'
  normal -> 'Duy trì thói quen tốt và tiếp tục phát huy!'
  overweight -> 'Tăng cường vận động và điều chỉnh chế độ ăn uống để cải thiện sức khỏe.'
  obese -> 'Nên tham khảo ý kiến bác sĩ để có kế hoạch giảm cân an toàn.'

Pure functions duplicated client-side (must match backend behavior from Plan 03-04):
  computeBMI(heightCm, weightKg): Math.round((weightKg / ((heightCm/100) ** 2)) * 10) / 10
  categorizeBMI(bmi): <18.5 underweight, <25 normal, <30 overweight, >=30 obese
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install slider dep + verify babel reanimated plugin + sanity-check chart deps</name>
  <files>mobile/package.json, mobile/babel.config.js</files>
  <read_first>
    mobile/package.json (confirm victory-native@40.x and @shopify/react-native-skia present; @react-native-community/slider missing)
    mobile/babel.config.js (current content — Reanimated plugin must be the LAST plugin in the array per Reanimated docs)
    mobile/app.json (check expo.plugins array; no preemptive Skia plugin add required for Expo Go)
  </read_first>
  <behavior>
    Manual run. The shell command modifies package.json. Verification grep gates run afterward.
  </behavior>
  <action>
    Run `cd mobile && npx expo install @react-native-community/slider` from the executor's shell. This will modify mobile/package.json adding the dependency at the version Expo SDK 54 expects. Commit the lockfile change with this task.

    Inspect mobile/babel.config.js. The expected shape for Reanimated v3 + Expo SDK 54 is:
      module.exports = function(api) { api.cache(true); return { presets: ['babel-preset-expo'], plugins: ['react-native-reanimated/plugin'] }; };

    If mobile/babel.config.js does not contain 'react-native-reanimated/plugin', append it as the LAST plugin. If the file already includes nativewind or other plugins, keep reanimated last. If the file does not exist, create it with the snippet above. Do NOT modify existing presets — only ensure the reanimated plugin is present and last.

    Do NOT preemptively add @shopify/react-native-skia to expo.plugins in app.json; Expo Go autolinks at dev time. Document in SUMMARY if runtime issues surface during the visual smoke pass.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; node -e "const p=require('./package.json'); if(!p.dependencies['@react-native-community/slider'])throw new Error('slider missing'); if(!p.dependencies['victory-native'])throw new Error('victory-native missing'); if(!p.dependencies['@shopify/react-native-skia'])throw new Error('skia missing'); console.log('deps ok')" &amp;&amp; node -e "const fs=require('fs'); const c=fs.readFileSync('babel.config.js','utf8'); if(!/react-native-reanimated\/plugin/.test(c))throw new Error('reanimated plugin missing'); console.log('babel ok')"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/package.json contains @react-native-community/slider in dependencies.
    - mobile/package.json still contains victory-native@^40.x and @shopify/react-native-skia@^1.x (no accidental removal).
    - mobile/babel.config.js contains the literal string 'react-native-reanimated/plugin' as the LAST entry in its plugins array.
    - The two node -e checks above exit 0 (deps ok / babel ok).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 3 presentational components (BMIResultCard, BMIScaleBar, BMIChart)</name>
  <files>mobile/src/components/ui/BMIResultCard.tsx, mobile/src/components/ui/BMIScaleBar.tsx, mobile/src/components/ui/BMIChart.tsx</files>
  <read_first>
    mobile/src/components/ui/GoalCard.tsx (card shell with bg-surface + borderRadius)
    mobile/src/components/ui/PrimaryButton.tsx (StyleSheet conventions)
    mobile/src/lib/api/types.ts (IBMICategory, IBMIHistoryEntry types)
    mobile/src/constants/colors.ts (PRIMARY, BMI_UNDERWEIGHT, BMI_NORMAL, BMI_OVERWEIGHT, BMI_OBESE, TEXT, TEXT_SECONDARY, SURFACE — added Plan 05)
    mobile/node_modules/victory-native/dist/index.d.ts (confirmed exports: CartesianChart, Bar, plus types InputDatum, ChartBounds, PointsArray)
    .planning/phases/03-core-health-tracking/03-UI-SPEC.md section 3.6 (full layout — BMI card, scale bar segments, sliders, advice, chart styling, empty state)
  </read_first>
  <behavior>
    Type-only verification gate; visual smoke happens in Task 3 when the BMI screen renders all three.

    BMIResultCard: View card bg-surface, borderRadius 16, shadow, padding 16. Top row: BMI score (Heading/20/700, PRIMARY color) + category label (Body/16) horizontally. Below: child component BMIScaleBar passed the bmi number. Below scale bar: scale range labels "15" and "40" at left/right ends (Label/14, TEXT_SECONDARY).

    BMIScaleBar: View flexDirection: 'row', height 8, borderRadius 4, overflow: 'hidden'. 4 child Views (equal width via flex:1) with backgroundColor in order: BMI_UNDERWEIGHT, BMI_NORMAL, BMI_OVERWEIGHT, BMI_OBESE. Position dot: absolutely positioned View, width 16, height 16, borderRadius 8, borderWidth 2, borderColor PRIMARY, backgroundColor white, top -4 (so it overlaps the bar vertically centered). Left position computed as: `Math.min(100, Math.max(0, ((bmi - 15) / 25) * 100))` — bmi 15 -> 0%, bmi 40 -> 100%, clamped. Use inline `style={{ left: \`${percent}%\` }}`.

    BMIChart: Uses victory-native CartesianChart + Bar.
      Imports:
        import { CartesianChart, Bar } from 'victory-native';
        import { useFont } from '@shopify/react-native-skia';
      Props records: IBMIHistoryEntry[]. If records.length === 0, render Text "Chưa có dữ liệu BMI. Nhập số đo và nhấn Lưu số đo để bắt đầu theo dõi." centered, color TEXT_SECONDARY, padding 24. Else:
        const data = records.map((r, idx) => ({ x: idx, bmi: r.bmi }));
        Return a View height 220, then:
          <CartesianChart data={data} xKey="x" yKeys={["bmi"]} domainPadding={{ left: 16, right: 16, top: 8, bottom: 8 }}>
            {({ points, chartBounds }) => (
              <Bar
                points={points.bmi}
                chartBounds={chartBounds}
                color="#4CAF50"
                roundedCorners={{ topLeft: 4, topRight: 4 }}
              />
            )}
          </CartesianChart>
      No axis labels for v1 (UI-SPEC mentions "date labels every 7 days" — optional v1 polish; skip if it complicates Skia font loading). If axis font is needed, useFont with a system fallback or just omit axes to avoid runtime font issues.

    Note: victory-native v40 uses a Skia render context — components MUST be rendered inside an Expo app at runtime (not at unit test time). The TS compile gate is sufficient for this plan.
  </behavior>
  <action>
    Create the 3 component files at mobile/src/components/ui/.

    BMIResultCard imports React; View, Text, StyleSheet from 'react-native'; IBMICategory from '../../lib/api/types'; PRIMARY, SURFACE, TEXT, TEXT_SECONDARY from '../../constants/colors'; BMIScaleBar from './BMIScaleBar'. Define inline CATEGORY_VI map.

    BMIScaleBar imports React; View, StyleSheet from 'react-native'; BMI_UNDERWEIGHT, BMI_NORMAL, BMI_OVERWEIGHT, BMI_OBESE, PRIMARY from '../../constants/colors'.

    BMIChart imports React; View, Text, StyleSheet from 'react-native'; CartesianChart, Bar from 'victory-native'; IBMIHistoryEntry from '../../lib/api/types'; TEXT_SECONDARY from '../../constants/colors'.

    Each is a default-exported functional component. No accessibility concerns for these specific components beyond standard rules (they are display-only — neither receives focus or input).
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 3 component files exist.
    - `grep -n "Thiếu cân\|Bình thường\|Thừa cân\|Béo phì" mobile/src/components/ui/BMIResultCard.tsx` matches all 4 Vietnamese category labels.
    - `grep -nE "BMI_UNDERWEIGHT|BMI_NORMAL|BMI_OVERWEIGHT|BMI_OBESE" mobile/src/components/ui/BMIScaleBar.tsx` matches all 4 segment color constants.
    - `grep -n "((bmi - 15) / 25) \* 100\|(bmi-15)/25" mobile/src/components/ui/BMIScaleBar.tsx` matches the position-formula expression.
    - `grep -n "from 'victory-native'" mobile/src/components/ui/BMIChart.tsx` matches.
    - `grep -n "CartesianChart\|Bar" mobile/src/components/ui/BMIChart.tsx` matches both imports.
    - `grep -n "Chưa có dữ liệu BMI\. Nhập số đo và nhấn Lưu số đo để bắt đầu theo dõi\." mobile/src/components/ui/BMIChart.tsx` matches (UI-SPEC empty state copy).
    - `grep -n "#4CAF50\|'#4CAF50'" mobile/src/components/ui/BMIChart.tsx` matches (bar color).
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: BMI screen — sliders, live recalc, save mutation, history chart, advice</name>
  <files>mobile/src/app/(tabs)/bmi/_layout.tsx, mobile/src/app/(tabs)/bmi/index.tsx</files>
  <read_first>
    mobile/src/lib/api/bmi.api.ts (saveBMIApi, getBMIHistoryApi signatures)
    mobile/src/lib/api/types.ts (IBMICategory, IBMIHistoryEntry, ISaveBMIResponse)
    mobile/src/providers/AuthProvider.tsx (useAuth hook — for initial heightCm/weightKg defaults)
    mobile/src/components/ui/BMIResultCard.tsx + BMIScaleBar.tsx + BMIChart.tsx (just created)
    mobile/src/components/ui/PrimaryButton.tsx (CTA used for Lưu số đo)
    mobile/src/components/ui/FormErrorText.tsx (error display)
    .planning/phases/03-core-health-tracking/03-UI-SPEC.md section 3.6 (full screen layout, copywriting, interaction)
    .planning/phases/03-core-health-tracking/03-CONTEXT.md (D-53, D-54, D-55, D-56)
    mobile/src/app/(onboarding)/_layout.tsx (Stack layout pattern)
    mobile/src/app/(tabs)/index.tsx (SafeAreaView pattern)
  </read_first>
  <behavior>
    Manual smoke required:
    - Open BMI tab. Sliders show profile values (or defaults 170/65 for a new user). BMI calculates live.
    - Drag height slider — BMI score / category / dot position / advice all update instantly without network call.
    - Tap "Lưu số đo" — PrimaryButton enters loading state. On success, toast "Đã lưu!" appears for 2s; chart refetches and shows the new bar.
    - On error (force airplane mode), FormErrorText "Không thể lưu chỉ số BMI. Kiểm tra kết nối và thử lại." appears below the save button.
    - For a brand-new user with no history, the chart shows the empty-state copy.
  </behavior>
  <action>
    mobile/src/app/(tabs)/bmi/_layout.tsx:
      import { Stack } from 'expo-router'; export default function BMILayout() { return <Stack screenOptions={{ headerShown: false }} />; }

    mobile/src/app/(tabs)/bmi/index.tsx:
    - Imports: React, useState, useMemo; SafeAreaView, ScrollView, View, Text, StyleSheet, ActivityIndicator from 'react-native'; Slider from '@react-native-community/slider'; useQuery, useMutation, useQueryClient from '@tanstack/react-query'.
    - Imports: saveBMIApi, getBMIHistoryApi from '../../../lib/api/bmi.api'; IBMICategory from '../../../lib/api/types'; useAuth from '../../../providers/AuthProvider'; PrimaryButton from '../../../components/ui/PrimaryButton'; FormErrorText from '../../../components/ui/FormErrorText'; BMIResultCard from '../../../components/ui/BMIResultCard'; BMIChart from '../../../components/ui/BMIChart'; BACKGROUND, PRIMARY, SURFACE, TEXT, TEXT_SECONDARY from '../../../constants/colors'.

    Define inline pure helpers (mirroring backend bmi.service.ts):
      function computeBMI(heightCm: number, weightKg: number): number { return Math.round((weightKg / ((heightCm / 100) ** 2)) * 10) / 10; }
      function categorizeBMI(bmi: number): IBMICategory { if (bmi < 18.5) return 'underweight'; if (bmi < 25) return 'normal'; if (bmi < 30) return 'overweight'; return 'obese'; }

    Define inline:
      const CATEGORY_VI: Record<IBMICategory, string> = { underweight: 'Thiếu cân', normal: 'Bình thường', overweight: 'Thừa cân', obese: 'Béo phì' };
      const CATEGORY_ADVICE: Record<IBMICategory, string> = { underweight: 'Hãy tăng cường dinh dưỡng và tập thể dục để đạt cân nặng khỏe mạnh.', normal: 'Duy trì thói quen tốt và tiếp tục phát huy!', overweight: 'Tăng cường vận động và điều chỉnh chế độ ăn uống để cải thiện sức khỏe.', obese: 'Nên tham khảo ý kiến bác sĩ để có kế hoạch giảm cân an toàn.' };

    Component body:
      const auth = useAuth();
      const profileHeight = (auth.user as any)?.profile?.heightCm ?? 170;
      const profileWeight = (auth.user as any)?.profile?.weightKg ?? 65;
      const [heightCm, setHeightCm] = useState<number>(Math.max(100, Math.min(220, profileHeight)));
      const [weightKg, setWeightKg] = useState<number>(Math.max(30, Math.min(200, profileWeight)));
      const [toastVisible, setToastVisible] = useState<boolean>(false);
      const [saveError, setSaveError] = useState<string | null>(null);

      const bmi = useMemo(() => computeBMI(heightCm, weightKg), [heightCm, weightKg]);
      const category = useMemo(() => categorizeBMI(bmi), [bmi]);

      const qc = useQueryClient();
      const historyQuery = useQuery({ queryKey: ['bmi', 'history'], queryFn: getBMIHistoryApi });

      const mutation = useMutation({
        mutationFn: () => saveBMIApi(heightCm, weightKg),
        onSuccess: () => {
          setSaveError(null);
          setToastVisible(true);
          setTimeout(() => setToastVisible(false), 2000);
          qc.invalidateQueries({ queryKey: ['bmi', 'history'] });
          // Update auth user's profile in-place so other screens see fresh values
          if (auth.user) {
            // The user object is owned by AuthProvider; rather than mutate, rely on next refresh.
            // For v1, the response already comes back with user.{heightCm, weightKg}; documentation only.
          }
        },
        onError: () => {
          setSaveError('Không thể lưu chỉ số BMI. Kiểm tra kết nối và thử lại.');
        },
      });

    Render structure (SafeAreaView flex: 1, bg-BACKGROUND):
    - ScrollView contentContainerStyle paddingBottom 32:
      - Heading "Phân tích BMI" (28/700, paddingHorizontal 16, marginTop 16).
      - <BMIResultCard bmi={bmi} category={category} /> (marginHorizontal 16, marginTop 16).
      - Sliders section: View bg-surface, borderRadius 16, marginHorizontal 16, marginTop 16, padding 16. Heading "Cập nhật số đo" (Heading/20/700). Two slider blocks:
        - "Chiều cao" label + value text `${heightCm} cm` (PRIMARY color). <Slider minimumValue={100} maximumValue={220} step={1} value={heightCm} onValueChange={setHeightCm} minimumTrackTintColor={PRIMARY} maximumTrackTintColor="#E0E0E0" />
        - "Cân nặng" label + value text `${weightKg} kg`. Same Slider props with min=30 max=200 step=1.
      - "Lưu số đo" button: <PrimaryButton label="Lưu số đo" onPress={() => mutation.mutate()} loading={mutation.isPending} /> — marginHorizontal 16, marginTop 16. Below button: if saveError, <FormErrorText message={saveError} />.
      - Advice card: View bg-surface, borderRadius 16, marginHorizontal 16, marginTop 16, padding 16. Heading "Lời khuyên" (Heading/20/700). Body Text CATEGORY_ADVICE[category] (Body/16).
      - Chart section: View paddingHorizontal 16, marginTop 16. Heading "Lịch sử 30 ngày" (Heading/20/700). Container View bg-surface, borderRadius 16, padding 16, marginTop 8: <BMIChart records={historyQuery.data ?? []} />.
    - Toast: if toastVisible, an absolutely-positioned View at bottom: 80, alignSelf: 'center', bg-PRIMARY, borderRadius 24, paddingHorizontal 24, paddingVertical 12. Text "Đã lưu!" white, fontWeight 700.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File mobile/src/app/(tabs)/bmi/index.tsx exists.
    - File mobile/src/app/(tabs)/bmi/_layout.tsx exists.
    - `grep -n "Phân tích BMI" mobile/src/app/\(tabs\)/bmi/index.tsx` matches the heading.
    - `grep -nE "(Chiều cao|Cân nặng|Lưu số đo|Đã lưu!|Lời khuyên|Lịch sử 30 ngày|Cập nhật số đo)" mobile/src/app/\(tabs\)/bmi/index.tsx` matches all 7 Vietnamese strings.
    - `grep -n "minimumValue={100}\|maximumValue={220}" mobile/src/app/\(tabs\)/bmi/index.tsx` matches the height slider range (D-56).
    - `grep -n "minimumValue={30}\|maximumValue={200}" mobile/src/app/\(tabs\)/bmi/index.tsx` matches the weight slider range (D-56).
    - `grep -n "computeBMI\|categorizeBMI" mobile/src/app/\(tabs\)/bmi/index.tsx` matches (client-side computation present).
    - `grep -n "saveBMIApi\|mutation.mutate" mobile/src/app/\(tabs\)/bmi/index.tsx` matches (PATCH wired).
    - `grep -n "invalidateQueries" mobile/src/app/\(tabs\)/bmi/index.tsx` matches (history refetch on save).
    - `grep -n "Không thể lưu chỉ số BMI\. Kiểm tra kết nối và thử lại\." mobile/src/app/\(tabs\)/bmi/index.tsx` matches.
    - `grep -n "Hãy tăng cường dinh dưỡng\|Duy trì thói quen tốt\|Tăng cường vận động\|Nên tham khảo ý kiến bác sĩ" mobile/src/app/\(tabs\)/bmi/index.tsx` matches all 4 advice strings (BMI-05).
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → Backend PATCH /api/bmi | Authenticated request; backend recomputes BMI server-side and ignores any client-supplied bmi/category fields |
| Sliders → screen state | User-controlled but bounded by Slider min/max in native code |
| Chart render path → Skia | Trusted local rendering; no external resource loaded by victory-native at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-45 | Tampering | Client computes BMI and category but only sends heightCm/weightKg | mitigate | saveBMIApi sends only { heightCm, weightKg } (verified by reading Plan 03-05 api shape); backend strict() schema + server-side computation in Plan 03-04 ignore any extra fields |
| T-03-46 | Tampering | Out-of-range slider values bypassing Zod | mitigate (defense-in-depth) | Native Slider component clamps to min/max props; backend Zod additionally rejects with 400 |
| T-03-47 | Information Disclosure | BMI history visible across users | mitigate | Plan 03-04 backend $match enforces userId scope; client cannot fetch other users' history |
| T-03-48 | Information Disclosure | useAuth().user spreads passwordHash to UI | mitigate | IAuthUser type definition (Phase 2) only declares id/email/name/role/profileCompleted — no password fields. Reading profileHeight/profileWeight via `as any` is a TS escape hatch only because the IAuthUser type does not include `profile`; a follow-up PR in Phase 5 (PRO-* requirements) will extend IAuthUser with the profile field. For now, default-value fallback (170/65) ensures correctness even if `profile` is undefined. Document this in SUMMARY. |
| T-03-49 | Denial of Service | Spamming "Lưu số đo" creates many BMIRecords | accept | Per D-53 every save creates a record by design (1 per user-initiated save). Backend's 30-day history aggregation (D-55, Plan 03-04) collapses same-day records to the latest, so chart UX remains sane. Future v2 could throttle. |
| T-03-SC | Tampering | npm install of @react-native-community/slider | mitigate | Official React Native Community package (org: react-native-community on npmjs); >1M weekly downloads; installed via `npx expo install` which selects the SDK 54 compatible version; not a SUS/SLOP package. Documented in user_setup. victory-native@40 and @shopify/react-native-skia@1.12.4 already vetted (in mobile/package.json since Phase 1). |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0.
- All 25 Phase 3 requirement IDs covered across plans 01-09 (verified by aggregating `requirements:` frontmatter across all 9 plans — see ROADMAP coverage).
- All 4 Vietnamese category labels + 4 Vietnamese advice strings + 7 Vietnamese UI strings present in bmi/index.tsx.
- Sliders enforce D-56 ranges (100-220 / 30-200).
- victory-native v40 + Skia + slider deps present in package.json.
- Reanimated plugin entry confirmed last in babel.config.js plugins array.
- Manual smoke documented in SUMMARY.
</verification>

<success_criteria>
1. BMI tab renders Phân tích BMI screen with sliders, result card, advice, and 30-day chart.
2. Sliders drive client-side BMI recompute (no API call) — score / category / dot position / advice all update live (BMI-03, BMI-04, D-53).
3. "Lưu số đo" calls PATCH /api/bmi atomically (BMIRecord + User.profile) — backend enforces D-54.
4. 30-day bar chart pulls from /api/bmi/history; empty-state copy for new users (BMI-06).
5. Vietnamese category labels + advice copy match UI-SPEC (BMI-01, BMI-05).
6. Slider ranges enforce D-56 (100-220 cm / 30-200 kg).
7. `tsc --noEmit` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-core-health-tracking/03-09-SUMMARY.md` when done. Include: the version of @react-native-community/slider installed, the babel.config.js plugin list after this plan, the BMI category boundaries used client-side, and confirmation that the manual smoke pass showed sliders + chart + save + toast working on iOS or Android.
</output>
