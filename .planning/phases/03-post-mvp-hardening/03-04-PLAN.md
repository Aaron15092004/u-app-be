---
phase: 03-post-mvp-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 02-02
  - 02-03
files_modified:
  - backend/src/api/ratings/ratings.controller.ts
  - backend/src/api/ratings/ratings.service.ts
  - backend/src/models/FeedbackPromptState.ts
  - mobile/src/lib/api/v2-contracts.api.ts
  - mobile/src/components/ui/AppRatingPrompt.tsx
  - mobile/src/constants/config.ts
  - mobile/app.json
autonomous: true
requirements:
  - RATE-01
  - RATE-02
  - RATE-03
  - RATE-05
requirements_addressed:
  - RATE-01
  - RATE-02
  - RATE-03
  - RATE-05
must_haves:
  truths:
    - "D-11: Native store review may be triggered only after positive internal feedback of 4 or 5 stars."
    - "D-12: Rating prompt dismiss/submit cooldown is 14 days."
    - "D-13: If native store review is unavailable or not displayed, fall back to App Store / Google Play links."
  artifacts:
    - path: "mobile/src/components/ui/AppRatingPrompt.tsx"
      provides: "Internal rating prompt and positive-review bridge"
    - path: "backend/src/models/FeedbackPromptState.ts"
      provides: "Cooldown/submission state"
---

<objective>
Enable policy-safe native store review prompting after positive internal feedback, with 14-day prompt cooldown and store-link fallback.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-post-mvp-hardening/03-CONTEXT.md
@.planning/phases/03-post-mvp-hardening/03-RESEARCH.md
@backend/src/api/ratings/ratings.controller.ts
@backend/src/api/ratings/ratings.service.ts
@backend/src/models/AppRating.ts
@backend/src/models/FeedbackPromptState.ts
@mobile/src/lib/api/v2-contracts.api.ts
@mobile/src/components/ui/AppRatingPrompt.tsx
@mobile/src/constants/config.ts
@mobile/app.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce 14-day rating prompt cooldown</name>
  <files>backend/src/api/ratings/ratings.controller.ts, backend/src/api/ratings/ratings.service.ts, backend/src/models/FeedbackPromptState.ts</files>
  <behavior>
    - Prompt status is eligible only after meaningful usage and outside cooldown.
    - Dismiss and submit both set a 14-day cooldown window.
    - Existing rating submit still stores stars/comment/trigger/app version/platform.
  </behavior>
  <action>Extend rating prompt status and dismiss/submit handling so mobile can reliably know eligibility and cooldown. Keep the trigger-count storage already present.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>Prompt cadence is controlled server-side.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger native store review only after positive internal rating</name>
  <files>mobile/src/components/ui/AppRatingPrompt.tsx, mobile/src/lib/api/v2-contracts.api.ts</files>
  <behavior>
    - Only 4-star and 5-star internal submissions attempt native store review.
    - Lower ratings submit internally without store review.
    - Prompt is not invoked from a direct "review us" button.
    - Failures do not block internal rating submission.
  </behavior>
  <action>After successful internal rating submission, call `expo-store-review` capability checks and request review only for positive ratings. Record `storeReviewRequested` in the existing rating payload where available.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Positive users may see native store review without violating prompt timing rules.</done>
</task>

<task type="auto">
  <name>Task 3: Add store URL fallback configuration</name>
  <files>mobile/src/constants/config.ts, mobile/app.json, mobile/src/components/ui/AppRatingPrompt.tsx</files>
  <behavior>
    - If native review has no action or errors, app can open configured App Store / Google Play URL.
    - Missing store URLs degrade silently or with a concise Vietnamese message.
    - Store URLs are public configuration, not secrets.
  </behavior>
  <action>Add app config/constants for store URLs and fallback linking. Keep placeholders clearly named if production IDs are not yet known.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Native review unavailable path is not a dead end.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
|---|---|---|
| Review prompt spam | Prompt state | Server cooldown and trigger threshold. |
| Platform policy violation | Mobile prompt | Native review only after signature positive internal feedback. |
| Broken store redirect | Config | Public configurable store URLs with graceful fallback. |
</threat_model>

<verification>
- `cd backend && npm run typecheck`
- `cd mobile && npx tsc --noEmit`
- Manual smoke: dismiss cooldown, 3-star submit, 4/5-star submit, unavailable native review fallback.
</verification>

<success_criteria>
- Internal rating remains the first step.
- Native review is gated to 4/5 stars.
- Cooldown is 14 days for dismiss and submit.
- Store-link fallback exists.
</success_criteria>

<source_audit>
REQ: RATE-01, RATE-02, RATE-03, RATE-05.
CONTEXT: D-11, D-12, D-13, D-19.
</source_audit>

<output>
Create `.planning/phases/03-post-mvp-hardening/03-04-SUMMARY.md` when done.
</output>
