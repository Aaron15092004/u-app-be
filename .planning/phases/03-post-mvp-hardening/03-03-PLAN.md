---
phase: 03-post-mvp-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/src/api/campaigns/campaigns.controller.ts
  - backend/src/api/campaigns/campaigns.service.ts
  - backend/src/api/ratings/ratings.controller.ts
  - backend/src/api/ratings/ratings.service.ts
  - admin/src/pages/CampaignsPage.tsx
  - admin/src/pages/RatingsPage.tsx
  - admin/src/features/campaigns/useCampaigns.ts
  - admin/src/features/ratings/useRatings.ts
  - admin/src/components/data-table/DataTable.tsx
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - RATE-04
requirements_addressed:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - RATE-04
must_haves:
  truths:
    - "D-08: Campaign/code admin dashboard shows total codes, redeemed rate, active campaigns, and campaigns nearing expiry."
    - "D-09: Rating feedback dashboard shows average rating, rating distribution, and recent comments."
    - "D-10: Exercise media admin hardening is handled by Plan 03-05, not this dashboard plan."
  artifacts:
    - path: "admin/src/pages/CampaignsPage.tsx"
      provides: "Campaign/code operations dashboard"
    - path: "admin/src/pages/RatingsPage.tsx"
      provides: "Internal feedback dashboard"
---

<objective>
Upgrade admin operations from MVP CRUD/list screens to compact campaign/code and rating dashboards that make launch operations visible.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-post-mvp-hardening/03-CONTEXT.md
@.planning/phases/03-post-mvp-hardening/03-RESEARCH.md
@backend/src/api/campaigns/campaigns.controller.ts
@backend/src/api/campaigns/campaigns.service.ts
@backend/src/api/ratings/ratings.controller.ts
@backend/src/api/ratings/ratings.service.ts
@admin/src/pages/CampaignsPage.tsx
@admin/src/pages/RatingsPage.tsx
@admin/src/features/campaigns/useCampaigns.ts
@admin/src/features/ratings/useRatings.ts
@admin/src/components/data-table/DataTable.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add campaign/code ops stats endpoints</name>
  <files>backend/src/api/campaigns/campaigns.controller.ts, backend/src/api/campaigns/campaigns.service.ts</files>
  <behavior>
    - Admin can fetch total codes, redeemed count/rate, active campaigns, and near-expiry campaigns.
    - Metrics use existing Campaign/RedeemCode models and respect admin auth.
    - Existing campaign list/generate/revoke/export behavior remains unchanged.
  </behavior>
  <action>Add a small admin stats endpoint under campaigns. Keep aggregation queries explicit and indexed fields friendly.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd backend && npm run test:admin</automated>
  </verify>
  <done>Campaign operations metrics are available to admin UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rating feedback stats endpoint</name>
  <files>backend/src/api/ratings/ratings.controller.ts, backend/src/api/ratings/ratings.service.ts</files>
  <behavior>
    - Admin can fetch average rating, star distribution, and recent comments.
    - Recent comments include user, rating, comment, feature trigger, created date, app version, and platform where present.
    - Empty data returns zero/empty states rather than errors.
  </behavior>
  <action>Add a concise ratings dashboard response alongside the existing admin list endpoint.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd backend && npm run test:admin</automated>
  </verify>
  <done>Feedback dashboard data is queryable.</done>
</task>

<task type="auto">
  <name>Task 3: Render admin operations dashboards</name>
  <files>admin/src/pages/CampaignsPage.tsx, admin/src/pages/RatingsPage.tsx, admin/src/features/campaigns/useCampaigns.ts, admin/src/features/ratings/useRatings.ts</files>
  <behavior>
    - Campaign page shows metric cards/compact panels above existing table.
    - Ratings page shows average, distribution, and recent comments above existing list.
    - Loading, empty, and error states follow existing admin patterns.
    - UI remains dense, operational, and not marketing-styled.
  </behavior>
  <action>Add React Query hooks for the new stats endpoints and render compact dashboard sections reusing existing admin components/styles where possible.</action>
  <verify>
    <automated>cd admin && npx tsc --noEmit</automated>
  </verify>
  <done>Admin can monitor campaign/code and rating health at a glance.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
|---|---|---|
| Sensitive feedback exposure | Admin ratings | Keep endpoints under admin auth and reuse existing user projection. |
| Expensive dashboard queries | Backend stats | Use aggregate/count queries and pagination for comments. |
| Misleading ops metrics | Campaign stats | Define redeemed rate denominator and handle zero-code campaigns. |
</threat_model>

<verification>
- `cd backend && npm run typecheck`
- `cd backend && npm run test:admin`
- `cd admin && npx tsc --noEmit`
- Manual smoke: campaign dashboard with empty/active/near-expiry data; ratings dashboard with empty and populated comments.
</verification>

<success_criteria>
- Admin sees campaign/code ops metrics.
- Admin sees rating feedback health.
- Existing CRUD/list/export flows still work.
</success_criteria>

<source_audit>
REQ: CODE-01, CODE-02, CODE-03, CODE-04, CODE-05, RATE-04.
CONTEXT: D-08, D-09, D-10, D-19.
</source_audit>

<output>
Create `.planning/phases/03-post-mvp-hardening/03-03-SUMMARY.md` when done.
</output>
