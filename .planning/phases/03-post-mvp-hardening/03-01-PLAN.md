---
phase: 03-post-mvp-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - 02-01
  - 02-03
files_modified:
  - backend/src/api/campaigns/campaigns.controller.ts
  - backend/src/api/campaigns/campaigns.service.ts
  - backend/src/api/campaigns/campaigns.validation.ts
  - backend/src/services/redeem-code.service.ts
  - backend/src/models/RedeemCode.ts
  - backend/src/models/UserScanEntitlement.ts
  - mobile/src/components/ui/RedeemCodeCard.tsx
  - mobile/src/components/ui/ScanEntitlementBadge.tsx
  - mobile/src/lib/api/v2-contracts.api.ts
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - CODE-06
  - CODE-07
  - CODE-08
  - CODE-09
  - CODE-10
  - CODE-11
  - CODE-12
requirements_addressed:
  - CODE-06
  - CODE-07
  - CODE-08
  - CODE-09
  - CODE-10
  - CODE-11
  - CODE-12
must_haves:
  truths:
    - "D-01: Phase 3 redeem hardening is manual-code only; do not implement QR redeem, deep links, or QR scanner."
    - "D-02: Manual code input gets formatting, paste handling, clear success/error states, and concise Vietnamese copy."
    - "D-03: Redeem errors are friendly and status-specific for invalid, already used, expired, revoked, and unauthenticated states."
    - "D-17: Redeem/quota abuse edge cases are the first hardening priority."
    - "D-18: Add redeem anti-abuse rate limiting by user/IP without a separate lockout workflow unless implementation requires it."
    - "CODE-07 is explicitly deferred by D-01; this plan prevents accidental QR scope creep while preserving manual redeem."
  artifacts:
    - path: "backend/src/api/campaigns/campaigns.service.ts"
      provides: "Atomic redeem, status-specific failures, and anti-abuse checks"
    - path: "mobile/src/components/ui/RedeemCodeCard.tsx"
      provides: "Manual redeem input and Vietnamese UX polish"
---

<objective>
Harden manual campaign-code redemption after the MVP launch: better mobile input UX, clearer status-specific feedback, and backend anti-abuse controls around redeem attempts.

This plan deliberately does not build QR redeem, deep links, or QR scanner behavior.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-post-mvp-hardening/03-CONTEXT.md
@.planning/phases/03-post-mvp-hardening/03-RESEARCH.md
@backend/src/api/campaigns/campaigns.controller.ts
@backend/src/api/campaigns/campaigns.service.ts
@backend/src/api/campaigns/campaigns.validation.ts
@backend/src/services/redeem-code.service.ts
@backend/src/models/RedeemCode.ts
@backend/src/models/UserScanEntitlement.ts
@mobile/src/components/ui/RedeemCodeCard.tsx
@mobile/src/components/ui/ScanEntitlementBadge.tsx
@mobile/src/lib/api/v2-contracts.api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add redeem anti-abuse guardrails</name>
  <files>backend/src/api/campaigns/campaigns.controller.ts, backend/src/api/campaigns/campaigns.service.ts, backend/src/api/campaigns/campaigns.validation.ts</files>
  <behavior>
    - Rate-limit redeem attempts by authenticated user and IP.
    - Preserve atomic single-use redeem semantics and hashed-code lookup.
    - Return stable error codes/messages for invalid, already used, expired, revoked, inactive campaign, unauthenticated, and rate-limited cases.
    - Do not introduce QR/deep-link code paths.
  </behavior>
  <action>Implement a lightweight redeem-attempt limiter using the existing middleware/service style. Prefer an in-memory or existing rate-limit pattern if present; otherwise keep it isolated to redeem controller/service and easy to replace later. Add explicit error codes to the redeem response contract while keeping Vietnamese messages.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>Redeem abuse attempts are throttled without weakening atomic redemption.</done>
</task>

<task type="auto">
  <name>Task 2: Polish manual redeem input UX</name>
  <files>mobile/src/components/ui/RedeemCodeCard.tsx, mobile/src/components/ui/ScanEntitlementBadge.tsx, mobile/src/lib/api/v2-contracts.api.ts</files>
  <behavior>
    - Input automatically normalizes casing and spacing but preserves user intent while typing/pasting.
    - Paste of grouped campaign codes works.
    - Loading, success, and all known failure states have concise Vietnamese copy.
    - Active entitlement display refreshes after successful redemption.
  </behavior>
  <action>Update `RedeemCodeCard` with deterministic formatting/paste handling, disabled states, and status-specific copy from the backend contract. Keep UI compact and aligned with existing Profile/Food entry points.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>User can enter/paste a bottle code and understand the result immediately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover redeem edge cases with integration tests</name>
  <files>backend/src/api/campaigns/campaigns.integration.test.ts, backend/package.json</files>
  <behavior>
    - Tests cover invalid, already used, expired, revoked, unauthenticated, inactive campaign, and rate-limited attempts.
    - Tests verify successful redeem still creates an entitlement and increments campaign redeemed count once.
    - Tests verify code hash/plaintext safety behavior remains unchanged.
  </behavior>
  <action>Add focused backend integration tests and wire them into the v2 MVP/hardening test script if needed.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd backend && npm run test:v2-mvp</automated>
  </verify>
  <done>Redeem/quota abuse regressions are covered before later Phase 3 UI work.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
|---|---|---|
| Brute-force code guessing | Redeem API | User/IP rate limit and generic invalid response. |
| Double redemption race | Redeem service | Preserve atomic `findOneAndUpdate` status transition. |
| Plaintext code leakage | Redeem storage | Continue hash-only storage; raw codes remain generation/export response only. |
| Scope creep into QR | Mobile/backend redeem | No QR/deep-link routes or scanner UI in this plan. |
</threat_model>

<verification>
- `cd backend && npm run typecheck`
- `cd backend && npm run test:v2-mvp`
- `cd mobile && npx tsc --noEmit`
- Manual smoke: paste code, redeem success, invalid, already-used, expired, revoked, unauthenticated, and rate-limited states.
</verification>

<success_criteria>
- Manual redeem UX is clearer and faster.
- Backend throttles abusive redeem attempts.
- Existing entitlement/quota behavior still works.
- QR/deep-link redeem remains out of scope.
</success_criteria>

<source_audit>
REQ: CODE-06, CODE-07, CODE-08, CODE-09, CODE-10, CODE-11, CODE-12.
CONTEXT: D-01, D-02, D-03, D-17, D-18, D-19.
</source_audit>

<output>
Create `.planning/phases/03-post-mvp-hardening/03-01-SUMMARY.md` when done.
</output>
