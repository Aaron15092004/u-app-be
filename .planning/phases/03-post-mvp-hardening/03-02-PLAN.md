---
phase: 03-post-mvp-hardening
plan: 02
type: execute
wave: 1
depends_on:
  - 02-04
files_modified:
  - backend/src/api/food/barcode-provider.service.ts
  - backend/src/api/food/food.controller.ts
  - backend/src/api/food/food.service.ts
  - backend/src/api/food/food.validation.ts
  - backend/src/models/FoodItem.ts
  - mobile/src/app/(food)/scan.tsx
  - mobile/src/app/(food)/result.tsx
  - mobile/src/lib/api/food.api.ts
  - mobile/src/lib/api/v2-contracts.api.ts
autonomous: true
requirements:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
  - BAR-05
  - BAR-06
  - BAR-07
requirements_addressed:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
  - BAR-05
  - BAR-06
  - BAR-07
must_haves:
  truths:
    - "D-04: Prioritize a real camera barcode scanner in the food scan flow."
    - "D-05: Barcode scan success always opens a review screen before save."
    - "D-06: Barcode lookup is local-first, then DB cache, then external fallback; cached results keep provenance and last verified metadata."
    - "D-07: Do not build a deep admin data-quality queue unless it naturally falls out of cache/audit work."
    - "Typed barcode fallback remains available even after camera scanning."
  artifacts:
    - path: "mobile/src/app/(food)/scan.tsx"
      provides: "Camera barcode scan entry point plus typed fallback"
    - path: "backend/src/api/food/barcode-provider.service.ts"
      provides: "External provider normalization and cache provenance"
---

<objective>
Complete the barcode food scan flow: real camera barcode scan, backend local/cache/external lookup, review-before-save, and provenance-preserving food logs.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-post-mvp-hardening/03-CONTEXT.md
@.planning/phases/03-post-mvp-hardening/03-RESEARCH.md
@backend/src/api/food/barcode-provider.service.ts
@backend/src/api/food/food.controller.ts
@backend/src/api/food/food.service.ts
@backend/src/api/food/food.validation.ts
@backend/src/models/FoodItem.ts
@mobile/src/app/(food)/scan.tsx
@mobile/src/app/(food)/result.tsx
@mobile/src/lib/api/food.api.ts
@mobile/src/lib/api/v2-contracts.api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement local-first barcode lookup and cache</name>
  <files>backend/src/api/food/barcode-provider.service.ts, backend/src/api/food/food.service.ts, backend/src/api/food/food.controller.ts, backend/src/api/food/food.validation.ts, backend/src/models/FoodItem.ts</files>
  <behavior>
    - Lookup preserves barcode as a digit string.
    - Backend checks local FoodItem barcode data before external provider.
    - External results are cached in FoodItem or existing compatible storage with source/provenance and `lastVerifiedAt`.
    - Provider timeout/missing/incomplete responses remain friendly and non-crashing.
  </behavior>
  <action>Refactor barcode lookup into a local/cache/external pipeline. Cache save-ready external results with provider metadata while avoiding duplicate FoodItem rows for the same barcode. Keep Open Food Facts behind the backend with a custom User-Agent.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd backend && npm run test:food</automated>
  </verify>
  <done>Barcode lookup has a useful local-first cache and stable provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Add real camera barcode scan mode</name>
  <files>mobile/src/app/(food)/scan.tsx, mobile/src/lib/api/v2-contracts.api.ts</files>
  <behavior>
    - User can open barcode mode from the existing food scan flow.
    - Camera scanner handles permission denied/unavailable states.
    - Scanner debounces repeated scan events and navigates only once per barcode.
    - Typed barcode fallback remains visible/reachable.
  </behavior>
  <action>Use the existing `expo-camera` dependency and current food scan screen patterns to add a barcode scanner mode. Restrict accepted barcode types to packaged food formats where supported; otherwise validate digits before lookup.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>User can scan packaged-food barcodes with camera infrastructure.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce review-before-save and provenance in logs</name>
  <files>mobile/src/app/(food)/result.tsx, mobile/src/lib/api/food.api.ts, backend/src/api/food/food.service.ts, backend/src/models/FoodLog.ts</files>
  <behavior>
    - Every barcode result opens a review screen before save.
    - User can edit product name, serving, calories, and macros before saving.
    - Missing/incomplete data routes to manual search or AI image scan fallback.
    - Saved food log preserves barcode source/provenance.
  </behavior>
  <action>Route barcode lookup results through the existing food result/review pattern rather than direct save. Extend save payload only as needed to store barcode provenance.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Barcode logs are user-reviewed and supportable later.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
|---|---|---|
| Bad external nutrition data | Barcode provider | Review-before-save and provenance. |
| Provider rate-limit/traffic spike | Backend provider | Cache successful lookups and degrade gracefully. |
| Duplicate scanner events | Mobile scanner | Debounce and single navigation per barcode. |
| Leading-zero loss | Contracts | Treat barcodes as strings end-to-end. |
</threat_model>

<verification>
- `cd backend && npm run typecheck`
- `cd backend && npm run test:food`
- `cd mobile && npx tsc --noEmit`
- Manual smoke: permission denied, scan barcode, typed fallback, save-ready review/save, incomplete fallback.
</verification>

<success_criteria>
- Camera barcode scan works in the food flow.
- Backend lookup is local-first/cache/external fallback.
- User always reviews data before saving.
- Food logs preserve barcode provenance.
</success_criteria>

<source_audit>
REQ: BAR-01, BAR-02, BAR-03, BAR-04, BAR-05, BAR-06, BAR-07.
CONTEXT: D-04, D-05, D-06, D-07, D-19.
</source_audit>

<output>
Create `.planning/phases/03-post-mvp-hardening/03-02-SUMMARY.md` when done.
</output>
