---
phase: 03-post-mvp-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - backend/src/api/media-assets/media-assets.controller.ts
  - backend/src/api/media-assets/media-assets.routes.ts
  - backend/src/api/media-assets/media-assets.validation.ts
  - backend/src/models/MediaAsset.ts
  - backend/src/models/Exercise.ts
  - admin/src/pages/ExercisesPage.tsx
  - admin/src/features/exercises/useExercises.ts
  - admin/src/components/ui/ImageUploadField.tsx
autonomous: true
requirements:
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04
  - MEDIA-05
  - MEDIA-06
requirements_addressed:
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04
  - MEDIA-05
  - MEDIA-06
must_haves:
  truths:
    - "D-10: Exercise media admin hardening prioritizes filename-based auto mapping."
    - "D-14: Auto-map exercise images by exact filename matching an exercise slug or id."
    - "D-15: A 100% filename match can apply directly without mandatory preview."
    - "D-16: Phase 3 needs audit logging only; undo batch and soft replacement are deferred."
  artifacts:
    - path: "backend/src/api/media-assets/media-assets.controller.ts"
      provides: "Media batch mapping and audit endpoint"
    - path: "admin/src/pages/ExercisesPage.tsx"
      provides: "Admin missing-image and upload/mapping workflow"
---

<objective>
Make exercise image operations fast enough for hundreds of exercises by adding exact filename auto-mapping, direct apply for 100% matches, and batch audit visibility.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-post-mvp-hardening/03-CONTEXT.md
@.planning/phases/03-post-mvp-hardening/03-RESEARCH.md
@backend/src/models/Exercise.ts
@backend/src/models/MediaAsset.ts
@backend/src/api/media-assets/media-assets.controller.ts
@backend/src/api/media-assets/media-assets.routes.ts
@backend/src/api/media-assets/media-assets.validation.ts
@admin/src/pages/ExercisesPage.tsx
@admin/src/features/exercises/useExercises.ts
@admin/src/components/ui/ImageUploadField.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add missing-image and filename-match backend workflow</name>
  <files>backend/src/api/media-assets/media-assets.controller.ts, backend/src/api/media-assets/media-assets.routes.ts, backend/src/api/media-assets/media-assets.validation.ts, backend/src/models/MediaAsset.ts, backend/src/models/Exercise.ts</files>
  <behavior>
    - Admin can list exercises missing images.
    - Uploaded image filenames are matched exactly to exercise slug or id after safe normalization.
    - 100% exact matches can be applied directly.
    - Exercise `imageUrl` remains populated for mobile compatibility and `imageAssetId` links to MediaAsset where possible.
  </behavior>
  <action>Add backend endpoints for missing-image queue, batch filename match preview/result, and apply exact matches. Do not implement fuzzy matching or batch undo.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
    <automated>cd backend && npm run test:admin</automated>
  </verify>
  <done>Backend can map uploaded media to exercises deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist media assignment audit metadata</name>
  <files>backend/src/models/MediaAsset.ts, backend/src/api/media-assets/media-assets.controller.ts</files>
  <behavior>
    - Audit records who uploaded/applied, file to exercise mapping, batch id, and timestamps.
    - Assignment status and errors are visible from API responses.
    - Deletion/replacement cannot orphan exercise images without an explicit safe path.
  </behavior>
  <action>Use existing `MediaAsset.metadata`, `uploadedBy`, `assignedExerciseId`, `batchId`, and status fields for audit unless a small new embedded shape is needed. Reject destructive replacement/deletion paths that would orphan active exercises.</action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <done>Batch media operations are auditable without rollback scope.</done>
</task>

<task type="auto">
  <name>Task 3: Add admin media mapping UI</name>
  <files>admin/src/pages/ExercisesPage.tsx, admin/src/features/exercises/useExercises.ts, admin/src/components/ui/ImageUploadField.tsx</files>
  <behavior>
    - Admin can filter/view exercises missing images.
    - Admin can upload/select multiple images through existing upload patterns.
    - Admin sees exact-match status, upload errors, and batch audit metadata.
    - 100% matches can be applied quickly without mandatory preview.
  </behavior>
  <action>Extend the exercises admin page with a compact media operations section. Reuse current upload/table patterns and keep direct apply restricted to exact matches.</action>
  <verify>
    <automated>cd admin && npx tsc --noEmit</automated>
  </verify>
  <done>Admin can attach images to many exercises without per-exercise manual edits.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
|---|---|---|
| Wrong image assignment | Filename matcher | Exact slug/id matching only; no fuzzy match. |
| Orphaned exercise images | Media replacement/deletion | Reject unsafe destructive paths. |
| Untraceable batch changes | Audit metadata | Record uploader/applier, file, exercise, batch, timestamp. |
</threat_model>

<verification>
- `cd backend && npm run typecheck`
- `cd backend && npm run test:admin`
- `cd admin && npx tsc --noEmit`
- Manual smoke: missing-image list, upload batch, exact match apply, unmatched file error, audit display.
</verification>

<success_criteria>
- Missing-image queue/filter exists.
- Exact filename mapping works by slug or id.
- 100% matches can apply quickly.
- Audit metadata exists; undo/soft replacement remains deferred.
</success_criteria>

<source_audit>
REQ: MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04, MEDIA-05, MEDIA-06.
CONTEXT: D-10, D-14, D-15, D-16, D-19.
</source_audit>

<output>
Create `.planning/phases/03-post-mvp-hardening/03-05-SUMMARY.md` when done.
</output>
