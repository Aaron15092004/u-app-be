---
phase: 05-home-dashboard
plan: 06
type: execute
wave: 3
depends_on:
  - 05-05
files_modified:
  - mobile/src/app/(tabs)/index.tsx
  - mobile/src/app/(home)/water.tsx
  - mobile/src/components/ui/TodaySummaryRow.tsx
  - mobile/src/components/ui/HomeSection.tsx
  - mobile/src/components/ui/MacroProgressBar.tsx
  - mobile/src/components/ui/NutritionProgressCard.tsx
  - mobile/src/components/ui/BMIWidget.tsx
  - mobile/src/components/ui/ShopBanner.tsx
  - mobile/src/components/ui/QuickActionsRow.tsx
  - mobile/src/components/ui/WaterControls.tsx
  - mobile/src/components/ui/WaterLogItem.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06

must_haves:
  truths:
    - "(tabs)/index.tsx fully rewritten — no more 'Phase 3 sắp ra mắt' placeholder; D-69 temp buttons removed"
    - "Home dashboard renders greeting with auth.user.name, 3 today-summary cards (kcal/water/workout), green Ủ Shop banner, 3 quick action buttons (Quét bữa ăn/Bắt đầu tập/Thói quen), BMI widget, nutrition progress bars"
    - "Water summary card on Home is tappable and pushes /(home)/water"
    - "Quick action buttons navigate to /(food)/scan, /(tabs)/exercises, /(tabs)/habits"
    - "Ủ Shop banner taps Linking.openURL with the URL from GET /api/config/shop-url (TanStack staleTime 1h)"
    - "(home)/water.tsx fully implemented: today count + goal (waterGoal sourced from waterQuery response — single query, no /api/home/today-summary call from water.tsx per WARNING 4 fix), +/- circular buttons, today log list with HH:MM timestamps, IDOR-safe delete via apiClient"
    - "All TanStack mutations invalidate ['home', 'summary'] after settle so dashboard reflects water changes"
    - "9 new components exist in mobile/src/components/ui/"
  artifacts:
    - path: "mobile/src/app/(tabs)/index.tsx"
      provides: "Home Dashboard screen (full rewrite)"
      min_lines: 80
    - path: "mobile/src/app/(home)/water.tsx"
      provides: "Water Log screen — reads waterGoal from waterQuery (WARNING 4 fix)"
      min_lines: 80
    - path: "mobile/src/components/ui/ShopBanner.tsx"
      provides: "Green gradient banner that opens shop URL"
      contains: "LinearGradient"
    - path: "mobile/src/components/ui/QuickActionsRow.tsx"
      provides: "3 filled green action buttons"
    - path: "mobile/src/components/ui/BMIWidget.tsx"
      provides: "BMI widget with empty state"
    - path: "mobile/src/components/ui/NutritionProgressCard.tsx"
      provides: "4 stacked macro progress bars"
  key_links:
    - from: "mobile/src/app/(tabs)/index.tsx"
      to: "getTodaySummaryApi + getShopUrlApi"
      via: "TanStack useQuery"
      pattern: "queryKey.*home.*summary"
    - from: "mobile/src/app/(home)/water.tsx"
      to: "logWaterApi + deleteWaterApi + getTodayWaterApi (waterGoal embedded — no second query needed)"
      via: "TanStack mutation + query"
      pattern: "invalidateQueries.*home.*summary"
    - from: "mobile/src/components/ui/ShopBanner.tsx"
      to: "Linking.openURL"
      via: "expo-linking"
      pattern: "Linking.openURL"
---

<validation_note>
**Mobile behavioral test deferral (per checker BLOCKER 2 fix):**

This is a mobile screen plan. Automated verification is `npx tsc --noEmit` (TypeScript strict compilation). End-to-end behavioral verification is consolidated into the blocking-human checkpoint at **Plan 07 Task 3** ("Phase 5 device verification"), which covers home dashboard render, water log +/- flow, shop banner tap, quick action navigation, and BMI widget interactions on a real device.

**Why TS-only here:** React Native Testing Library cannot exercise `expo-linear-gradient` rendering, `expo-linking` system browser launches, or the @tanstack/react-query cache invalidation chain across screens without an EAS development build. TS strict catches the primary regression risk: API contract drift against Plans 02/03 backend response shapes (ITodaySummary, ITodayWater including waterGoal).

**Styling decision (per checker WARNING 5):** CLAUDE.md specifies NativeWind v4 as the project mobile styling standard. However, existing Phase 3–4 components (`WeeklyStatCard.tsx`, `DailyChallengeCard.tsx`, `PrimaryButton.tsx`, etc.) all use `StyleSheet.create`. Phase 5 continues `StyleSheet.create` for consistency with the established component pattern. NativeWind v4 migration is deferred to a dedicated UI refactor phase. New components in this plan match the existing convention.
</validation_note>

<objective>
Wave 3 (parallel with Plan 07): Replace the placeholder (tabs)/index.tsx with the real Home Dashboard, and replace the (home)/water.tsx stub with the full Water Log screen. Build 9 new UI components driven by UI-SPEC (TodaySummaryRow, HomeSection, MacroProgressBar, NutritionProgressCard, BMIWidget, ShopBanner, QuickActionsRow, WaterControls, WaterLogItem). Wire everything to the Wave 1 backend endpoints via the Wave 2 API modules. Remove D-69 temp navigation buttons.

Purpose: Delivers HOME-01 through HOME-06 and the Water Log screen (consumer of D-73/D-74). Plan 07 runs in parallel and touches different files (profile tab and permission rationale only). Pitfall 9 mitigation: Plan 07 ships in the same wave with new logout location, so home rewrite never leaves the user without logout.

Output: 2 screen rewrites plus 9 new UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-home-dashboard/05-UI-SPEC.md
@.planning/phases/05-home-dashboard/05-RESEARCH.md
@.planning/phases/05-home-dashboard/05-PATTERNS.md
@.planning/phases/05-home-dashboard/05-CONTEXT.md
@.planning/phases/05-home-dashboard/05-05-SUMMARY.md
@mobile/src/lib/api/types.ts
@mobile/src/lib/api/home.api.ts
@mobile/src/lib/api/water.api.ts
@mobile/src/app/(tabs)/habits/index.tsx
@mobile/src/app/(tabs)/index.tsx
@mobile/src/components/ui/WeeklyStatCard.tsx
@mobile/src/components/ui/PrimaryButton.tsx
@mobile/src/components/ui/ScreenHeader.tsx
@mobile/src/constants/colors.ts
@mobile/src/providers/AuthProvider.tsx

<interfaces>
Mobile types (added in Plan 05 — POST checker fixes):
  ITodaySummary { kcalConsumed, macros: { protein, carbs, fat }, waterGlasses, waterGoal, workoutMinutes, bmi: { value, category } | null }
  IWaterLog { _id, userId, loggedAt, createdAt }
  ITodayWater { logs: IWaterLog[], count, waterGoal }   // waterGoal added per WARNING 4

API modules (Plan 05):
  getTodaySummaryApi(): Promise of ITodaySummary
  getShopUrlApi(): Promise of { url: string }
  logWaterApi(loggedAt?: string): Promise of IWaterLog
  getTodayWaterApi(): Promise of ITodayWater  // returns waterGoal embedded
  deleteWaterApi(id: string): Promise of void

AuthProvider greeting source:
  const auth = useAuth();
  auth.user?.name  // for greeting "Xin chào, {name}!"

Existing WeeklyStatCard (REUSE):
  Props: { label: string; value: string | number; unit?: string }
  Card is not wrapped in Pressable. For tappable water card, wrap WeeklyStatCard with TouchableOpacity.

PrimaryButton (do NOT use for quick actions — no icon slot):
  Props: { label, onPress, loading?, disabled?, variant? }
  Build a fresh Pressable for QuickActionsRow because PrimaryButton lacks icon support.

UI-SPEC color tokens (Plan 01 + existing):
  PRIMARY = '#4CAF50', PRIMARY_DARK = '#388E3C', BACKGROUND = '#F5F5F5', SURFACE = '#FFFFFF', TEXT = '#212121', TEXT_SECONDARY = '#757575'
  BMI_UNDERWEIGHT = '#64B5F6', BMI_NORMAL = '#4CAF50', BMI_OVERWEIGHT = '#FFA726', BMI_OBESE = '#EF5350'
  Macro carbs color = '#FFA726', Macro fat color = '#64B5F6'

expo-linear-gradient (installed in Plan 01):
  import { LinearGradient } from 'expo-linear-gradient';
  Use colors=['#4CAF50', '#388E3C'], start={x:0,y:0}, end={x:1,y:0}.

expo-linking (already installed):
  import * as Linking from 'expo-linking';
  Linking.openURL(url);

TanStack Query patterns:
  useQuery({ queryKey: ['home', 'summary'], queryFn: getTodaySummaryApi, staleTime: 30_000 })
  useQuery({ queryKey: ['config', 'shop-url'], queryFn: getShopUrlApi, staleTime: 60 * 60 * 1000 })
  useQuery({ queryKey: ['water', 'today'], queryFn: getTodayWaterApi })   // returns { logs, count, waterGoal }
  useMutation: onSettled invalidates ['water','today'] and ['home','summary']

UI-SPEC Macro goals (Screen 1.6):
  Calo goal: 2000 kcal (constant — no user kcal goal in v1)
  Protein goal: 50g, Carbs goal: 250g, Chất béo goal: 70g
  Progress fill width = Math.min(1, actual / goal) * 100

UI-SPEC Quick Actions (Screen 1.4) — filled green, white icon + label:
  Card 1: scan-outline, "Quét bữa ăn" -> router.push('/(food)/scan')
  Card 2: barbell-outline, "Bắt đầu tập" -> router.push('/(tabs)/exercises')
  Card 3: checkmark-circle-outline, "Thói quen" -> router.push('/(tabs)/habits')

BMI category Vietnamese labels (existing convention from bmi/index.tsx):
  underweight -> 'Thiếu cân', normal -> 'Bình thường', overweight -> 'Thừa cân', obese -> 'Béo phì'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build 9 Home/Water UI components</name>
  <files>mobile/src/components/ui/TodaySummaryRow.tsx, mobile/src/components/ui/HomeSection.tsx, mobile/src/components/ui/MacroProgressBar.tsx, mobile/src/components/ui/NutritionProgressCard.tsx, mobile/src/components/ui/BMIWidget.tsx, mobile/src/components/ui/ShopBanner.tsx, mobile/src/components/ui/QuickActionsRow.tsx, mobile/src/components/ui/WaterControls.tsx, mobile/src/components/ui/WaterLogItem.tsx</files>
  <read_first>
    - mobile/src/components/ui/WeeklyStatCard.tsx (full file — component pattern using StyleSheet.create)
    - mobile/src/components/ui/PrimaryButton.tsx (full file — Pressable/styles pattern using StyleSheet.create)
    - mobile/src/components/ui/DailyChallengeCard.tsx (progress bar pattern: track #E0E0E0, fill, height 8 borderRadius 4)
    - mobile/src/constants/colors.ts (all tokens)
    - .planning/phases/05-home-dashboard/05-UI-SPEC.md (Screen 1 + Screen 2 sections — exact dimensions, colors, copy)
    - .planning/phases/05-home-dashboard/05-PATTERNS.md (StyleSheet conventions)
  </read_first>
  <action>
    **Styling convention** (per validation_note above): Use `StyleSheet.create` to match existing Phase 3-4 components. NativeWind v4 migration is deferred. All 9 components follow the convention: import React, named imports from 'react-native', define interface Props, default function returning React.JSX.Element, `StyleSheet.create` at bottom of file.

    Build 9 components, each in its own file under mobile/src/components/ui/.

    1. TodaySummaryRow.tsx — Props { kcal: number; waterGlasses: number; waterGoal: number; workoutMinutes: number; onWaterPress: () => void }. Renders 3 WeeklyStatCard in a flexDirection row view with gap 8. Card 2 (water) wraps WeeklyStatCard in TouchableOpacity onPress=onWaterPress activeOpacity 0.8. Water value = `${waterGlasses} / ${waterGoal}` unit "ly" label "Nước". Kcal card unit "kcal" label "Đã ăn". Workout card unit "phút" label "Tập luyện".

    2. HomeSection.tsx — Props { title: string; children: React.ReactNode; style?: ViewStyle }. Renders a section: title 16px weight 700 TEXT marginBottom 12, then children. Card wrapper: SURFACE bg, borderRadius 12, padding 16, elevation 2 with shadowColor #000 shadowOffset {0,1} shadowOpacity 0.08 shadowRadius 4.

    3. MacroProgressBar.tsx — Props { label: string; current: number; goal: number; color: string; unit?: string }. Layout: row with label left (16px weight 400 TEXT), value plus unit right (12px weight 400 TEXT_SECONDARY) showing `${current}/${goal}${unit ?? 'g'}`. Below: progress track (height 8, borderRadius 4, backgroundColor '#E0E0E0') with inner fill (backgroundColor `color`, width style `${Math.min(1, current / goal) * 100}%`).

    4. NutritionProgressCard.tsx — Props { macros: { protein: number; carbs: number; fat: number }; kcal: number }. Renders white card (borderRadius 12 padding 16 elevation 2) with title "Dinh dưỡng hôm nay" 16px weight 700 TEXT marginBottom 12. Four MacroProgressBar stacked with gap 12: Calo (current=kcal, goal=2000, color=PRIMARY, unit='kcal'), Protein (goal=50, color=PRIMARY), Carbs (goal=250, color='#FFA726'), Chất béo (goal=70, color='#64B5F6'). If kcal===0 AND protein+carbs+fat===0, also render a centered Text "Chưa có bữa ăn nào hôm nay" 16px TEXT_SECONDARY below the bars.

    5. BMIWidget.tsx — Props { bmi: { value: number; category: string } | null; onPress: () => void }. White card, borderRadius 12 padding 16 elevation 2, wrapped in Pressable onPress=onPress (activeOpacity 0.95 via style pressed state). Header "Chỉ số BMI" 16px weight 700 TEXT marginBottom 12. If bmi: large number 28px weight 700 colored by category (underweight->BMI_UNDERWEIGHT, normal->BMI_NORMAL, overweight->BMI_OVERWEIGHT, obese->BMI_OBESE), then category Vietnamese label 16px weight 700 same color. If bmi is null: "Chưa có dữ liệu BMI" 16px TEXT_SECONDARY then "Cập nhật ngay ->" 16px weight 700 PRIMARY.

    6. ShopBanner.tsx — Props { url: string | null; isLoading: boolean }. Wraps LinearGradient from expo-linear-gradient with colors=['#4CAF50','#388E3C'] start={x:0,y:0} end={x:1,y:0}. Inside: row layout. Left: Ionicons storefront-outline size 28 color '#FFFFFF' plus text block (heading "Ủ Shop" 16px weight 700 '#FFFFFF' plus subtext "Khám phá sản phẩm sức khỏe" 12px weight 400 'rgba(255,255,255,0.85)'). Right: chevron-forward size 20 '#FFFFFF'. padding 16 borderRadius 12 marginVertical 16. Wrap banner in TouchableOpacity activeOpacity 0.85 onPress that calls Linking.openURL(url) when url is non-null. If isLoading, hide chevron-forward and show banner. If url is null AND not loading, return null (don't render broken banner).

    7. QuickActionsRow.tsx — Props { onScan: () => void; onWorkout: () => void; onHabits: () => void }. Row with 3 equal-width filled green buttons gap 8. Each button is a Pressable (NOT PrimaryButton — PrimaryButton lacks icon slot). Style: flex 1, backgroundColor PRIMARY, borderRadius 12, padding 16, alignItems center, justifyContent center. Inside: Ionicons icon size 28 color '#FFFFFF' plus Text 16px weight 700 '#FFFFFF' below with marginTop 8. Icons + labels: card1 scan-outline "Quét bữa ăn", card2 barbell-outline "Bắt đầu tập", card3 checkmark-circle-outline "Thói quen". Press feedback opacity 0.85.

    8. WaterControls.tsx — Props { count: number; onIncrement: () => void; onDecrement: () => void; isLoading: boolean }. Row layout flexDirection row alignItems center justifyContent center gap 24. Three children: minus button (Pressable 52x52 circular borderRadius 26 borderWidth 1.5 borderColor PRIMARY, Ionicons remove size 24 PRIMARY; opacity 0.4 + not pressable when count===0), count Text 28px weight 700 TEXT, plus button (52x52 circular PRIMARY background, Ionicons add size 24 '#FFFFFF'). When isLoading, replace icons with ActivityIndicator size="small" and disable both pressables.

    9. WaterLogItem.tsx — Props { index: number; loggedAt: string; onDelete?: () => void }. Row card: SURFACE bg borderRadius 8 padding 12 marginBottom 8 flexDirection row alignItems center. Left: Ionicons water-outline size 20 color '#64B5F6'. Middle: column with "Ly {index}" 16px weight 700 TEXT, then HH:MM time string 12px TEXT_SECONDARY (parse loggedAt ISO string to local time, format using getHours/getMinutes padStart(2,'0')). Right (optional): trash-outline icon Pressable if onDelete prop provided.

    All 9 files: import Ionicons from '@expo/vector-icons', color tokens from '../../constants/colors', types from '../../lib/api/types' as needed. Match StyleSheet pattern in WeeklyStatCard.tsx.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cd mobile && npx tsc --noEmit exits 0
    - All 9 component files exist under mobile/src/components/ui/
    - `grep -c "LinearGradient" mobile/src/components/ui/ShopBanner.tsx` outputs at least 2 (import + JSX)
    - `grep -c "Linking.openURL" mobile/src/components/ui/ShopBanner.tsx` outputs 1
    - `grep -c "scan-outline\|barbell-outline\|checkmark-circle-outline" mobile/src/components/ui/QuickActionsRow.tsx` ≥ 3
    - `grep -c "borderRadius: 26\|borderRadius:26" mobile/src/components/ui/WaterControls.tsx` ≥ 2 (both +/- circular buttons)
    - `grep -c "Chưa có bữa ăn nào hôm nay" mobile/src/components/ui/NutritionProgressCard.tsx` outputs 1
    - `grep -c "Chưa có dữ liệu BMI\|Cập nhật ngay" mobile/src/components/ui/BMIWidget.tsx` ≥ 1
    - Each component file is ≥ 30 lines (non-trivial implementation, not stub)
  </acceptance_criteria>
  <done>
    All 9 UI components exist, implement UI-SPEC dimensions/colors/copy exactly, import LinearGradient from expo-linear-gradient (ShopBanner) and Linking from expo-linking. TypeScript compiles. Components use StyleSheet.create per existing Phase 3-4 convention (NativeWind migration deferred per validation_note).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite (tabs)/index.tsx as Home Dashboard and (home)/water.tsx as Water Log screen — water.tsx reads waterGoal from waterQuery (WARNING 4 fix)</name>
  <files>mobile/src/app/(tabs)/index.tsx, mobile/src/app/(home)/water.tsx</files>
  <read_first>
    - mobile/src/app/(tabs)/habits/index.tsx (TanStack query + skeleton + error pattern reference)
    - mobile/src/app/(tabs)/index.tsx (current — full rewrite required; remove D-69 temp nav buttons)
    - mobile/src/app/(home)/water.tsx (current stub from Plan 05)
    - mobile/src/providers/AuthProvider.tsx (useAuth().user.name)
    - mobile/src/lib/api/home.api.ts (getTodaySummaryApi, getShopUrlApi)
    - mobile/src/lib/api/water.api.ts (logWaterApi, getTodayWaterApi, deleteWaterApi — getTodayWaterApi now returns waterGoal)
    - mobile/src/lib/api/types.ts (ITodayWater.waterGoal — added by Plan 05)
    - .planning/phases/05-home-dashboard/05-UI-SPEC.md (Screen 1 + Screen 2 — full layout specs and section order)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Pitfall 9 reminder — Plan 07 ships in same wave with logout)
    - .planning/phases/05-home-dashboard/05-02-SUMMARY.md (confirm ITodayWater includes waterGoal; do NOT make a second /api/home/today-summary query from water.tsx)
  </read_first>
  <action>
    Replace `mobile/src/app/(tabs)/index.tsx` ENTIRELY (delete D-69 placeholder + logout + temp nav buttons). New file:
    - Imports: React useState, SafeAreaView (react-native), ScrollView, View, Text, StyleSheet from 'react-native'; useRouter from 'expo-router'; useQuery from '@tanstack/react-query'; useAuth from '../../providers/AuthProvider'; getTodaySummaryApi, getShopUrlApi from '../../lib/api/home.api'; the 6 home components built in Task 1 (TodaySummaryRow, NutritionProgressCard, BMIWidget, ShopBanner, QuickActionsRow); BACKGROUND, TEXT, TEXT_SECONDARY from '../../constants/colors'.
    - Default export function HomeScreen returning React.JSX.Element.
    - Two queries at top:
      summaryQuery = useQuery with queryKey ['home','summary'], queryFn=getTodaySummaryApi, staleTime 30_000.
      shopUrlQuery = useQuery with queryKey ['config','shop-url'], queryFn=getShopUrlApi, staleTime 60*60*1000.
    - Loading state: if summaryQuery.isLoading render a SafeAreaView with simple skeleton blocks (per UI-SPEC: 4 gray #E0E0E0 placeholder Views matching card heights).
    - Error state: if summaryQuery.isError render inline error card with retry button calling summaryQuery.refetch().
    - On success: render in this exact order inside ScrollView (paddingHorizontal 16 paddingTop 16 paddingBottom 48):
      1. Greeting Row — Text "Xin chào, {auth.user?.name ?? 'bạn'}!" 28px weight 700 TEXT plus optional Vietnamese date subline "Thứ ..., ... tháng ..." 16px TEXT_SECONDARY plus Pressable bell icon (Ionicons notifications-outline 24 TEXT, accessibilityLabel "Thông báo", onPress no-op).
      2. TodaySummaryRow with kcal=summaryQuery.data.kcalConsumed, waterGlasses=summaryQuery.data.waterGlasses, waterGoal=summaryQuery.data.waterGoal, workoutMinutes=summaryQuery.data.workoutMinutes, onWaterPress=()=>router.push('/(home)/water').
      3. ShopBanner with url=shopUrlQuery.data?.url ?? null, isLoading=shopUrlQuery.isLoading.
      4. QuickActionsRow with onScan=()=>router.push('/(food)/scan'), onWorkout=()=>router.push('/(tabs)/exercises'), onHabits=()=>router.push('/(tabs)/habits').
      5. BMIWidget with bmi=summaryQuery.data.bmi (null-safe), onPress=()=>router.push('/(tabs)/bmi').
      6. NutritionProgressCard with kcal=summaryQuery.data.kcalConsumed, macros=summaryQuery.data.macros.
    - Vietnamese date format: use `const days = ['Chủ nhật','Thứ hai','Thứ ba','Thứ tư','Thứ năm','Thứ sáu','Thứ bảy']` and a simple `${days[d.getDay()]}, ${d.getDate()} tháng ${d.getMonth()+1}` (no library).
    - DO NOT add a logout button — Plan 07 owns logout in the Profile tab. The 5th Profile tab is already wired up in Plan 05.

    Replace `mobile/src/app/(home)/water.tsx` ENTIRELY. **WARNING 4 FIX — read waterGoal from waterQuery, NOT from a second /api/home/today-summary call.** New file:
    - Imports: React, SafeAreaView, ScrollView, View, Text, StyleSheet, ActivityIndicator, Alert from 'react-native'; useQuery, useMutation, useQueryClient from '@tanstack/react-query'; ScreenHeader, WaterControls, WaterLogItem from '../../components/ui/'; logWaterApi, getTodayWaterApi, deleteWaterApi from '../../lib/api/water.api'.
    - **Do NOT import getTodaySummaryApi here.** Do NOT add a second useQuery against ['home','summary'] from this screen. Plan 02 (per WARNING 4 fix) embeds `waterGoal` in the `/api/water/today` response (`ITodayWater.waterGoal`). Single query, single source of truth.
    - Default export function WaterLogScreen.
    - Hooks at top:
      ```
      const qc = useQueryClient();
      const waterQuery = useQuery({ queryKey: ['water', 'today'], queryFn: getTodayWaterApi });
      const waterGoal = waterQuery.data?.waterGoal ?? 8;   // fallback for first paint before data arrives
      const count = waterQuery.data?.count ?? 0;
      const logs = waterQuery.data?.logs ?? [];
      ```
    - Mutations:
      logMutation = useMutation with mutationFn=()=>logWaterApi(), onSettled=()=>{ qc.invalidateQueries({ queryKey: ['water','today'] }); qc.invalidateQueries({ queryKey: ['home','summary'] }); }.
      deleteMutation = useMutation with mutationFn=(id: string)=>deleteWaterApi(id), onSettled = same invalidations as logMutation.
    - For decrement: delete the most recent log via `deleteMutation.mutate(logs[0]._id)`. Guard count===0 (no-op).
    - Layout (SafeAreaView flex 1 backgroundColor BACKGROUND, ScrollView paddingHorizontal 16 paddingTop 16):
      1. ScreenHeader title="Nhật ký nước" showBack=true.
      2. Daily goal card (white borderRadius 12 padding 16 elevation 2): big "{count} / {waterGoal}" with count 28px weight 700 PRIMARY and waterGoal 20px weight 400 TEXT_SECONDARY; sublabel "ly hôm nay" 16px weight 400 TEXT_SECONDARY; progress bar (height 12 borderRadius 6 track #E0E0E0 fill PRIMARY, width=Math.min(count/waterGoal,1)*100%). If count>=waterGoal: show "Đã đạt mục tiêu! Tuyệt vời" 16px weight 700 PRIMARY in place of "ly hôm nay" subline.
      3. WaterControls component below goal card, with count={count}, onIncrement=()=>logMutation.mutate(), onDecrement=()=>{ if (logs[0]) deleteMutation.mutate(logs[0]._id); }, isLoading={logMutation.isPending || deleteMutation.isPending}.
      4. Section heading "Lịch sử hôm nay" 16px weight 700 TEXT marginTop 24 marginBottom 8.
      5. Empty state if logs.length === 0: Text "Chưa uống ly nào hôm nay. Hãy bắt đầu nhé!" 16px TEXT_SECONDARY centered marginTop 24.
      6. Otherwise: list of WaterLogItem (index reversed: oldest=1, newest at top labeled with its true sequence — use `index={count - i}` for the i-th item in the reversed-by-loggedAt-desc array; or just `index={count - logs.indexOf(log)}` to display "Ly N" decreasing from top).
    - Loading/error: skeleton on isLoading; inline error card with retry on isError (mirror habits/index.tsx pattern).

    **Styling** (per validation_note): both screens use `StyleSheet.create` to match existing Phase 3-4 screens.

    Verify after writing: NO references to D-69 in (tabs)/index.tsx remain (`grep -c 'D-69\|Phase 3 sắp ra mắt' mobile/src/app/\(tabs\)/index.tsx` should drop to 0). Verify water.tsx does NOT import getTodaySummaryApi.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd mobile && npx tsc --noEmit` exits 0
    - `grep -c "Phase 3 sắp ra mắt" mobile/src/app/\\(tabs\\)/index.tsx` outputs 0 (placeholder text removed)
    - `grep -c "D-69" mobile/src/app/\\(tabs\\)/index.tsx` outputs 0 (D-69 comment removed)
    - `grep -c "Đăng xuất\|auth.logout" mobile/src/app/\\(tabs\\)/index.tsx` outputs 0 (logout moved to Profile tab per UI-SPEC and Plan 07)
    - `grep -c "queryKey: \\['home', 'summary'\\]" mobile/src/app/\\(tabs\\)/index.tsx` ≥ 1
    - `grep -c "queryKey: \\['config', 'shop-url'\\]" mobile/src/app/\\(tabs\\)/index.tsx` ≥ 1
    - `grep -c "router.push\\('/(food)/scan'\\)\|router.push\\('/(tabs)/exercises'\\)\|router.push\\('/(tabs)/habits'\\)\|router.push\\('/(home)/water'\\)\|router.push\\('/(tabs)/bmi'\\)" mobile/src/app/\\(tabs\\)/index.tsx` ≥ 5
    - `grep -c "TodaySummaryRow\|ShopBanner\|QuickActionsRow\|BMIWidget\|NutritionProgressCard" mobile/src/app/\\(tabs\\)/index.tsx` ≥ 5
    - `grep -c "invalidateQueries.*home.*summary\|queryKey: \\['home', 'summary'\\]" mobile/src/app/\\(home\\)/water.tsx` ≥ 1 (mutation invalidates dashboard cache)
    - `grep -c "logWaterApi\|deleteWaterApi\|getTodayWaterApi" mobile/src/app/\\(home\\)/water.tsx` ≥ 3
    - `grep -c "getTodaySummaryApi" mobile/src/app/\\(home\\)/water.tsx` outputs 0 (WARNING 4 fix — water.tsx must NOT call getTodaySummaryApi for waterGoal; it reads from waterQuery.data.waterGoal)
    - `grep -c "waterQuery.data?.waterGoal\|waterQuery.data.waterGoal" mobile/src/app/\\(home\\)/water.tsx` ≥ 1 (waterGoal sourced from water query response)
    - `grep -c "Nhật ký nước" mobile/src/app/\\(home\\)/water.tsx` ≥ 1
    - `wc -l mobile/src/app/\\(tabs\\)/index.tsx` outputs ≥ 80
    - `wc -l mobile/src/app/\\(home\\)/water.tsx` outputs ≥ 80
  </acceptance_criteria>
  <done>
    Home Dashboard fully rewritten with greeting, today summary, shop banner, quick actions, BMI widget, nutrition progress; D-69 temp buttons + placeholder text removed; logout NOT on home (delegated to Plan 07 Profile tab). Water Log screen fully implemented with goal card, +/- controls, today list, IDOR-safe delete via apiClient. waterGoal sourced from waterQuery response (single source — WARNING 4 fix); water.tsx does NOT call getTodaySummaryApi. All mutations invalidate ['home','summary'] cache. TypeScript compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile UI -> backend POST /api/water | logWaterApi sends only loggedAt; userId from JWT — IDOR-safe per Plan 02 |
| mobile UI -> Linking.openURL | URL sourced from backend GET /api/config/shop-url — never accepts user input client-side either |
| TanStack Query cache | Cross-query invalidation keeps home summary fresh after water mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | DELETE /api/water/:id from water screen | mitigate | Mobile passes only the log _id; backend enforces IDOR via { _id, userId } filter (Plan 02 Test 7). |
| T-05-06-02 | Information Disclosure | Stale home dashboard after mutation | mitigate | All water mutations invalidate ['home','summary'] in onSettled so user does not see stale glass count after add/remove. |
| T-05-06-03 | Tampering | Shop URL open redirect | mitigate | Banner only opens the URL returned by backend GET /api/config/shop-url; URL never sourced from any user input, mobile or server. |
| T-05-06-04 | Spoofing | Greeting personalization | accept | auth.user.name from MMKV cache + JWT; no PII written to FCM (Plan 04 already enforced). |
| T-05-06-05 | Denial of Service | LinearGradient missing | accept | Plan 01 installed expo-linear-gradient via expo install; if module fails to load at runtime, UI-SPEC documents fallback (solid #4CAF50). |
| T-05-06-06 | Information Disclosure | waterGoal stale (WARNING 4) | mitigate | water.tsx reads waterGoal from same query that fetches logs — atomic; no race condition between waterGoal and count. Edit Profile in Plan 07 invalidates ['water','today'] alongside ['home','summary'] so a waterGoal update is immediately reflected on next water screen open. |
</threat_model>

<verification>
- cd mobile && npx tsc --noEmit green
- Manual (Plan 07 Task 3 device checkpoint covers all behavioral verification per validation_note): launch Expo dev build, log in, land on Home — see greeting + 3 cards + banner + 3 actions + BMI + nutrition.
- Tap water summary card -> water screen pushes correctly via (home) stack; waterGoal displayed comes from single /api/water/today response (verifiable via Charles/Flipper showing no /api/home/today-summary fetch from water screen)
- Tap shop banner -> system browser opens shop URL
- Tap +/- on water screen -> count updates, home dashboard reflects on return
</verification>

<success_criteria>
- (tabs)/index.tsx renders full Home Dashboard per UI-SPEC Screen 1
- (home)/water.tsx renders full Water Log per UI-SPEC Screen 2; waterGoal sourced from waterQuery response (WARNING 4 fix); no getTodaySummaryApi call from water.tsx
- 9 new UI components shipped using StyleSheet.create (NativeWind migration deferred per validation_note / WARNING 5)
- All TanStack mutations invalidate ['home','summary'] cache
- D-69 temp buttons + Phase 3 placeholder removed
- Logout NOT on home (delegated to Plan 07)
- TypeScript compiles
</success_criteria>

<output>
Create .planning/phases/05-home-dashboard/05-06-SUMMARY.md with:
- Final (tabs)/index.tsx structure (section order + components used)
- Final (home)/water.tsx structure — explicitly noting waterGoal sourcing from waterQuery response (no /api/home/today-summary call)
- Component dependency graph (which screen uses which component)
- Note for verify: cache invalidation chains
- Note: logout responsibility transferred to Plan 07 Profile tab
- Styling note: continued StyleSheet.create per existing Phase 3-4 convention (NativeWind v4 migration deferred)
</output>
</content>
</invoke>