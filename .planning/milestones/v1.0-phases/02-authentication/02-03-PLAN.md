---
phase: 02-authentication
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - mobile/src/app/(onboarding)/index.tsx
  - mobile/src/app/(onboarding)/screen-2.tsx
  - mobile/src/app/(onboarding)/screen-3.tsx
  - mobile/src/app/(onboarding)/_layout.tsx
autonomous: false
requirements: [AUTH-01]
tags: [mobile, onboarding, mmkv, expo-router, vertical-slice]

must_haves:
  truths:
    - "First-time user (onboarding_seen=false) can see Screen 1, tap 'Tiếp tục', see Screen 2, tap 'Tiếp tục', see Screen 3"
    - "On Screen 3, tapping 'Tạo tài khoản' sets onboarding_seen=true and routes to /(auth)/register"
    - "On Screen 3, tapping 'Đăng nhập' sets onboarding_seen=true and routes to /(auth)/login"
    - "The 'Bỏ qua' skip button is NOT rendered on any of the three onboarding screens (D-35)"
    - "After onboarding_seen=true, returning to the app does NOT show onboarding (verified by root layout protection in 02-04)"
    - "Onboarding screens disable back-swipe gesture (gestureEnabled=false in Stack screen options)"
  artifacts:
    - path: "mobile/src/app/(onboarding)/index.tsx"
      provides: "Screen 1: 'Chào mừng đến với Ủ' with logo + 'Tiếp tục' CTA → screen-2"
    - path: "mobile/src/app/(onboarding)/screen-2.tsx"
      provides: "Screen 2: 'Theo dõi sức khỏe hàng ngày' with feature icons + 'Tiếp tục' CTA → screen-3"
    - path: "mobile/src/app/(onboarding)/screen-3.tsx"
      provides: "Screen 3: 'Bắt đầu hành trình khỏe mạnh' with TWO CTAs (D-36 + UI-SPEC override) — 'Tạo tài khoản' → register, 'Đăng nhập' → login; both set onboarding_seen=true (D-34)"
    - path: "mobile/src/app/(onboarding)/_layout.tsx"
      provides: "Stack layout with headerShown=false, gestureEnabled=false"
  key_links:
    - from: "mobile/src/app/(onboarding)/screen-3.tsx"
      to: "mobile/src/lib/storage/mmkv.ts"
      via: "setOnboardingSeen(true) before router.replace()"
      pattern: "setOnboardingSeen\\(true\\)"
    - from: "mobile/src/app/(onboarding)/screen-3.tsx"
      to: "/(auth)/register and /(auth)/login"
      via: "router.replace()"
      pattern: "router\\.replace\\('/\\(auth\\)/(register|login)'\\)"
---

<objective>
Deliver the vertical-slice for AUTH-01: a first-time user can complete the 3-screen onboarding flow and land at either Register or Login per the Screen 3 CTAs. This is the thinnest end-to-end onboarding experience — pure mobile with no backend dependency.

Purpose: After this plan, the onboarding requirement is fully demoable. A user opens the app, sees 3 screens, taps "Tạo tài khoản", arrives at the register route (which will be a stub until 02-04 lands the screen).

Output: Three onboarding screen files matching the UI-SPEC Screens 1-3 visual contract, an updated onboarding _layout.tsx with `gestureEnabled=false`, and MMKV `onboarding_seen` flag set on Screen 3 CTA tap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-PATTERNS.md
@.planning/phases/02-authentication/02-UI-SPEC.md

@mobile/src/lib/storage/mmkv.ts
@mobile/src/components/ui/PrimaryButton.tsx
@mobile/src/components/ui/OnboardingDots.tsx
@mobile/src/constants/colors.ts
@mobile/src/app/(onboarding)/_layout.tsx
@mobile/src/app/index.tsx

<interfaces>
<!-- Routes this plan creates. The root layout (built in 02-04) will route HERE for first-time users. -->

Routes registered by Expo Router file-system:
- `/(onboarding)/` (index)        → Screen 1
- `/(onboarding)/screen-2`        → Screen 2
- `/(onboarding)/screen-3`        → Screen 3

Navigation contract:
- Screen 1 'Tiếp tục' → `router.push('/(onboarding)/screen-2')`
- Screen 2 'Tiếp tục' → `router.push('/(onboarding)/screen-3')`
- Screen 3 'Tạo tài khoản' → `setOnboardingSeen(true); router.replace('/(auth)/register')`
- Screen 3 'Đăng nhập' → `setOnboardingSeen(true); router.replace('/(auth)/login')`
</interfaces>

<ui_contracts>
Per 02-UI-SPEC.md §Screen 1, §Screen 2, §Screen 3 — copied for executor convenience:

**Screen 1 (index.tsx):**
- Full-screen SafeAreaView, bg-white, px-6.
- Vertical center: 120x120 logo (use Ionicons `leaf-outline` size 96, color #4CAF50 inside a rounded-2xl bg-primary/15 container as a fallback for actual logo asset — see Note below).
- Heading "Chào mừng đến với Ủ" (text-[28px] font-bold text-text mt-8).
- Subtitle "Ứng dụng quản lý sức khỏe toàn diện với dinh dưỡng từ thực vật" (text-base text-text-secondary mt-3 px-8 text-center).
- Bottom: OnboardingDots total=3 current=0 with mb-6.
- PrimaryButton label="Tiếp tục" variant="filled" full-width with mx-6 mb-3.
- NO "Bỏ qua" anywhere (D-35).

**Screen 2 (screen-2.tsx):**
- Full-screen SafeAreaView, bg-white, px-6.
- Three feature icon tiles in a centered flex-row gap-4, each w-16 h-16 rounded-2xl bg-primary/15. Icons (Ionicons size 28, color #4CAF50): `nutrition-outline`, `trending-up-outline`, `calendar-outline`.
- Heading "Theo dõi sức khỏe hàng ngày" (text-[28px] font-bold mt-8).
- Subtitle "Phân tích bữa ăn, theo dõi BMI, và quản lý lịch tập luyện một cách dễ dàng" (text-base text-text-secondary mt-3 px-8 text-center).
- OnboardingDots total=3 current=1.
- PrimaryButton "Tiếp tục" full-width.

**Screen 3 (screen-3.tsx):**
- Full-screen SafeAreaView, bg-white, px-6.
- Hero illustration: w-32 h-32 rounded-full bg-primary, centered, with white Ionicons `heart` size 48 overlay. Two small decorative w-8 h-8 rounded-full bg-accent badges at top-left and top-right of the circle, each containing a small white Ionicons icon (`flash` and `happy`). These are non-interactive — pure decoration. The orange accent (#FF6B35) appears HERE ONLY in the auth phase.
- Heading "Bắt đầu hành trình khỏe mạnh" (text-[28px] font-bold mt-8).
- Subtitle "Xây dựng thói quen lành mạnh và đạt được mục tiêu sức khỏe của bạn" (text-base text-text-secondary mt-3 px-8 text-center).
- OnboardingDots total=3 current=2.
- PrimaryButton variant="filled" label="Tạo tài khoản" full-width mx-6 mb-3 — on press: `setOnboardingSeen(true); router.replace('/(auth)/register')`.
- PrimaryButton variant="outlined" label="Đăng nhập" full-width mx-6 mb-6 — on press: `setOnboardingSeen(true); router.replace('/(auth)/login')`.

**Layout (_layout.tsx):**
- Stack with `screenOptions={{ headerShown: false, gestureEnabled: false }}` (UI-SPEC §Interaction "Onboarding back swipe — Disabled").

**Logo asset note:** No real logo PNG is committed yet. Use the Ionicons fallback `leaf-outline` rendering above. If `mobile/assets/logo.png` exists (check `ls mobile/assets/`), prefer `&lt;Image source={require('../../../assets/logo.png')} className="w-32 h-32" /&gt;` instead.
</ui_contracts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update onboarding _layout + create Screen 1</name>
  <files>mobile/src/app/(onboarding)/_layout.tsx, mobile/src/app/(onboarding)/index.tsx</files>
  <action>
    Modify `mobile/src/app/(onboarding)/_layout.tsx` to add `gestureEnabled: false` to the Stack's `screenOptions`: replace the current `&lt;Stack screenOptions={{ headerShown: false }} /&gt;` with `&lt;Stack screenOptions={{ headerShown: false, gestureEnabled: false }} /&gt;`. Keep the rest of the file as-is.

    Create `mobile/src/app/(onboarding)/index.tsx` (Screen 1 per AUTH-01 + D-35). Use `SafeAreaView` from `react-native`, `useRouter` from `expo-router`, `Ionicons` from `@expo/vector-icons`. Import `PrimaryButton` from `'../../components/ui/PrimaryButton'` and `OnboardingDots` from `'../../components/ui/OnboardingDots'`. Render structure (top → bottom): a `&lt;View className="flex-1 bg-white px-6"&gt;` wrapping a `&lt;SafeAreaView className="flex-1"&gt;`. First flex spacer `&lt;View className="flex-1" /&gt;`. Logo: try `ls mobile/assets/logo.png` first — if exists, render `&lt;Image source={require('../../../assets/logo.png')} className="w-32 h-32 self-center" resizeMode="contain" /&gt;`; otherwise render `&lt;View className="w-32 h-32 self-center rounded-2xl bg-primary/15 items-center justify-center"&gt;&lt;Ionicons name="leaf-outline" size={96} color="#4CAF50" /&gt;&lt;/View&gt;`. Heading Text className="text-[28px] font-bold text-text mt-8 text-center">"Chào mừng đến với Ủ". Subtitle Text className="text-base text-text-secondary mt-3 px-8 text-center">"Ứng dụng quản lý sức khỏe toàn diện với dinh dưỡng từ thực vật". Second flex spacer. `&lt;OnboardingDots total={3} current={0} /&gt;`. `&lt;PrimaryButton label="Tiếp tục" onPress={() => router.push('/(onboarding)/screen-2')} /&gt;` (no skip button per D-35). Use `useRouter()` to obtain `router`.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fs = require('fs'); const t = fs.readFileSync('src/app/(onboarding)/index.tsx', 'utf8'); if (!/Tiếp tục/.test(t)) process.exit(1); if (/Bỏ qua/.test(t)) { console.error('SKIP BUTTON STILL PRESENT'); process.exit(1); } if (!/router\.push\('\/\(onboarding\)\/screen-2'\)/.test(t)) process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>
    `_layout.tsx` Stack uses `gestureEnabled: false`. `index.tsx` renders the heading "Chào mừng đến với Ủ", contains "Tiếp tục" CTA wired to push '/(onboarding)/screen-2', does NOT contain "Bỏ qua" anywhere. tsc exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Screen 2 + Screen 3 with MMKV gating</name>
  <files>mobile/src/app/(onboarding)/screen-2.tsx, mobile/src/app/(onboarding)/screen-3.tsx</files>
  <action>
    Create `mobile/src/app/(onboarding)/screen-2.tsx`. Same imports + outer container as Screen 1. Replace the logo with a centered horizontal row of three w-16 h-16 rounded-2xl bg-primary/15 tiles (use a flex-row gap-4 self-center container), each with one Ionicons (size 28, color #4CAF50): in order `nutrition-outline`, `trending-up-outline`, `calendar-outline`. Heading "Theo dõi sức khỏe hàng ngày". Subtitle "Phân tích bữa ăn, theo dõi BMI, và quản lý lịch tập luyện một cách dễ dàng". `&lt;OnboardingDots total={3} current={1} /&gt;`. PrimaryButton "Tiếp tục" → `router.push('/(onboarding)/screen-3')`. NO "Bỏ qua" (D-35).

    Create `mobile/src/app/(onboarding)/screen-3.tsx` per UI-SPEC §Screen 3 with the D-36 override (two CTAs). Imports also include `setOnboardingSeen` from `'../../lib/storage/mmkv'`. Hero illustration: `&lt;View className="w-32 h-32 rounded-full bg-primary self-center items-center justify-center"&gt;&lt;Ionicons name="heart" size={48} color="#FFFFFF" /&gt;&lt;/View&gt;`. Decorative badges (non-interactive): wrap the hero in a relative-positioned container; add two `&lt;View className="absolute w-8 h-8 rounded-full bg-accent items-center justify-center"&gt;` with style `top: 0, left: '50%', marginLeft: -64` and `top: 0, right: '50%', marginRight: -64` (approximate offsets so they appear at top-left and top-right of the circle). Inside each badge a small Ionicons `flash` (left badge) and `happy` (right badge) size 16 color "#FFFFFF". Heading "Bắt đầu hành trình khỏe mạnh". Subtitle "Xây dựng thói quen lành mạnh và đạt được mục tiêu sức khỏe của bạn". `&lt;OnboardingDots total={3} current={2} /&gt;`. Two CTAs stacked vertically:

    ```
    &lt;PrimaryButton label="Tạo tài khoản" variant="filled" onPress={() => { setOnboardingSeen(true); router.replace('/(auth)/register'); }} /&gt;
    &lt;View className="h-3" /&gt;
    &lt;PrimaryButton label="Đăng nhập" variant="outlined" onPress={() => { setOnboardingSeen(true); router.replace('/(auth)/login'); }} /&gt;
    ```

    Both CTAs MUST set `onboarding_seen=true` BEFORE the navigation call (D-34). NO "Bỏ qua" anywhere (D-35). NO back button on Screen 3 — gesture is already disabled at layout level.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fs = require('fs'); const s2 = fs.readFileSync('src/app/(onboarding)/screen-2.tsx', 'utf8'); const s3 = fs.readFileSync('src/app/(onboarding)/screen-3.tsx', 'utf8'); for (const [name, t] of [['screen-2', s2], ['screen-3', s3]]) { if (/Bỏ qua/.test(t)) { console.error(name + ' has skip button'); process.exit(1); } } if (!/Tạo tài khoản/.test(s3) || !/Đăng nhập/.test(s3)) { console.error('Screen 3 missing two CTAs'); process.exit(1); } if (!/setOnboardingSeen\(true\)/.test(s3)) { console.error('Screen 3 not setting onboarding_seen'); process.exit(1); } if ((s3.match(/setOnboardingSeen\(true\)/g) || []).length < 2) { console.error('Screen 3 must set onboarding_seen=true in BOTH CTAs'); process.exit(1); } if (!/router\.replace\('\/\(auth\)\/register'\)/.test(s3) || !/router\.replace\('\/\(auth\)\/login'\)/.test(s3)) { console.error('Screen 3 missing register or login route'); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <done>
    screen-2.tsx renders 3 feature icons + "Tiếp tục" routing to screen-3. screen-3.tsx renders two CTAs ("Tạo tài khoản", "Đăng nhập"), both call `setOnboardingSeen(true)` before navigation, route to `/(auth)/register` and `/(auth)/login` respectively. No "Bỏ qua" anywhere. tsc exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual onboarding flow verification</name>
  <what-built>
    Three onboarding screens with MMKV gating per UI-SPEC §Screens 1-3 and D-34/D-35/D-36 override. The flow is reachable when the executor temporarily routes to `/(onboarding)` from `mobile/src/app/_layout.tsx` Slot (will be made automatic in plan 02-04).
  </what-built>
  <how-to-verify>
    1. From a clean MMKV state (uninstall + reinstall the dev build OR call `storage.clearAll()` in a temporary screen), launch the Expo dev build on iOS or Android.
    2. Manually navigate to `/(onboarding)` by editing `mobile/src/app/index.tsx` to import `Redirect` from `expo-router` and return `&lt;Redirect href="/(onboarding)/" /&gt;` for this verification only. Revert before plan 02-04 lands.
    3. Confirm Screen 1: "Chào mừng đến với Ủ" heading, three dots with first one filled green (pill), single "Tiếp tục" button, NO "Bỏ qua" link anywhere.
    4. Tap "Tiếp tục" — Screen 2 shows three feature icons (nutrition / trending-up / calendar) and dot 2 is filled.
    5. Tap "Tiếp tục" — Screen 3 shows the green heart illustration with two small orange decorative badges, dot 3 is filled, TWO buttons appear: filled-green "Tạo tài khoản" and outlined-green "Đăng nhập".
    6. Tap "Đăng nhập" — app navigates to `/(auth)/login` (will be a missing route warning until plan 02-04 — that's expected).
    7. Force-close the app and reopen — onboarding does NOT re-show (because `onboarding_seen` is now `true` in MMKV). Without a root layout protection in place yet, you may still see the temporary Redirect — that's fine. The real gate test happens in plan 02-04.
    8. Try swiping back from Screen 2 — gesture must NOT navigate back (gestureEnabled=false).
  </how-to-verify>
  <resume-signal>Type "approved" once visual + gesture + MMKV behavior matches. If anything is off, describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MMKV → device disk | onboarding_seen flag persisted unencrypted (acceptable: non-sensitive UX state) |
| Onboarding navigation → auth routes | No user input crosses here yet — pure router pushes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | onboarding_seen flag flipped via filesystem | accept | Non-sensitive UX flag; worst case user re-sees onboarding (D-37 says flag preserved across logout, not security-critical) |
| T-02-13 | Denial of Service | screen-3 onPress fails silently if MMKV write throws | mitigate | `setOnboardingSeen` is sync (MMKV is synchronous); wrap in try/catch only if needed — current implementation can let an exception propagate to render's error boundary |
| T-02-SC | Tampering | npm install of new dependencies | accept | No new npm dependencies in this plan |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0.
- The verify command in Task 2 confirms structural rules via Node-side grep substitutes.
- Manual checkpoint confirms visual contract.
- `grep -c "Bỏ qua" mobile/src/app/(onboarding)/*.tsx` returns 0 (D-35 enforcement).
</verification>

<success_criteria>
1. Three onboarding screens render per UI-SPEC §Screens 1-3.
2. No "Bỏ qua" button on any screen (D-35).
3. Screen 3 has TWO CTAs that route to register and login respectively (D-36 + UI-SPEC override).
4. Both Screen 3 CTAs set `onboarding_seen=true` BEFORE navigation (D-34).
5. Onboarding stack has `gestureEnabled: false`.
6. Human verification confirms the flow on a real device.
</success_criteria>

<output>
Create `.planning/phases/02-authentication/02-03-SUMMARY.md` when done, summarising: screen paths, CTAs and routes, and the temporary Redirect (if used) that must be reverted before 02-04.
</output>
