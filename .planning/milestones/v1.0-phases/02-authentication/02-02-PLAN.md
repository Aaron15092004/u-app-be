---
phase: 02-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/lib/storage/mmkv.ts
  - mobile/src/lib/auth/auth-store.ts
  - mobile/src/lib/api/types.ts
  - mobile/src/components/ui/AuthInput.tsx
  - mobile/src/components/ui/PrimaryButton.tsx
  - mobile/src/components/ui/OnboardingDots.tsx
  - mobile/src/components/ui/FormErrorText.tsx
  - mobile/src/components/ui/ScreenHeader.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-07]
tags: [mobile, mmkv, zustand, nativewind, components, contracts]

must_haves:
  truths:
    - "MMKV singleton instance is exported from a single module and consumed via that import everywhere"
    - "A Zustand store holds the in-memory access token + IAuthUser (D-22: access token in-memory only, never SecureStore)"
    - "Mobile API types include LoginResponse, RegisterResponse, RefreshResponse, IAuthUser shared across screens"
    - "Reusable UI primitives exist: AuthInput, PrimaryButton, OnboardingDots, FormErrorText, ScreenHeader — used by all auth screens"
  artifacts:
    - path: "mobile/src/lib/storage/mmkv.ts"
      provides: "MMKV singleton instance + typed helpers for onboarding_seen key (D-34)"
      exports: ["storage", "getOnboardingSeen", "setOnboardingSeen"]
    - path: "mobile/src/lib/auth/auth-store.ts"
      provides: "Zustand store: { user, accessToken, setUser, setAccessToken, clear } (D-22)"
      exports: ["useAuthStore", "IAuthUser"]
    - path: "mobile/src/lib/api/types.ts"
      provides: "Shared auth API request/response types"
      exports: ["LoginRequest", "LoginResponse", "RegisterRequest", "RegisterResponse", "RefreshResponse", "ForgotPasswordRequest", "ResetPasswordRequest", "GoogleSignInRequest", "AppleSignInRequest", "CompleteProfileRequest", "IAuthUser"]
    - path: "mobile/src/components/ui/AuthInput.tsx"
      provides: "Reusable input with label, icon, eye toggle, error state, unit label (per UI-SPEC §Screens 4/5/6)"
    - path: "mobile/src/components/ui/PrimaryButton.tsx"
      provides: "h-[52px] button with loading/disabled, filled or outlined variant (per UI-SPEC)"
    - path: "mobile/src/components/ui/OnboardingDots.tsx"
      provides: "3-dot pagination indicator (per UI-SPEC §Onboarding)"
    - path: "mobile/src/components/ui/FormErrorText.tsx"
      provides: "Inline red error message with FadeInDown animation (per UI-SPEC §Interaction)"
    - path: "mobile/src/components/ui/ScreenHeader.tsx"
      provides: "Back chevron + title + subtitle header (per UI-SPEC §Screen 4/5/7)"
  key_links:
    - from: "mobile/src/lib/storage/mmkv.ts"
      to: "react-native-mmkv"
      via: "new MMKV() singleton"
      pattern: "new MMKV\\("
    - from: "mobile/src/lib/auth/auth-store.ts"
      to: "zustand"
      via: "create()"
      pattern: "import.*create.*from 'zustand'"
---

<objective>
Build the shared mobile primitives every auth/onboarding screen consumes: the MMKV storage singleton (for `onboarding_seen` per D-34), the Zustand auth store (for in-memory access token + user per D-22), the API request/response type contracts, and the six reusable UI components from the UI-SPEC component inventory.

Purpose: Interface-first wave 1 work that unblocks Wave 2's onboarding (02-03) and email auth (02-04) plans to be developed in parallel. No screens are added in this plan — only primitives consumed by them.

Output: Eight new mobile source files. No backend changes. No existing files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-PATTERNS.md
@.planning/phases/02-authentication/02-UI-SPEC.md

@mobile/src/constants/colors.ts
@mobile/src/providers/AuthProvider.tsx
@mobile/src/providers/ThemeProvider.tsx
@mobile/src/app/index.tsx
@mobile/tailwind.config.js
@mobile/package.json

<interfaces>
<!-- Contracts this plan creates. Wave 2 plans (02-03, 02-04) import from these paths. -->

From mobile/src/lib/storage/mmkv.ts (NEW):
```typescript
import { MMKV } from 'react-native-mmkv';
export const storage: MMKV;             // singleton
export function getOnboardingSeen(): boolean;
export function setOnboardingSeen(value: boolean): void;
// Key constant: ONBOARDING_SEEN_KEY = 'onboarding_seen' (D-34)
```

From mobile/src/lib/auth/auth-store.ts (NEW):
```typescript
export interface IAuthUser {
  id: string;
  email: string;
  name: string;                 // empty string if not yet completed profile
  role: 'user' | 'admin';
  profileCompleted: boolean;
}

interface AuthState {
  user: IAuthUser | null;
  accessToken: string | null;   // in-memory only — never persisted (D-22)
  setUser: (user: IAuthUser | null) => void;
  setAccessToken: (token: string | null) => void;
  clear: () => void;
}
export const useAuthStore: import('zustand').UseBoundStore<import('zustand').StoreApi<AuthState>>;
```

From mobile/src/lib/api/types.ts (NEW):
```typescript
export interface IAuthUser { id: string; email: string; name: string; role: 'user' | 'admin'; profileCompleted: boolean; }

export interface LoginRequest { email: string; password: string; }
export interface LoginResponse { user: IAuthUser; accessToken: string; refreshToken: string; }
export interface RegisterRequest { email: string; password: string; }
export interface RegisterResponse { user: IAuthUser; accessToken: string; refreshToken: string; }
export interface RefreshRequest { refreshToken: string; }
export interface RefreshResponse { accessToken: string; refreshToken: string; }
export interface ForgotPasswordRequest { email: string; }
export interface ResetPasswordRequest { token: string; password: string; }
export interface GoogleSignInRequest { idToken: string; }
export interface AppleSignInRequest { identityToken: string; nonce?: string; }
export interface CompleteProfileRequest { name: string; age: number; heightCm: number; weightKg: number; goalType: 'lose' | 'maintain' | 'gain'; }
export interface CompleteProfileResponse { user: IAuthUser; }
```
</interfaces>

<ui_contracts>
<!-- Visual contracts from 02-UI-SPEC.md that the components must satisfy. -->

PrimaryButton:
- variant='filled' (default): bg-primary (#4CAF50), white text, font-bold, h-[52px], rounded-2xl, active:bg-primary-dark, opacity-50 when disabled.
- variant='outlined': white background, border border-primary, text-primary, font-bold.
- loading: shows white ActivityIndicator (size="small") in place of label, button stays disabled.

AuthInput (per UI-SPEC Screens 4/5/6):
- Wraps label (text-sm font-bold text-text) above input; input is h-[52px], bg-background, border border-gray-300, rounded-xl, px-4 (or pl-10 when leftIcon present).
- Focus state: border-2 border-primary.
- Error state: border-red-700 + FormErrorText below.
- secureTextEntry + rightIcon eye toggle: tap toggles between Ionicons `eye-outline` / `eye-off-outline`.
- unitLabel prop (e.g., 'cm', 'kg', 'tuổi') renders absolute right inside input as text-text-secondary.
- All tappable elements meet 44px minimum (auto-satisfied by 52px input height).

OnboardingDots:
- 3 dots total; active dot is a pill `w-5 h-2 rounded-full bg-primary`; inactive dots `w-2 h-2 rounded-full bg-gray-300`; `gap-2`, centered row.
- Props: `{ total: number; current: number }` (current is 0-indexed).

FormErrorText:
- Renders nothing if `message` empty; else Text with text-sm text-red-700, FadeInDown entering animation (Reanimated 100ms).

ScreenHeader:
- Back chevron (Ionicons chevron-back, size 24, text-text) shown when `showBack && router.canGoBack()`; title (text-2xl font-bold text-text, mt-8); subtitle (text-base text-text-secondary, mt-2) optional.
</ui_contracts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MMKV singleton + Zustand auth store + API type contracts</name>
  <files>mobile/src/lib/storage/mmkv.ts, mobile/src/lib/auth/auth-store.ts, mobile/src/lib/api/types.ts, mobile/src/lib/auth/auth-store.test.ts</files>
  <behavior>
    mmkv.ts:
    - Exports a single `storage` instance of `MMKV`. Subsequent imports return the same instance (no re-instantiation).
    - `setOnboardingSeen(true)` then `getOnboardingSeen()` returns `true`. Default value when unset is `false`.

    auth-store.ts:
    - `useAuthStore.getState()` initial value: `{ user: null, accessToken: null }`.
    - `useAuthStore.getState().setUser(mockUser)` updates `user`; `setAccessToken('jwt')` updates `accessToken`.
    - `useAuthStore.getState().clear()` resets both to `null`.
    - `accessToken` is never written to SecureStore or MMKV from this store (D-22) — the test asserts no call to those modules occurs during `setAccessToken`.

    types.ts: no runtime behavior — `tsc --noEmit` must compile.
  </behavior>
  <action>
    Per D-34, create `mobile/src/lib/storage/mmkv.ts`. Import `{ MMKV }` from `react-native-mmkv`. Export `export const storage = new MMKV()`. Export const `ONBOARDING_SEEN_KEY = 'onboarding_seen'`. Export `function getOnboardingSeen(): boolean { return storage.getBoolean(ONBOARDING_SEEN_KEY) ?? false; }`. Export `function setOnboardingSeen(value: boolean): void { storage.set(ONBOARDING_SEEN_KEY, value); }`.

    Per D-22, create `mobile/src/lib/auth/auth-store.ts`. Import `create` from `zustand`. Define `IAuthUser` interface as shown in `<interfaces>`. Define `AuthState` with `user`, `accessToken`, `setUser`, `setAccessToken`, `clear`. Export `useAuthStore = create&lt;AuthState&gt;((set) => ({ user: null, accessToken: null, setUser: (user) => set({ user }), setAccessToken: (accessToken) => set({ accessToken }), clear: () => set({ user: null, accessToken: null }) }))`. Do NOT use `zustand/middleware/persist` — access token must stay in-memory per D-22.

    Create `mobile/src/lib/api/types.ts` with the exact exports listed in `<interfaces>`. Re-export the `IAuthUser` interface from this file as the canonical type; `auth-store.ts` should `import type { IAuthUser } from '../api/types'` rather than declaring its own copy (single source of truth).

    Create `mobile/src/lib/auth/auth-store.test.ts` using `node:test`. Mock `react-native-mmkv` by registering a Node ESM loader override or — simpler — write the test as a pure-state Zustand test (Zustand works without React in tests; just import `useAuthStore` and call `.getState()`). For the MMKV singleton, mock by re-exporting a fake `storage` via a `__mocks__/react-native-mmkv.ts` shim used by `node --import` (acceptable fallback: skip mmkv automated tests and rely on manual smoke when running the dev build — but DO assert the import path resolves via `tsc --noEmit`).
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    Files created at the four paths; `tsc --noEmit` exits 0; `useAuthStore.getState()` initial state is `{ user: null, accessToken: null, setUser: <fn>, setAccessToken: <fn>, clear: <fn> }`; `auth-store.ts` does NOT import any persistence module (verified by `grep -v '^//' mobile/src/lib/auth/auth-store.ts | grep -c -E "persist|SecureStore|MMKV" → 0`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AuthInput + PrimaryButton + FormErrorText components</name>
  <files>mobile/src/components/ui/AuthInput.tsx, mobile/src/components/ui/PrimaryButton.tsx, mobile/src/components/ui/FormErrorText.tsx</files>
  <behavior>
    AuthInput:
    - Renders label above TextInput.
    - When `error` prop is set, input border becomes red and a FormErrorText is rendered below.
    - When `secureTextEntry` is true, an eye toggle Pressable is rendered as a rightIcon overlay that flips visibility.
    - When `unitLabel` prop is set, the unit text appears at the right inside the input.
    - `accessibilityLabel` on the eye toggle alternates "Hiện mật khẩu" / "Ẩn mật khẩu" (per UI-SPEC §Accessibility).

    PrimaryButton:
    - When `loading` is true, ActivityIndicator replaces label; `accessibilityState.busy` is true; onPress is no-op.
    - When `disabled` is true, opacity-50 + onPress no-op.
    - `variant='outlined'`: white bg, border-primary, text-primary.

    FormErrorText:
    - When `message` is falsy/empty, returns `null`.
    - When `message` is non-empty, renders Text with red color and the FadeInDown animation entering.
  </behavior>
  <action>
    Use `react-native` primitives + `expo-router`'s expected ecosystem (no extra deps). NativeWind v4 className syntax is supported per `mobile/tailwind.config.js`. Components MAY use either className (NativeWind) or StyleSheet — prefer className following UI-SPEC; ensure `mobile/src/global.css` (or equivalent) already imports nativewind — if not present, fall back to StyleSheet matching the color constants in `mobile/src/constants/colors.ts` (PRIMARY='#4CAF50' etc). Note: existing `mobile/src/app/index.tsx` uses StyleSheet — verify that NativeWind className compiles by checking `tailwind.config.js` (already present per Phase 1). If NativeWind class compilation fails at runtime, use StyleSheet equivalents matching the same visual contract.

    `AuthInput.tsx`: Props: `{ label: string; placeholder?: string; value: string; onChangeText: (text: string) => void; error?: string; leftIcon?: keyof typeof Ionicons.glyphMap; rightIcon?: keyof typeof Ionicons.glyphMap; secureTextEntry?: boolean; keyboardType?: TextInputProps['keyboardType']; unitLabel?: string; autoCapitalize?: TextInputProps['autoCapitalize']; }`. Use `useState&lt;boolean&gt;(false)` for `isFocused` and (when `secureTextEntry` initial is true) `isPasswordHidden`. Import `Ionicons` from `@expo/vector-icons`. Render structure: `&lt;View&gt;` containing `&lt;Text&gt; {label} &lt;/Text&gt;`, then `&lt;View&gt;` containing optional leftIcon (absolutely positioned), TextInput (h-[52px], with conditional `secureTextEntry={secureTextEntry &amp;&amp; isPasswordHidden}` so eye toggle works), optional eye toggle Pressable (right side) — when `secureTextEntry` prop is true, render eye toggle regardless of explicit `rightIcon`. Optional unitLabel Text positioned right. Below input: `&lt;FormErrorText message={error ?? ''} /&gt;`. Border color via conditional className: error → border-red-700; isFocused → border-2 border-primary; default → border border-gray-300. Background bg-background, rounded-xl, px-4 (or pl-10 with leftIcon).

    `PrimaryButton.tsx`: Props: `{ label: string; onPress: () => void; loading?: boolean; disabled?: boolean; variant?: 'filled' | 'outlined'; }`. Default variant: 'filled'. Render `&lt;Pressable&gt;` with `onPress` only fires when not loading && not disabled. Use `accessibilityState={{ busy: !!loading, disabled: !!disabled }}`. Conditional className: filled → bg-primary active:bg-primary-dark; outlined → bg-white border-2 border-primary. Both: h-[52px], rounded-2xl, items-center, justify-center. Label color: filled → white, outlined → text-primary. When loading, render `&lt;ActivityIndicator color="#FFFFFF" size="small" /&gt;` (or `color="#4CAF50"` for outlined) instead of label. When disabled, add `opacity-50`.

    `FormErrorText.tsx`: Props: `{ message: string }`. If `!message`, return `null`. Else import `Animated, { FadeInDown }` from `react-native-reanimated` and render `&lt;Animated.Text entering={FadeInDown.duration(100)} className="text-sm text-red-700 mt-1"&gt;{message}&lt;/Animated.Text&gt;`.

    No automated test framework set up for React Native in this repo yet. Use `tsc --noEmit` as the verification gate; a `checkpoint:human-verify` is intentionally NOT added here because the next plan (02-03) will exercise these components in real screens and the executor will visually verify there.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    Three component files exist with the prop shapes in the action; `tsc --noEmit` exits 0; `grep -n "h-\[52px\]" mobile/src/components/ui/AuthInput.tsx mobile/src/components/ui/PrimaryButton.tsx` shows the 52px touch target on each; `grep -n "FadeInDown" mobile/src/components/ui/FormErrorText.tsx` confirms reanimated import.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OnboardingDots + ScreenHeader</name>
  <files>mobile/src/components/ui/OnboardingDots.tsx, mobile/src/components/ui/ScreenHeader.tsx</files>
  <behavior>
    OnboardingDots:
    - Renders `total` dots in a horizontal row.
    - The dot at index `current` (0-indexed) is a green pill `w-5 h-2`; others are grey circles `w-2 h-2`.
    - Misuse: `current >= total` or `current < 0` clamps to nearest valid index (no crash).

    ScreenHeader:
    - When `showBack` is true AND `router.canGoBack()` returns true, renders a back chevron Pressable that calls `router.back()` on press.
    - Always renders `title` as text-2xl font-bold.
    - Renders `subtitle` text-base text-text-secondary mt-2 when provided.
  </behavior>
  <action>
    `OnboardingDots.tsx`: Import `View` from 'react-native'. Props: `{ total: number; current: number }`. Compute `const safeCurrent = Math.max(0, Math.min(current, total - 1))`. Return `&lt;View className="flex-row justify-center items-center gap-2 mb-6"&gt;{Array.from({ length: total }).map((_, i) =&gt; ( &lt;View key={i} className={i === safeCurrent ? 'w-5 h-2 rounded-full bg-primary' : 'w-2 h-2 rounded-full bg-gray-300'} /&gt; ))}&lt;/View&gt;`.

    `ScreenHeader.tsx`: Import `useRouter` from 'expo-router' and `Ionicons` from '@expo/vector-icons'. Props: `{ title: string; subtitle?: string; showBack?: boolean; onBack?: () => void; }`. Implementation: `const router = useRouter(); const canGoBack = router.canGoBack(); const showsBack = showBack &amp;&amp; canGoBack;`. Render top container with optional back chevron Pressable (Ionicons name="chevron-back" size={24} color="#212121", accessibilityLabel="Quay lại"). On press: call `onBack?.() ?? router.back()`. Title: `&lt;Text className="text-2xl font-bold text-text mt-8"&gt;{title}&lt;/Text&gt;`. Subtitle: when present, `&lt;Text className="text-base text-text-secondary mt-2"&gt;{subtitle}&lt;/Text&gt;`.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    Two component files exist; `tsc --noEmit` exits 0; `grep -n "w-5 h-2 rounded-full bg-primary" mobile/src/components/ui/OnboardingDots.tsx` matches the active pill spec; `grep -n "chevron-back" mobile/src/components/ui/ScreenHeader.tsx` matches the back icon.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand auth store → React tree | In-memory only — must never persist access token (D-22) |
| MMKV → device disk | onboarding_seen flag persisted unencrypted (acceptable: non-sensitive UX state) |
| Component props → render | Untrusted user-typed strings flow through label/value props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | accessToken persisted | mitigate | `auth-store.ts` uses bare `create()` — no `persist` middleware; grep gate verifies; D-22 |
| T-02-10 | Information Disclosure | accessToken in MMKV | mitigate | mmkv.ts only exposes `getOnboardingSeen` / `setOnboardingSeen` helpers — no generic API for tokens (defense in depth) |
| T-02-11 | Tampering | onboarding_seen flag flipped by malicious app | accept | Non-sensitive UX flag; worst case user re-sees onboarding |
| T-02-SC | Tampering | npm install of new dependencies | accept | No new npm dependencies added in this plan — all imports use Phase 1 packages (react-native, zustand, react-native-mmkv, react-native-reanimated, @expo/vector-icons) already audited |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0.
- `grep -n "from 'zustand'" mobile/src/lib/auth/auth-store.ts` matches.
- `grep -v '^//' mobile/src/lib/auth/auth-store.ts | grep -c -E "persist|SecureStore|AsyncStorage"` returns 0 (D-22 + CLAUDE.md never-AsyncStorage rule).
- All six new component files import only from packages already in `mobile/package.json`.
</verification>

<success_criteria>
1. MMKV singleton lives in one module; `onboarding_seen` (D-34) read/write helpers exported.
2. Zustand auth store holds `accessToken` in-memory only; no persistence middleware; no SecureStore/MMKV writes (D-22).
3. Shared API types exported for downstream Wave 2 plans.
4. AuthInput, PrimaryButton, OnboardingDots, FormErrorText, ScreenHeader implement the UI-SPEC contracts (52px touch targets, focus states, accessibility labels).
5. `tsc --noEmit` exits 0; no AsyncStorage usage anywhere.
</success_criteria>

<output>
Create `.planning/phases/02-authentication/02-02-SUMMARY.md` when done, summarising: MMKV singleton API, Zustand auth store API, component prop signatures, and confirmation that no persistence is attached to the access token.
</output>
