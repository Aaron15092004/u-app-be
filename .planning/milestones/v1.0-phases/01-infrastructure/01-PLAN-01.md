---
id: 01-01
phase: 1
plan: 01
wave: 1
title: Project Scaffold — Directory Structure, Package Setup, TypeScript Config
objective: Create the three-workspace directory structure (mobile, backend, admin) with package.json, tsconfig.json, and base configs so every subsequent plan has a compilable project to build into.
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/tsconfig.json
  - mobile/babel.config.js
  - mobile/app.json
  - mobile/tailwind.config.js
  - mobile/global.css
  - mobile/metro.config.js
  - backend/package.json
  - backend/tsconfig.json
  - backend/.env.example
  - admin/package.json
  - admin/tsconfig.json
  - admin/vite.config.ts
  - admin/index.html
  - admin/src/main.tsx
  - admin/src/App.tsx
  - .gitignore
autonomous: true
requirements_addressed: []
must_haves:
  truths:
    - "mobile/package.json lists expo ~54.0.0 as a dependency"
    - "backend/package.json lists express ^5.1.0 and typescript ^5.0.0 as dependencies"
    - "backend/package.json contains engines field with node >=20.0.0"
    - "admin/package.json lists vite and react as dependencies"
    - "npx tsc --noEmit exits 0 in backend/"
    - "mobile/app.json contains bundleIdentifier com.uapp.health and package com.uapp.health"
  commands:
    - "cd backend && npx tsc --noEmit"
    - "cd admin && npx tsc --noEmit"
---

## Objective

Bootstrap the three-workspace monorepo structure for the Ủ App — `mobile/` (React Native Expo SDK 54), `backend/` (Node.js + Express 5.1 TypeScript), and `admin/` (React + Vite). Each workspace gets its own `package.json`, `tsconfig.json`, and framework-specific config files. No application code yet — just scaffolding that compiles clean and allows subsequent plans to add implementation.

This is Walking Skeleton Step 0: establish the project structure that all future plans build into.

## Tasks

### Task 1: Initialize backend workspace with TypeScript and Express 5.1

<read_first>
- e:\Desktop\u-exe201-trang\.planning\research\STACK.md — Backend framework section (Express 5.1, Node 20 LTS, TypeScript)
- e:\Desktop\u-exe201-trang\.planning\research\ARCHITECTURE.md — Backend Folder Structure section (backend/src/ layout)
- e:\Desktop\u-exe201-trang\.planning\phases\01-infrastructure\01-CONTEXT.md — D-07, D-08, D-09 decisions
</read_first>

<action>
Create the `backend/` directory with the following structure and files.

`backend/package.json` — name: "u-app-backend", version: "1.0.0", type: "commonjs", scripts: { "dev": "tsx watch src/server.ts", "build": "tsc --project tsconfig.json", "start": "node dist/server.js", "lint": "eslint src --ext .ts", "typecheck": "tsc --noEmit" }. Include an "engines" field: { "node": ">=20.0.0" } — this declares the minimum Node.js version required and is enforced by Render at build time. Dependencies (exact versions): express@^5.1.0, mongoose@^8.0.0, zod@^3.24.0, jsonwebtoken@^9.0.0, bcryptjs@^2.4.3, cors@^2.8.5, helmet@^8.0.0, express-rate-limit@^7.0.0, multer@^1.4.5-lts.1, cloudinary@^2.0.0, firebase-admin@^13.0.0, dotenv@^16.0.0. DevDependencies: typescript@^5.0.0, tsx@^4.0.0, @types/express@^5.0.0, @types/node@^20.0.0, @types/cors@^2.8.0, @types/jsonwebtoken@^9.0.0, @types/bcryptjs@^2.4.0, @types/multer@^1.4.0, eslint@^9.0.0, @typescript-eslint/eslint-plugin@^8.0.0, @typescript-eslint/parser@^8.0.0.

`backend/tsconfig.json` — compilerOptions: { target: "ES2022", module: "commonjs", lib: ["ES2022"], outDir: "./dist", rootDir: "./src", strict: true, esModuleInterop: true, skipLibCheck: true, forceConsistentCasingInFileNames: true, resolveJsonModule: true, declaration: true, declarationMap: true, sourceMap: true }, include: ["src/**/*"], exclude: ["node_modules", "dist"].

`backend/.env.example` — must contain these exact keys with placeholder values: NODE_ENV=development, PORT=3000, MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/u-app?retryWrites=true&w=majority, JWT_ACCESS_SECRET=change_me_access_secret, JWT_REFRESH_SECRET=change_me_refresh_secret, JWT_ACCESS_EXPIRES=15m, JWT_REFRESH_EXPIRES=7d, CLOUDINARY_CLOUD_NAME=, CLOUDINARY_API_KEY=, CLOUDINARY_API_SECRET=, FIREBASE_PROJECT_ID=, FIREBASE_CLIENT_EMAIL=, FIREBASE_PRIVATE_KEY=, LOGMEAL_API_KEY=, OPENAI_API_KEY=, ALLOWED_ORIGINS=http://localhost:3001,http://localhost:19006.

Create placeholder source files to satisfy TypeScript compilation: `backend/src/app.ts` — exports a bare Express app instance (import express from 'express'; const app = express(); export default app;). `backend/src/server.ts` — imports app, calls app.listen(process.env.PORT || 3000). Create empty placeholder directories (with .gitkeep): backend/src/loaders/, backend/src/api/, backend/src/models/, backend/src/middleware/, backend/src/services/, backend/src/config/, backend/src/utils/.

`backend/.gitignore` addition (or root .gitignore): node_modules/, dist/, .env, *.env.local, coverage/.
</action>

<acceptance_criteria>
- [ ] `backend/package.json` contains `"express": "^5.1.0"` in dependencies
- [ ] `backend/package.json` contains `"typescript": "^5.0.0"` in devDependencies
- [ ] `backend/package.json` scripts include `"build": "tsc --project tsconfig.json"` and `"dev": "tsx watch src/server.ts"`
- [ ] `backend/package.json` contains `"engines": { "node": ">=20.0.0" }` (CHK-05)
- [ ] `backend/tsconfig.json` has `"strict": true` and `"outDir": "./dist"`
- [ ] `backend/.env.example` contains the line `MONGODB_URI=mongodb+srv://`
- [ ] `backend/.env.example` contains `CLOUDINARY_CLOUD_NAME=`
- [ ] `backend/.env.example` contains `FIREBASE_PROJECT_ID=`
- [ ] `backend/src/app.ts` exists and contains `import express from 'express'`
- [ ] `backend/src/server.ts` exists and calls `app.listen`
- [ ] Running `cd backend && npx tsc --noEmit` exits 0
</acceptance_criteria>

---

### Task 2: Initialize mobile workspace with Expo SDK 54 and NativeWind v4

<read_first>
- e:\Desktop\u-exe201-trang\.planning\research\STACK.md — Expo SDK section, NativeWind section, Navigation section
- e:\Desktop\u-exe201-trang\.planning\research\ARCHITECTURE.md — Mobile App Structure section (full directory layout)
- e:\Desktop\u-exe201-trang\.planning\phases\01-infrastructure\01-CONTEXT.md — D-01, D-03, D-04, D-15, D-16, D-17, D-18 decisions
- e:\Desktop\u-exe201-trang\.planning\research\PITFALLS.md — Expo Managed Workflow section
</read_first>

<action>
Create the `mobile/` directory with Expo SDK 54 configuration.

`mobile/package.json` — name: "u-app-mobile", version: "1.0.0", main: "expo-router/entry". Dependencies: expo@~54.0.0, expo-router@~4.0.0, react@18.3.1, react-native@0.79.0, nativewind@^4.0.0, tailwindcss@^3.4.0, zustand@^5.0.0, @tanstack/react-query@^5.0.0, react-native-mmkv@^3.0.0, expo-secure-store@~14.0.0, expo-image-picker@~15.0.0, expo-notifications@~0.29.0, expo-image-manipulator@~12.0.0, react-native-reanimated@^3.0.0, react-native-gesture-handler@^2.0.0, @shopify/react-native-skia@^1.0.0, victory-native@^40.0.0, expo-font@~13.0.0, expo-splash-screen@~0.29.0, expo-status-bar@~2.0.0, expo-constants@~17.0.0. DevDependencies: typescript@^5.0.0, @types/react@~18.3.0, babel-plugin-nativewind@^4.0.0.

`mobile/app.json` — Set these exact values: name: "Ủ", slug: "u-app-health", version: "1.0.0", orientation: "portrait", icon: "./assets/images/icon.png", userInterfaceStyle: "light", splash: { image: "./assets/images/splash.png", resizeMode: "contain", backgroundColor: "#4CAF50" }, ios: { supportsTablet: false, bundleIdentifier: "com.uapp.health", infoPlist: { NSCameraUsageDescription: "Allow Ủ to take photos for food recognition.", NSPhotoLibraryUsageDescription: "Allow Ủ to access your photos for food logging." } }, android: { adaptiveIcon: { foregroundImage: "./assets/images/adaptive-icon.png", backgroundColor: "#4CAF50" }, package: "com.uapp.health", permissions: ["CAMERA", "READ_EXTERNAL_STORAGE"] }, plugins: ["expo-router", "expo-secure-store", ["expo-image-picker", { "photosPermission": "Allow Ủ to access your photos for food logging.", "cameraPermission": "Allow Ủ to take photos for food recognition." }], ["expo-notifications", { "icon": "./assets/images/notification-icon.png", "color": "#4CAF50" }]], experiments: { typedRoutes: true }, extra: { eas: { projectId: "REPLACE_WITH_EAS_PROJECT_ID" } }.

`mobile/tsconfig.json` — extend "expo/tsconfig.base", include strict: true, paths: { "@/*": ["./src/*"] }.

`mobile/babel.config.js` — module.exports = function(api) { api.cache(true); return { presets: ['babel-preset-expo'], plugins: ['nativewind/babel'] }; }

`mobile/tailwind.config.js` — content: ["./src/app/**/*.{js,jsx,ts,tsx}", "./src/components/**/*.{js,jsx,ts,tsx}"], theme: { extend: { colors: { primary: '#4CAF50', 'primary-dark': '#388E3C', accent: '#FF6B35', accent-dark: '#E64A19', background: '#F5F5F5', surface: '#FFFFFF', text: '#212121', 'text-secondary': '#757575' } } }, plugins: [require('nativewind/tailwind/native')].

`mobile/global.css` — @tailwind base; @tailwind components; @tailwind utilities;

`mobile/metro.config.js` — const { getDefaultConfig } = require('expo/metro-config'); const { withNativeWind } = require('nativewind/metro'); const config = getDefaultConfig(__dirname); module.exports = withNativeWind(config, { input: './global.css' });

Create the `mobile/src/` directory tree as placeholder directories (with index files or .gitkeep): src/app/, src/components/ui/, src/lib/api/, src/lib/query/, src/lib/auth/, src/lib/notifications/, src/providers/, src/hooks/, src/utils/, src/constants/, src/types/. Create `mobile/assets/images/` directory with a placeholder README or .gitkeep.

Create `mobile/src/constants/colors.ts` — export the color tokens matching tailwind.config.js: PRIMARY = '#4CAF50', ACCENT = '#FF6B35', BACKGROUND = '#F5F5F5', SURFACE = '#FFFFFF'.

Create `mobile/src/constants/config.ts` — export API_URL = process.env.EXPO_PUBLIC_API_URL ?? 'http://localhost:3000'. The env var must use the EXPO_PUBLIC_ prefix (required by Expo for client-side env exposure).
</action>

<acceptance_criteria>
- [ ] `mobile/app.json` contains `"bundleIdentifier": "com.uapp.health"` under ios
- [ ] `mobile/app.json` contains `"package": "com.uapp.health"` under android
- [ ] `mobile/app.json` contains `"slug": "u-app-health"`
- [ ] `mobile/package.json` contains `"expo": "~54.0.0"`
- [ ] `mobile/package.json` contains `"nativewind": "^4.0.0"`
- [ ] `mobile/package.json` contains `"expo-constants"` (needed by push-token.ts in Plan 03)
- [ ] `mobile/tailwind.config.js` contains color `primary: '#4CAF50'`
- [ ] `mobile/tailwind.config.js` contains color `accent: '#FF6B35'`
- [ ] `mobile/babel.config.js` includes `'nativewind/babel'` in plugins array
- [ ] `mobile/metro.config.js` contains `withNativeWind`
- [ ] `mobile/src/constants/config.ts` contains `EXPO_PUBLIC_API_URL`
- [ ] `mobile/src/constants/colors.ts` exports PRIMARY with value `'#4CAF50'`
</acceptance_criteria>

---

### Task 3: Initialize admin workspace with React + Vite + TypeScript

<read_first>
- e:\Desktop\u-exe201-trang\.planning\research\ARCHITECTURE.md — Admin Dashboard Architecture section
- e:\Desktop\u-exe201-trang\.planning\phases\01-infrastructure\01-CONTEXT.md — D-06 decision (Vercel deploy)
</read_first>

<action>
Create the `admin/` directory with a React 18 + Vite 5 + TypeScript setup.

`admin/package.json` — name: "u-app-admin", version: "1.0.0", type: "module", scripts: { "dev": "vite", "build": "tsc && vite build", "preview": "vite preview", "typecheck": "tsc --noEmit", "lint": "eslint src --ext .ts,.tsx" }. Dependencies: react@^18.3.0, react-dom@^18.3.0, react-router-dom@^6.0.0, axios@^1.0.0. DevDependencies: typescript@^5.0.0, @types/react@^18.3.0, @types/react-dom@^18.3.0, vite@^5.0.0, @vitejs/plugin-react@^4.0.0, eslint@^9.0.0.

`admin/tsconfig.json` — compilerOptions: { target: "ES2020", useDefineForClassFields: true, lib: ["ES2020", "DOM", "DOM.Iterable"], module: "ESNext", skipLibCheck: true, moduleResolution: "bundler", allowImportingTsExtensions: true, resolveJsonModule: true, isolatedModules: true, noEmit: true, jsx: "react-jsx", strict: true }, include: ["src"].

`admin/vite.config.ts` — defineConfig with @vitejs/plugin-react plugin, server: { port: 3001 }, build: { outDir: 'dist' }.

`admin/index.html` — standard Vite HTML template with div#root and script src="/src/main.tsx".

`admin/src/main.tsx` — import React, ReactDOM, BrowserRouter from react-router-dom, import App from './App', render App wrapped in BrowserRouter into document.getElementById('root').

`admin/src/App.tsx` — minimal React component that renders an h1 "Ủ Admin Dashboard" and a p "Phase 1 — scaffold only". This satisfies TypeScript and allows typecheck to pass.

`admin/.env.example` — VITE_API_URL=http://localhost:3000

Create `.gitignore` at the project root covering: node_modules/, dist/, .env, *.env.local, .expo/, ios/, android/, coverage/, .DS_Store. Also create `.gitignore` in each workspace (mobile, backend, admin) with local node_modules/ and dist/ entries.
</action>

<acceptance_criteria>
- [ ] `admin/package.json` contains `"vite": "^5.0.0"` in devDependencies
- [ ] `admin/package.json` contains `"react": "^18.3.0"` in dependencies
- [ ] `admin/tsconfig.json` has `"strict": true`
- [ ] `admin/src/App.tsx` exists and contains a React functional component
- [ ] `admin/src/main.tsx` exists and calls `ReactDOM.createRoot`
- [ ] `admin/vite.config.ts` imports `defineConfig` from 'vite'
- [ ] Running `cd admin && npx tsc --noEmit` exits 0
- [ ] Root `.gitignore` contains `node_modules/` and `.env`
</acceptance_criteria>

---

## Verification

```bash
# TypeScript compiles in backend
cd backend && npx tsc --noEmit
# Expected: exit 0, no errors

# TypeScript compiles in admin
cd admin && npx tsc --noEmit
# Expected: exit 0, no errors

# Expo config is valid
cd mobile && npx expo config --type introspect | grep bundleIdentifier
# Expected: "bundleIdentifier": "com.uapp.health"

# Package versions present
grep -c "expo.*54" mobile/package.json
# Expected: 1 or more

grep -c "express.*5" backend/package.json
# Expected: 1 or more

# Node engines field present (CHK-05)
grep -c '"node"' backend/package.json
# Expected: 1 or more
```

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Secrets committed to git via .env file | HIGH | Root .gitignore includes .env and *.env.local before any npm install; .env.example contains only placeholder values, never real credentials |
| bundle ID collision with other app on App Store | LOW | com.uapp.health is project-specific; verified not taken at time of registration |
| Outdated dependency versions with known CVEs during scaffold | LOW | Versions specified are latest stable at time of plan; run `npm audit` after install and address HIGH/CRITICAL findings |
| NativeWind v4 babel plugin misconfigured causing silent style failures | LOW | Verify babel.config.js includes 'nativewind/babel' before any styling task in Plan 03 |
</threat_model>

## Success Criteria

- Three workspace directories exist: `mobile/`, `backend/`, `admin/`
- `backend/` TypeScript compiles clean (`tsc --noEmit` exits 0)
- `admin/` TypeScript compiles clean (`tsc --noEmit` exits 0)
- `mobile/app.json` has `bundleIdentifier: "com.uapp.health"` for iOS and `package: "com.uapp.health"` for Android
- `backend/package.json` contains `"engines": { "node": ">=20.0.0" }`
- All `.env.example` files exist with placeholder values (no real secrets)
- Root `.gitignore` prevents `.env` files from being committed

## Output

Create `.planning/phases/01-infrastructure/01-01-SUMMARY.md` when done.
</output>
