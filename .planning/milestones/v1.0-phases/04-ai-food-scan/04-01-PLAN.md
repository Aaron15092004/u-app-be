---
phase: 04-ai-food-scan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - mobile/package.json
  - backend/src/api/food/food.integration.test.ts
autonomous: true
requirements: [FOOD-01, FOOD-02, FOOD-05, FOOD-07, FOOD-08, FOOD-09]

must_haves:
  truths:
    - "openai package is resolvable in backend (import OpenAI from 'openai' succeeds)"
    - "expo-camera package is resolvable in mobile (import from 'expo-camera' succeeds)"
    - "food integration test file exists and test:food script runs (even before implementation)"
  artifacts:
    - path: "backend/package.json"
      provides: "openai dependency entry"
      contains: "openai"
    - path: "mobile/package.json"
      provides: "expo-camera dependency entry"
      contains: "expo-camera"
    - path: "backend/src/api/food/food.integration.test.ts"
      provides: "Integration test scaffold for all food endpoints"
      min_lines: 60
  key_links:
    - from: "backend/src/api/food/food.integration.test.ts"
      to: "backend/package.json scripts.test:food"
      via: "npm run test:food"
      pattern: "test:food"
---

<objective>
Install the two new package dependencies (openai for backend, expo-camera for mobile) and create the integration test scaffold that will gate all Phase 4 backend work. All Phase 4 implementation plans are blocked on these installs.

Purpose: Without openai installed, the backend scan endpoint cannot call GPT-4o-mini. Without expo-camera, the scan screen crashes on mount. The test scaffold ensures FOOD requirements are verified automatically as they are implemented.

Output: Updated package.json files with new dependencies, food.integration.test.ts scaffold with all test cases (initially failing/pending), and test:food script added to backend/package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ai-food-scan/04-CONTEXT.md
@.planning/phases/04-ai-food-scan/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install backend openai package and mobile expo-camera/image packages</name>
  <files>backend/package.json, mobile/package.json</files>
  <read_first>
    - backend/package.json — current dependencies list; verify openai is absent
    - mobile/package.json — current dependencies; verify expo-camera is absent, note expo-image-picker ~15.0.0 and expo-image-manipulator ~12.0.0 versions
  </read_first>
  <action>
    Run three install commands in sequence from the project root:

    1. Backend: `cd backend && npm install openai` — installs openai@6.38.0 (official OpenAI Node.js SDK). Verify "openai" appears in backend/package.json dependencies after install.

    2. Mobile expo-camera: `cd mobile && npx expo install expo-camera` — installs expo-camera compatible with the SDK 54 project. Verify "expo-camera" appears in mobile/package.json dependencies after install.

    3. Mobile image package upgrades: `cd mobile && npx expo install expo-image-picker expo-image-manipulator` — upgrades from SDK 51 versions (~15.0.0, ~12.0.0) to SDK 54 compatible versions. These are only used in Phase 4 screens; no existing code depends on them.

    Do NOT add @types/openai — the openai package ships its own TypeScript types.
    Do NOT run npm install in the mobile directory directly — always use npx expo install for Expo SDK packages (it resolves correct peer dependency versions).
  </action>
  <verify>
    <automated>cd backend && node -e "require('openai'); console.log('openai OK')"</automated>
    Check: grep -c '"openai"' backend/package.json outputs >= 1
    Check: grep -c '"expo-camera"' mobile/package.json outputs >= 1
  </verify>
  <done>
    backend/package.json has "openai" in dependencies. mobile/package.json has "expo-camera" in dependencies. expo-image-picker and expo-image-manipulator entries updated to SDK 54 compatible version tags. All packages install without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create food integration test scaffold + add test:food script</name>
  <files>
    backend/src/api/food/food.integration.test.ts,
    backend/package.json
  </files>
  <read_first>
    - backend/src/api/bmi/bmi.integration.test.ts — exact pattern: env stubs at file top, MongoMemoryServer, before/after/beforeEach hooks, supertest, signAccessToken pattern
    - backend/package.json — current scripts section; add test:food following test:bmi pattern
    - .planning/phases/04-ai-food-scan/04-RESEARCH.md — Validation Architecture section for test map and required test cases
  </read_first>
  <action>
    Create `backend/src/api/food/food.integration.test.ts` following the exact same structure as `bmi.integration.test.ts`:

    Top env stubs (before any imports):
    - process.env.JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, NODE_ENV, MONGODB_URI, CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET, FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY — same pattern as bmi test
    - Also add: `process.env.OPENAI_API_KEY = 'test-key';` — prevents undefined key error when food service is imported

    Imports: node:test (test, describe, before, after, beforeEach), node:assert/strict, supertest, mongoose, MongoMemoryServer, app from ../../app, User and FoodLog from models, signAccessToken from ../../utils/jwt.

    Test structure — create these 7 test cases as PENDING (use `test.todo()` or `test()` with `assert.fail('TODO: implement when routes exist')` so the suite runs but the cases can be easily activated):

    1. `POST /api/food/scan without auth → 401`
    2. `POST /api/food/scan with auth + mock image → 200 + NutritionResult with foods array and totals`
    3. `POST /api/food/scan when daily AI count >= 20 → 429 with Vietnamese rate limit message`
    4. `POST /api/food/logs with valid body → 201 + FoodLog document created`
    5. `GET /api/food/logs?date=YYYY-MM-DD → 200 + array of logs for that user only (cross-tenant isolation)`
    6. `DELETE /api/food/logs/:id (own log) → 200; DELETE other user's log → 404`
    7. `GET /api/food/items?q=pho → 200 + array (requires FoodItem seeded or created inline)`

    Note: For the scan test (case 2), the test MUST mock the openai module. Add a comment block explaining the mock approach: import the food service after mocking openai via `process.env.OPENAI_API_KEY = 'test-key'` and stub `analyzeImage` to return a fixed NutritionResult. The actual OpenAI mock implementation goes in the test case body — do not call real OpenAI in tests.

    MongoMemoryServer setup: before() connects, after() disconnects + stops. beforeEach() clears User and FoodLog collections, creates two users (userA, userB), signs tokens (tokenA, tokenB).

    After creating the test file, add to backend/package.json scripts:
    `"test:food": "node --env-file=.env.test --require tsx/cjs --test src/api/food/food.integration.test.ts"`

    This follows the identical pattern of test:bmi in the same file.
  </action>
  <verify>
    <automated>cd backend && npm run test:food 2>&1 | head -30</automated>
    The test runner must start without import errors. Individual test cases may fail (TODO) — the suite itself must not crash on startup.
  </verify>
  <done>
    backend/src/api/food/food.integration.test.ts exists with 7 test cases (pending/todo). backend/package.json scripts has "test:food" entry following the same pattern as "test:bmi". Running `npm run test:food` starts the test runner without module-not-found or syntax errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | Package install downloads untrusted code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | npm install openai, expo-camera | mitigate | All four packages verified in Package Legitimacy Audit (RESEARCH.md): openai [OK], expo-camera [OK], expo-image-picker [OK], expo-image-manipulator [OK]. All are official packages from canonical organizations. |
| T-04-02 | Information Disclosure | OPENAI_API_KEY in test files | mitigate | test stub uses literal 'test-key' string — never the real key. Real key only in backend/.env (gitignored). |
</threat_model>

<verification>
1. `cd backend && node -e "require('openai'); console.log('ok')"` exits 0
2. `grep -c '"expo-camera"' mobile/package.json` returns >= 1
3. `cd backend && npm run test:food 2>&1` starts without "Cannot find module" errors
4. `grep -c '"test:food"' backend/package.json` returns >= 1
</verification>

<success_criteria>
- openai resolvable in backend node_modules
- expo-camera in mobile/package.json
- expo-image-picker and expo-image-manipulator upgraded to SDK 54 compatible versions
- food.integration.test.ts exists with 7 test case skeletons
- npm run test:food script runs without crashing
</success_criteria>

<output>
Create `.planning/phases/04-ai-food-scan/04-01-SUMMARY.md` when done
</output>
