---
phase: 04-ai-food-scan
plan: 06
type: execute
wave: 5
depends_on: [04-03, 04-05]
files_modified:
  - mobile/src/app/(food)/scan.tsx
  - mobile/src/app/(food)/result.tsx
  - mobile/src/components/ui/ScanFrame.tsx
  - mobile/src/components/ui/CameraControls.tsx
  - mobile/src/components/ui/NutritionSummaryCard.tsx
  - mobile/src/components/ui/NutritionDetailRow.tsx
  - mobile/src/components/ui/FoodTagPill.tsx
autonomous: true
requirements: [FOOD-01, FOOD-02, FOOD-03, FOOD-04, FOOD-05, FOOD-06]

must_haves:
  truths:
    - "Camera scan screen opens in dark theme (backgroundColor #000000) with CameraView live preview, scan frame overlay, and three bottom buttons"
    - "Tapping capture compresses the image (max 800×800, JPEG 0.7) via expo-image-manipulator then calls scanFoodApi"
    - "Gallery button opens expo-image-picker launchImageLibraryAsync then follows same compress → scan flow"
    - "Flash button toggles Ionicons flash-outline / flash icon"
    - "Camera permission request shows permission card with 'Cấp quyền Camera' PrimaryButton"
    - "Rate limit 429 response shows Alert with Vietnamese message"
    - "Loading state shows ActivityIndicator white on capture button, all buttons disabled"
    - "AI result screen shows food name, tags as FoodTagPill components, green kcal card, 3-col macro row, 4 micro-nutrient rows"
    - "'Xác nhận & Lưu' calls saveFoodLogApi, shows success toast 'Đã lưu bữa ăn!', navigates to /food/diary"
    - "'Chụp lại' calls router.back() and clearScan() without saving"
  artifacts:
    - path: "mobile/src/app/(food)/scan.tsx"
      provides: "Full camera scan screen implementation"
      contains: "CameraView"
    - path: "mobile/src/app/(food)/result.tsx"
      provides: "Full AI result screen implementation"
      contains: "NutritionSummaryCard"
    - path: "mobile/src/components/ui/ScanFrame.tsx"
      provides: "Green corner bracket overlay component"
      contains: "ScanFrame"
    - path: "mobile/src/components/ui/CameraControls.tsx"
      provides: "Gallery + Capture + Flash button row"
      contains: "CameraControls"
    - path: "mobile/src/components/ui/NutritionSummaryCard.tsx"
      provides: "Green kcal card + 3-column macro row"
      contains: "NutritionSummaryCard"
    - path: "mobile/src/components/ui/NutritionDetailRow.tsx"
      provides: "Single micro-nutrient row with color coding"
      contains: "NutritionDetailRow"
    - path: "mobile/src/components/ui/FoodTagPill.tsx"
      provides: "Green tag pill badge"
      contains: "FoodTagPill"
  key_links:
    - from: "mobile/src/app/(food)/scan.tsx"
      to: "mobile/src/lib/api/food.api.ts"
      via: "scanFoodApi(compressedUri)"
      pattern: "scanFoodApi"
    - from: "mobile/src/app/(food)/scan.tsx"
      to: "mobile/src/stores/foodScanStore.ts"
      via: "useFoodScanStore().setScanResult()"
      pattern: "setScanResult"
    - from: "mobile/src/app/(food)/result.tsx"
      to: "mobile/src/lib/api/food.api.ts"
      via: "saveFoodLogApi(body)"
      pattern: "saveFoodLogApi"
    - from: "mobile/src/app/(food)/result.tsx"
      to: "mobile/src/stores/foodScanStore.ts"
      via: "useFoodScanStore().scanResult"
      pattern: "scanResult"
---

<objective>
Implement the camera scan screen (FOOD-01/02/03) and AI result screen (FOOD-04/05/06) with full UI as specified in 04-UI-SPEC.md, plus the 5 UI components they require (ScanFrame, CameraControls, NutritionSummaryCard, NutritionDetailRow, FoodTagPill).

Purpose: This plan delivers the core AI food scan user journey — the primary distinguishing feature of Phase 4. Users can photograph a meal and see its nutrition data.

Output: scan.tsx (full CameraView implementation), result.tsx (full nutrition display), and 5 new UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-food-scan/04-CONTEXT.md
@.planning/phases/04-ai-food-scan/04-RESEARCH.md
@.planning/phases/04-ai-food-scan/04-UI-SPEC.md
@mobile/src/stores/foodScanStore.ts
@mobile/src/lib/api/food.api.ts
@mobile/src/components/ui/PrimaryButton.tsx
</context>

<interfaces>
<!-- expo-camera API — SDK 54 pattern from RESEARCH.md Pattern 4 -->
import { CameraView, useCameraPermissions } from 'expo-camera';
import type { CameraType } from 'expo-camera';
const [permission, requestPermission] = useCameraPermissions();
const cameraRef = useRef<CameraView>(null);
const photo = await cameraRef.current.takePictureAsync({ quality: 1 });  // photo.uri
// permission check: permission.status === 'granted' (not just permission.granted — see Pitfall 6)

<!-- expo-image-manipulator — SDK 54 v12 pattern from RESEARCH.md Pattern 2 -->
import * as ImageManipulator from 'expo-image-manipulator';
const result = await ImageManipulator.manipulateAsync(
  uri,
  [{ resize: { width: 800 } }],
  { compress: 0.7, format: ImageManipulator.SaveFormat.JPEG }
);
// result.uri is the compressed URI

<!-- expo-image-picker — launchImageLibraryAsync -->
import * as ImagePicker from 'expo-image-picker';
const result = await ImagePicker.launchImageLibraryAsync({ mediaTypes: ImagePicker.MediaTypeOptions.Images, quality: 1 });
if (!result.canceled && result.assets[0]) { const uri = result.assets[0].uri; }

<!-- foodScanStore — from mobile/src/stores/foodScanStore.ts (Plan 05) -->
const { setScanResult, setIsScanning, setPendingImageUri, clearScan } = useFoodScanStore();
const { scanResult } = useFoodScanStore();

<!-- food API — from mobile/src/lib/api/food.api.ts (Plan 05) -->
await scanFoodApi(compressedUri);   // returns NutritionResult
await saveFoodLogApi(body);         // saves to /api/food/logs

<!-- Expo Router navigation -->
import { router } from 'expo-router';
router.push('/(food)/result');
router.back();
router.push('/(food)/diary');

<!-- Color values from UI-SPEC.md (use these exact hex values) -->
PRIMARY = '#4CAF50'
BACKGROUND = '#F5F5F5'
TEXT = '#212121'
TEXT_SECONDARY = '#757575'
DESTRUCTIVE = '#EF5350'
FIBER_COLOR = '#4CAF50'
SUGAR_COLOR = '#FFA726'
SODIUM_COLOR = '#EF5350'
VITAMIN_COLOR = '#64B5F6'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ScanFrame, CameraControls, FoodTagPill UI components</name>
  <files>
    mobile/src/components/ui/ScanFrame.tsx,
    mobile/src/components/ui/CameraControls.tsx,
    mobile/src/components/ui/FoodTagPill.tsx
  </files>
  <read_first>
    - .planning/phases/04-ai-food-scan/04-UI-SPEC.md — Screen 1 (Camera Scan) layout: ScanFrame dimensions (260×260, green #4CAF50 corner brackets, arm 40px, stroke 4px, borderRadius 8px, hint text), CameraControls spec (gallery 52px circle rgba(255,255,255,0.15), capture 72px circle #4CAF50, flash 52px circle toggle)
    - .planning/phases/04-ai-food-scan/04-UI-SPEC.md — Component Inventory table for precise component responsibilities
    - mobile/src/components/ui/PrimaryButton.tsx — existing component pattern for StyleSheet + props interface approach
  </read_first>
  <action>
    FILE 1: Create mobile/src/components/ui/ScanFrame.tsx

    Pure presentational component. No state, no callbacks.

    Props: none.

    Renders: A 260×260 centered container View. Inside it: 4 absolute-positioned corner bracket Views, each drawn as two overlapping thin rectangles forming an L-shape using the green color #4CAF50. arm length 40px, stroke 4px, borderRadius 8px at outer corners. In the center: Ionicons 'camera-outline' at 40px white, and Text "Căn chỉnh bữa ăn vào khung" (14px/400/white/center). Implementation: use 4 absolute corner Views each composing 2 thin colored bars for the L-bracket shape. Position them using { position: 'absolute', top/bottom: 0, left/right: 0 }.

    FILE 2: Create mobile/src/components/ui/CameraControls.tsx

    Props interface:
    - onGallery: () => void
    - onCapture: () => void
    - onFlash: () => void
    - flashEnabled: boolean
    - isLoading: boolean

    Renders: A horizontal row (flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 40) with three pressable elements:

    Gallery button: 52px × 52px circle, backgroundColor: 'rgba(255,255,255,0.15)', borderRadius: 26. Contains Ionicons 'image-outline' 24px white. Disabled when isLoading (opacity 0.4). accessibilityLabel="Chọn từ thư viện".

    Capture button: 72px × 72px circle, backgroundColor: '#4CAF50', borderRadius: 36. 3px white border with 4px gap (outer ring: 80px circle, borderWidth: 3, borderColor: white, position absolute centered). When isLoading: shows ActivityIndicator white size='large' instead of the icon. accessibilityLabel="Chụp ảnh".

    Flash button: 52px × 52px circle, backgroundColor: flashEnabled ? 'rgba(255,255,255,0.25)' : 'rgba(255,255,255,0.15)', borderRadius: 26. Contains Ionicons 'flash' (flashEnabled) or 'flash-outline' (disabled) 24px white. accessibilityLabel="Bật/tắt đèn flash".

    All buttons: pointerEvents: isLoading ? 'none' : 'auto' (box).

    FILE 3: Create mobile/src/components/ui/FoodTagPill.tsx

    Props: { label: string }

    Renders: A View with style: { backgroundColor: '#E8F5E9', paddingHorizontal: 8, paddingVertical: 4, borderRadius: 12, marginRight: 6 }. Contains Text with style: { color: '#4CAF50', fontSize: 14, fontWeight: '400' }.

    Export default FoodTagPill.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "ScanFrame|CameraControls|FoodTagPill" | head -10</automated>
    Zero TypeScript errors for these 3 component files.
  </verify>
  <done>
    ScanFrame.tsx: pure presentational, renders 260×260 frame with green (#4CAF50) corner brackets (40px arm, 4px stroke) + camera-outline icon + hint text. CameraControls.tsx: accepts onGallery/onCapture/onFlash/flashEnabled/isLoading props, renders 3 button row with specified sizes. FoodTagPill.tsx: renders green pill badge with label text. All three pass TypeScript strict mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create NutritionSummaryCard, NutritionDetailRow; implement scan.tsx and result.tsx</name>
  <files>
    mobile/src/components/ui/NutritionSummaryCard.tsx,
    mobile/src/components/ui/NutritionDetailRow.tsx,
    mobile/src/app/(food)/scan.tsx,
    mobile/src/app/(food)/result.tsx
  </files>
  <read_first>
    - .planning/phases/04-ai-food-scan/04-UI-SPEC.md — Screen 2 (AI Result) layout in detail: sections 1-6, CTA buttons, macro icons (Ionicons fitness-outline/leaf-outline/water-outline), micro-nutrient row colors (fiber #4CAF50, sugar #FFA726, sodium #EF5350, vitaminC #64B5F6)
    - .planning/phases/04-ai-food-scan/04-UI-SPEC.md — Screen 1 (Camera Scan) layout: dark theme, permission card, loading state
    - .planning/phases/04-ai-food-scan/04-UI-SPEC.md — Copywriting Contract table for exact Vietnamese strings
    - .planning/phases/04-ai-food-scan/04-RESEARCH.md — Pattern 4 (CameraView), Pattern 2 (compression), Common Pitfall 6 (iOS permission)
    - mobile/src/stores/foodScanStore.ts — useFoodScanStore hook API
    - mobile/src/lib/api/food.api.ts — scanFoodApi, saveFoodLogApi signatures
    - mobile/src/components/ui/ScanFrame.tsx — just created; verify it compiles
    - mobile/src/components/ui/CameraControls.tsx — just created; verify props interface
  </read_first>
  <action>
    FILE 1: Create mobile/src/components/ui/NutritionSummaryCard.tsx

    Props: { calories: number; protein: number; carbs: number; fat: number; }

    Renders:
    - Green card: backgroundColor: '#4CAF50', borderRadius: 16, padding: 16, marginHorizontal: 16, marginBottom: 16
    - "Tổng calo" label: fontSize 14, color white, fontWeight '400'
    - calories value: fontSize 36, fontWeight '700', color white
    - Below the card: horizontal 3-column View (flexDirection: 'row', justifyContent: 'space-around')
    - Each column: centered, label + value. Protein: Ionicons 'fitness-outline' #4CAF50 + value (fontSize 24, fontWeight '700', #212121) + "Protein" label (14px/400/#757575). Carbs: Ionicons 'leaf-outline' #FFA726 + value + "Carbs". Fat: Ionicons 'water-outline' #64B5F6 + value + "Chất béo".
    - Numeric values rounded to 1 decimal: `${protein.toFixed(1)}g`

    FILE 2: Create mobile/src/components/ui/NutritionDetailRow.tsx

    Props: { label: string; value: number; unit: string; color: string; }

    Renders: A View (flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingVertical: 12, paddingHorizontal: 16). Left: Text label (fontSize 16, fontWeight '400', color '#212121'). Left also shows a 4px × 16px colored bar (backgroundColor: color) to the left of the label. Right: Text "{value}{unit}" (fontSize 16, fontWeight '400', color: '#757575').

    FILE 3: Implement mobile/src/app/(food)/scan.tsx (full implementation — replaces stub)

    Imports: React, useState, useRef, useEffect from react. View, Text, StyleSheet, Alert, Pressable, ActivityIndicator from react-native. CameraView, useCameraPermissions from expo-camera. ImageManipulator from expo-image-manipulator. ImagePicker from expo-image-picker. router from expo-router. ScanFrame, CameraControls from components/ui. useFoodScanStore, scanFoodApi from stores/lib.

    State: flash: 'off' | 'on', const [permission, requestPermission] = useCameraPermissions().

    cameraRef = useRef<CameraView>(null).

    Zustand: const { setIsScanning, isScanning, setScanResult, setPendingImageUri } = useFoodScanStore().

    compressImage(uri: string): Promise<string> — use ImageManipulator.manipulateAsync(uri, [{ resize: { width: 800 } }], { compress: 0.7, format: ImageManipulator.SaveFormat.JPEG }) returning result.uri.

    handleCapture():
    - if isScanning return
    - setIsScanning(true)
    - photo = await cameraRef.current.takePictureAsync({ quality: 1 })
    - compressed = await compressImage(photo.uri)
    - setPendingImageUri(compressed)
    - result = await scanFoodApi(compressed)
    - setScanResult(result) — sets isScanning: false internally
    - router.push('/(food)/result')
    - On error: setIsScanning(false). If error.response?.status === 429: Alert.alert('', 'Bạn đã quét 20 lần hôm nay. Vui lòng thử lại vào ngày mai.'). Otherwise: Alert.alert('Lỗi', 'Đã xảy ra lỗi. Vui lòng thử lại.')

    handleGallery():
    - result = await ImagePicker.launchImageLibraryAsync({ mediaTypes: ImagePicker.MediaTypeOptions.Images, quality: 1 })
    - if result.canceled return
    - Same compress → scan flow as handleCapture (using result.assets[0].uri)

    Permission check: if (!permission) return null. If permission.status !== 'granted': render permission card (centered dark View: "Ứng dụng cần quyền truy cập camera để quét bữa ăn", PrimaryButton "Cấp quyền Camera" onPress={requestPermission}, Text link "Chọn từ thư viện" onPress={handleGallery}).

    Main render: SafeAreaView (flex: 1, backgroundColor: '#000'). Inside: CameraView (flex: 1, facing: 'back', flash: flash, ref: cameraRef). Inside CameraView overlay: ScreenHeader-like header (back button, title "Quét bữa ăn", subtitle, white text). Centered: ScanFrame component. Below: hint text "Nhấn nút chụp hoặc chọn từ thư viện" (14px/white/center). Bottom fixed area: CameraControls component (onGallery/onCapture/onFlash/flashEnabled/isLoading). When isScanning: Text "Đang phân tích..." below ScanFrame.

    FILE 4: Implement mobile/src/app/(food)/result.tsx (full implementation — replaces stub)

    Imports: React, useState from react. View, Text, ScrollView, StyleSheet, Alert from react-native. router from expo-router. useFoodScanStore, NutritionResult from stores. saveFoodLogApi from lib/api. PrimaryButton, ScreenHeader from components/ui. NutritionSummaryCard, NutritionDetailRow, FoodTagPill from components/ui.

    Zustand: const { scanResult, clearScan } = useFoodScanStore().

    If !scanResult: router.back() in useEffect and return null (guard against blank screen if store was cleared).

    const firstFood = scanResult.foods[0] (use first food item for tags display, or aggregate all tags).

    isSaving state: boolean.

    handleSave():
    - setIsSaving(true)
    - await saveFoodLogApi({ foods: scanResult.foods.map(f => ({ name: f.name, weightG: f.weightG, calories: f.calories, protein: f.protein, carbs: f.carbs, fat: f.fat, fiber: f.fiber, sugar: f.sugar, sodium: f.sodium, vitaminC: f.vitaminC })), totals: scanResult.totals, aiProvider: scanResult.aiProvider, imageUrl: null })
    - clearScan()
    - Alert.alert('', 'Đã lưu bữa ăn!') — brief success feedback
    - router.push('/(food)/diary')
    - On error: setIsSaving(false), Alert.alert('Lỗi', 'Đã xảy ra lỗi. Vui lòng thử lại.')

    handleRetry():
    - clearScan()
    - router.back()

    Render: ScrollView (backgroundColor: '#FFFFFF', contentContainerStyle paddingBottom: 120). ScreenHeader title="Kết quả phân tích" subtitle="Thông tin dinh dưỡng bữa ăn" showBack=true. Food name (24px/700/#212121, paddingHorizontal: 16, marginTop: 16). Tags row: horizontal ScrollView of FoodTagPill for each tag in firstFood.tags. Section heading "Tổng quan dinh dưỡng". NutritionSummaryCard with totals values. Section heading "Thông tin chi tiết". Four NutritionDetailRow: fiber (label="Chất xơ", unit="g", color="#4CAF50"), sugar (label="Đường", unit="g", color="#FFA726"), sodium (label="Natri", unit="mg", color="#EF5350"), vitaminC (label="Vitamin C", unit="mg", color="#64B5F6") — values from firstFood. Fixed bottom: PrimaryButton variant="filled" label="Xác nhận & Lưu" onPress={handleSave} loading={isSaving}. PrimaryButton variant="outlined" label="Chụp lại" onPress={handleRetry} disabled={isSaving}.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "scan\.tsx|result\.tsx|NutritionSummary|NutritionDetail" | head -20</automated>
    Zero TypeScript errors. Also verify scan.tsx contains 'CameraView' and result.tsx contains 'NutritionSummaryCard'.
  </verify>
  <done>
    NutritionSummaryCard.tsx: green kcal card + 3-col macro row with Ionicons. NutritionDetailRow.tsx: label + colored bar + value/unit. scan.tsx: CameraView with useCameraPermissions, ScanFrame overlay, CameraControls, compress flow, scanFoodApi call, rate limit 429 alert, loading state. result.tsx: reads scanResult from store, displays all nutrition fields via NutritionSummaryCard/NutritionDetailRow/FoodTagPill, "Xác nhận & Lưu" saves and navigates to diary, "Chụp lại" clears and goes back. All files pass TypeScript strict mode.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera hardware → scan.tsx | Raw image before compression |
| scan.tsx → backend POST /api/food/scan | Compressed image upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | Raw camera photo → OpenAI without compression | mitigate | CLAUDE.md rule: compressImage() step is mandatory in handleCapture and handleGallery before scanFoodApi; resize max 800×800, JPEG 0.7 |
| T-04-06-02 | Denial of Service | Multiple rapid taps on capture button | mitigate | isScanning state guard at top of handleCapture — returns early if already scanning |
| T-04-06-03 | Information Disclosure | pendingImageUri in Zustand store | accept | URI is a temp file path on device; store is in-memory; clearScan() called on success/retry; no PII in food images |
| T-04-06-04 | Elevation of Privilege | result.tsx — client provides aiProvider value | accept | aiProvider comes from backend scan response (not user input); passed through as-is from scanResult store |
</threat_model>

<verification>
1. `cd mobile && npx tsc --noEmit 2>&1 | grep -E "scan\.|result\."` returns no errors
2. scan.tsx contains `CameraView` and `manipulateAsync` and `scanFoodApi`
3. result.tsx contains `NutritionSummaryCard` and `saveFoodLogApi` and `clearScan`
4. ScanFrame.tsx renders with no props (pure presentational)
5. CameraControls.tsx accepts onGallery, onCapture, onFlash, flashEnabled, isLoading props
</verification>

<success_criteria>
- scan.tsx: CameraView live preview, dark theme, ScanFrame overlay, CameraControls, permission card, compress flow, scanFoodApi (30s), 429 alert, isScanning guard, navigates to result on success
- result.tsx: reads foodScanStore.scanResult, displays food name + tags + NutritionSummaryCard + 4 NutritionDetailRows, "Xác nhận & Lưu" saves + navigates to diary, "Chụp lại" clears + goes back
- 5 UI components created: ScanFrame, CameraControls, NutritionSummaryCard, NutritionDetailRow, FoodTagPill
- `npx tsc --noEmit` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/04-ai-food-scan/04-06-SUMMARY.md` when done
</output>
