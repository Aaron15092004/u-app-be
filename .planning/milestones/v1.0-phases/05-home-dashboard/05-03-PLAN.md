---
phase: 05-home-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - backend/src/api/users/users.validation.ts
  - backend/src/api/users/users.service.ts
  - backend/src/api/users/users.controller.ts
  - backend/src/api/users/users.routes.ts
  - backend/src/api/users/users.integration.test.ts
autonomous: true
requirements:
  - PRO-02
  - PRO-03
  - PRO-05

must_haves:
  truths:
    - "GET /api/users/profile/stats authenticated; returns { streakDays, totalWorkouts, totalKcalBurned, notifications } scoped to req.user.id"
    - "PATCH /api/users/profile authenticated; updates User.name + User.profile.heightCm/weightKg/goalType/waterGoal via Zod whitelist only (no mass assignment)"
    - "PATCH /api/users/notifications authenticated; updates User.notifications.waterReminder/workoutReminder/waterReminderTime/workoutReminderTime via Zod whitelist; HH:MM format validated"
    - "users router mounted at /api/users in app.ts AFTER Plan 02 completes (serial mount edit; not parallel with Plan 02)"
    - "Integration tests prove: mass-assignment rejected (e.g., role/email cannot be changed via PATCH profile), IDOR cross-user scoping, HH:MM regex enforcement on time fields"
  artifacts:
    - path: "backend/src/api/users/users.routes.ts"
      provides: "Three user endpoints"
      exports: ["default Router"]
    - path: "backend/src/api/users/users.service.ts"
      provides: "Profile + notifications + stats business logic; getProfileStats also returns current notifications for form init"
      exports: ["getProfileStats", "updateUserProfile", "updateUserNotifications"]
    - path: "backend/src/api/users/users.validation.ts"
      provides: "Zod whitelist schemas for PATCH endpoints"
      exports: ["updateProfileSchema", "updateNotificationsSchema"]
  key_links:
    - from: "backend/src/api/users/users.service.ts"
      to: "habits.service.getStreak"
      via: "delegation import"
      pattern: "getStreak"
    - from: "backend/src/api/users/users.validation.ts"
      to: ".strict() Zod schemas"
      via: "mass-assignment defence"
      pattern: "\\.strict\\(\\)"
    - from: "backend/src/app.ts"
      to: "users router"
      via: "app.use"
      pattern: "app.use\\('/api/users'"
---

<validation_note>
**Mobile/Backend test coverage:** This is a backend plan — all behavioral verification is automated integration tests via `npm run test:users`. No mobile concerns apply.

**Serialization with Plan 02:** Per checker fix (BLOCKER 1), this plan now depends on Plan 02 and runs in Wave 2, not Wave 1. The `backend/src/app.ts` mount happens here in this plan (after Plan 02's home/water/config routers are already mounted), so there is zero concurrent edit to `app.ts`.
</validation_note>

<objective>
Wave 2 (serialized after Plan 02): Implement the three /api/users endpoints — Profile stats aggregation (PRO-02), PATCH profile with Zod whitelist (PRO-03), and PATCH notifications with HH:MM validation (PRO-05). Delegate streak computation to existing `getStreak` in `habits.service.ts`. Aggregate workout totals via `WorkoutLog.aggregate`. Mount the router at `/api/users` in app.ts (after Plan 02's mounts are already in place). Extend the Wave 0 users integration test scaffold with real assertions covering all three endpoints plus mass-assignment defence.

Purpose: This plan delivers the data the mobile Profile tab needs (stats row, edit profile screen, notification settings screen). Originally a Wave 1 plan, moved to Wave 2 to serialize the `backend/src/app.ts` edit with Plan 02 — both plans were mounting routers in the same file, which created a merge conflict with no atomic coordination. Now Plan 02 ships first, then this plan adds the users mount after.

Also: `getProfileStats` returns the current `notifications` block in its response so the mobile notifications screen (Plan 07) can initialise its form state from the user's saved settings rather than hardcoded defaults (fix for checker WARNING 3 — see Plan 07 Task 2).

Output: 4 new backend files, 1 app.ts modification (additive mount of /api/users), 1 test file extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-home-dashboard/05-CONTEXT.md
@.planning/phases/05-home-dashboard/05-RESEARCH.md
@.planning/phases/05-home-dashboard/05-PATTERNS.md
@.planning/phases/05-home-dashboard/05-VALIDATION.md
@.planning/phases/05-home-dashboard/05-01-SUMMARY.md
@.planning/phases/05-home-dashboard/05-02-SUMMARY.md
@backend/src/models/User.ts
@backend/src/models/WorkoutLog.ts
@backend/src/api/habits/habits.service.ts
@backend/src/api/bmi/bmi.service.ts
@backend/src/api/bmi/bmi.controller.ts
@backend/src/api/bmi/bmi.routes.ts
@backend/src/api/food/food.controller.ts
@backend/src/api/food/food.validation.ts
@backend/src/middleware/auth.middleware.ts
@backend/src/utils/response.ts
@backend/src/app.ts

<interfaces>
<!-- Existing getStreak delegation (habits.service.ts lines 78-121) -->
From backend/src/api/habits/habits.service.ts:
  export async function getStreak(userId: string): Promise<{ streakDays: number }>
  // Computes consecutive UTC+7 days with >=3 distinct habits (D-49/50)

<!-- User schema after Plan 01 migration -->
From backend/src/models/User.ts (post-migration):
  profile: {
    dateOfBirth?: Date;
    gender?: 'male' | 'female' | 'other';
    heightCm?: number;
    weightKg?: number;
    goalType?: 'lose' | 'maintain' | 'gain';
    waterGoal?: number;        // NEW (D-74)
  };
  notifications: {
    waterReminder: boolean;
    workoutReminder: boolean;
    waterReminderTime: string;  // NEW (D-79) default '08:00'
    workoutReminderTime: string;// NEW (D-79) default '07:00'
  };

<!-- WorkoutLog shape for stats aggregation -->
From backend/src/models/WorkoutLog.ts:
  durationMinutes: number;
  caloriesBurned: number;
  date: Date;
  // userId-indexed; query with `userId: ObjectId, date: { $gte: ... }` if filtering, but for all-time stats just `{ userId }`

<!-- Existing User.findByIdAndUpdate dot-notation pattern (bmi.service.ts lines 59-65) -->
  await User.findByIdAndUpdate(
    userObjId,
    { 'profile.heightCm': heightCm, 'profile.weightKg': weightKg },
    { new: true, runValidators: true }
  ).select('profile').lean();

<!-- Zod .strict() pattern from food.validation.ts -->
  z.object({...}).strict()  // rejects unknown keys

<!-- bmi.routes.ts mount pattern -->
  router.patch('/', authenticate, bmiController.saveBMI);
  router.get('/history', authenticate, bmiController.getHistory);

<!-- bmi.controller.ts handler structure (lines 1-38) for analog -->
  - safeParse body, error 400 on failure
  - extract userId from AuthRequest
  - call service, success(res, result)
  - catch typed error, return e.statusCode ?? 500

<!-- app.ts state AFTER Plan 02 ships (per 05-02-SUMMARY.md) -->
  app.use('/api/food', foodRouter);
  app.use('/api/water', waterRouter);     // mounted by Plan 02
  app.use('/api/home', homeRouter);       // mounted by Plan 02
  app.use('/api/config', configRouter);   // mounted by Plan 02
  // ADD HERE (Plan 03): app.use('/api/users', usersRouter);
  app.use(errorMiddleware);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Users validation + service + controller + routes with Zod whitelist and getStreak delegation</name>
  <files>backend/src/api/users/users.validation.ts, backend/src/api/users/users.service.ts, backend/src/api/users/users.controller.ts, backend/src/api/users/users.routes.ts, backend/src/app.ts, backend/src/api/users/users.integration.test.ts</files>
  <read_first>
    - backend/src/api/habits/habits.service.ts (getStreak full implementation lines 78-121)
    - backend/src/api/bmi/bmi.service.ts (User.findByIdAndUpdate dot-notation pattern lines 59-65)
    - backend/src/api/bmi/bmi.controller.ts (full file — controller analog)
    - backend/src/api/bmi/bmi.routes.ts (route registration analog)
    - backend/src/api/food/food.validation.ts (zod .strict() patterns + Vietnamese error messages)
    - backend/src/models/User.ts (post-migration schema — verify Plan 01 applied)
    - backend/src/app.ts (state AFTER Plan 02 — water/home/config routers already mounted)
    - backend/src/api/users/users.integration.test.ts (Wave 0 scaffold)
    - .planning/phases/05-home-dashboard/05-02-SUMMARY.md (confirm Plan 02 app.ts mount positions)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Pattern 9 + Pattern 10)
  </read_first>
  <behavior>
    - Test 1: GET /api/users/profile/stats without auth → 401
    - Test 2: GET /api/users/profile/stats with userA token (no HabitLog/WorkoutLog) → 200 + { streakDays: 0, totalWorkouts: 0, totalKcalBurned: 0, notifications: { waterReminder: true, workoutReminder: true, waterReminderTime: '08:00', workoutReminderTime: '07:00' } } (defaults from D-79)
    - Test 3: Seed userA: 3 HabitLogs today (water, vegetables, exercise; >=3 distinct → qualifies streak), 2 WorkoutLogs (durationMinutes=20+30, caloriesBurned=150+200). GET stats → { streakDays: 1, totalWorkouts: 2, totalKcalBurned: 350, notifications: <defaults> }
    - Test 4 (IDOR): userB token after userA's seed → { streakDays: 0, totalWorkouts: 0, totalKcalBurned: 0, notifications: <defaults> }
    - Test 5: PATCH /api/users/profile without auth → 401
    - Test 6: PATCH /api/users/profile { name: 'Aaron', heightCm: 175, weightKg: 70, goalType: 'maintain', waterGoal: 10 } → 200 + updated user; verify via User.findById that profile.heightCm=175, profile.waterGoal=10, name='Aaron'
    - Test 7 (mass-assignment defence): PATCH /api/users/profile { name: 'X', role: 'admin', email: 'evil@x.com', passwordHash: 'pwned' } → 400 from Zod .strict() OR 200 but role/email/passwordHash unchanged in DB. Verify User.role still 'user' and email unchanged.
    - Test 8 (HH:MM regex): PATCH /api/users/notifications { waterReminderTime: '25:99' } → 400 with Vietnamese error message
    - Test 9: PATCH /api/users/notifications { waterReminder: false, workoutReminder: true, waterReminderTime: '09:30', workoutReminderTime: '06:00' } → 200; verify User.notifications updated to those exact values
    - Test 10 (partial update): PATCH /api/users/notifications { waterReminderTime: '10:15' } → 200; only waterReminderTime updated, other fields unchanged
    - Test 11 (waterGoal range): PATCH /api/users/profile { waterGoal: 100 } → 400 (must be 1..20 to match UI-SPEC range 4-16, allow 1-20 server-side)
    - Test 12 (notifications round-trip): PATCH /api/users/notifications { waterReminderTime: '11:11' } then GET /api/users/profile/stats → response.notifications.waterReminderTime === '11:11' (proves form-init use case for mobile Plan 07)
  </behavior>
  <action>
    Create `backend/src/api/users/users.validation.ts`:
    - `updateProfileSchema = z.object({ name: z.string().min(1, 'Tên không được để trống').max(80).optional(), heightCm: z.number().min(80, 'Chiều cao không hợp lệ').max(250).optional(), weightKg: z.number().min(20, 'Cân nặng không hợp lệ').max(300).optional(), goalType: z.enum(['lose', 'maintain', 'gain']).optional(), waterGoal: z.number().int('Số ly phải là số nguyên').min(1, 'Số ly tối thiểu là 1').max(20, 'Số ly tối đa là 20').optional() }).strict()`. Add header comment "Strict whitelist — rejects role, email, passwordHash, etc. (mass assignment defence)."
    - `updateNotificationsSchema = z.object({ waterReminder: z.boolean().optional(), workoutReminder: z.boolean().optional(), waterReminderTime: z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/, 'Giờ không hợp lệ (HH:MM)').optional(), workoutReminderTime: z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/, 'Giờ không hợp lệ (HH:MM)').optional() }).strict()`. Both schemas use `.strict()`.

    Create `backend/src/api/users/users.service.ts`:
    - Imports: `mongoose`, `User`, `WorkoutLog`, `{ getStreak }` from '../habits/habits.service'. Add `makeError` helper.
    - `getProfileStats(userId: string): Promise<{ streakDays: number; totalWorkouts: number; totalKcalBurned: number; notifications: { waterReminder: boolean; workoutReminder: boolean; waterReminderTime: string; workoutReminderTime: string } }>`:
      - `const userObjId = new mongoose.Types.ObjectId(userId);`
      - Run in parallel via Promise.all: streak via `getStreak(userId)`, count via `WorkoutLog.countDocuments({ userId: userObjId })`, kcal via `WorkoutLog.aggregate([{ $match: { userId: userObjId } }, { $group: { _id: null, total: { $sum: '$caloriesBurned' } } }])`, AND user notifications block via `User.findById(userObjId).select('notifications').lean()`.
      - Return `{ streakDays: streakResult.streakDays, totalWorkouts: count, totalKcalBurned: kcalAgg[0]?.total ?? 0, notifications: { waterReminder: user?.notifications?.waterReminder ?? true, workoutReminder: user?.notifications?.workoutReminder ?? true, waterReminderTime: user?.notifications?.waterReminderTime ?? '08:00', workoutReminderTime: user?.notifications?.workoutReminderTime ?? '07:00' } }`. (Defaults match D-79.) This embedded notifications block is what mobile Plan 07 reads to initialise its notification settings form — see WARNING 3 in the revision context.
    - `updateUserProfile(userId, body: z.infer<typeof updateProfileSchema>)`:
      - Build `update: Record<string, unknown> = {}`. For each provided field, set the correct path. `name` → `update.name = body.name`. heightCm/weightKg/goalType/waterGoal → use dot-notation `update['profile.heightCm'] = body.heightCm` etc.
      - `const user = await User.findByIdAndUpdate(userObjId, update, { new: true, runValidators: true }).select('name email profile').lean(); if (!user) throw makeError('Không tìm thấy người dùng', 404); return user;`
    - `updateUserNotifications(userId, body)`:
      - Build update with `notifications.waterReminder`, `notifications.workoutReminder`, `notifications.waterReminderTime`, `notifications.workoutReminderTime` dot-notation.
      - findByIdAndUpdate, select('notifications').lean(), return user. 404 if not found.

    Create `backend/src/api/users/users.controller.ts`:
    - Three handlers `getProfileStats`, `updateProfile`, `updateNotifications`. Pattern: extract userId from AuthRequest; for PATCH handlers safeParse with the right schema (Vietnamese first-error message); call service; success(res, result); typed try/catch identical to bmi.controller.ts.

    Create `backend/src/api/users/users.routes.ts`:
    - `router.get('/profile/stats', authenticate, usersController.getProfileStats);`
    - `router.patch('/profile', authenticate, usersController.updateProfile);`
    - `router.patch('/notifications', authenticate, usersController.updateNotifications);`
    - Note: the path order matters — `/profile/stats` MUST be registered before any `/profile/:id` pattern (none here, but defensive). `default export router`.

    Modify `backend/src/app.ts` (SERIAL — after Plan 02 completes):
    - Plan 02 has already mounted /api/food, /api/water, /api/home, /api/config in this file (verify by reading current app.ts and 05-02-SUMMARY.md). This plan ADDS one line, no conflict with Plan 02.
    - Add import: `import usersRouter from './api/users/users.routes';` (place near the other api router imports — alphabetical-ish; the import order is non-functional).
    - Add mount: `app.use('/api/users', usersRouter);` AFTER `app.use('/api/config', configRouter);` (which Plan 02 added) and BEFORE `app.use(errorMiddleware);`. This is an additive single-line edit — Plan 02 is already done.
    - If for any reason Plan 02's mounts are NOT present when this plan runs (executor MUST have read 05-02-SUMMARY.md first), STOP and surface the dependency violation to the user. Do NOT attempt to add Plan 02's mounts here.

    Extend `backend/src/api/users/users.integration.test.ts`:
    - Remove placeholder; add the 12 tests in `<behavior>`.
    - For Test 7 mass-assignment: `.strict()` schema returns 400 on unknown keys — the assertion is `res.status === 400` AND a subsequent `User.findById(userIdA)` shows role still 'user' and email unchanged. Alternative pass: if executor opts for `.passthrough()` and field whitelist in service, the test must verify role/email unchanged after a 200 response (either path is acceptable as long as DB state proves no mass assignment).
    - For Test 12 round-trip: PATCH then GET; assert getResponse.data.notifications.waterReminderTime === '11:11'.
    - Use `signAccessToken({ sub: userIdA, role: 'user' })` from the scaffold's existing imports.
    - Import additional models needed (HabitLog, WorkoutLog) and deleteMany in beforeEach.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run test:users &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm run test:users` exits 0 with all 12 tests passing
    - `cd backend && npx tsc --noEmit` exits 0
    - `grep -c '\.strict()' backend/src/api/users/users.validation.ts` outputs 2 (both PATCH schemas strict)
    - `grep -c 'role\|email\|passwordHash' backend/src/api/users/users.validation.ts` outputs 0 (sensitive fields NOT in whitelist)
    - `grep -c 'HH:MM\|([01]\\\\d|2[0-3]' backend/src/api/users/users.validation.ts` ≥ 1 (HH:MM regex present)
    - `grep -c "import { getStreak }" backend/src/api/users/users.service.ts` outputs 1 (delegation, not reimplementation)
    - `grep -c "(req as AuthRequest).user.id" backend/src/api/users/users.controller.ts` outputs 3 (all 3 handlers)
    - `grep -c "authenticate" backend/src/api/users/users.routes.ts` outputs 3
    - `grep -c "app.use('/api/users'" backend/src/app.ts` outputs 1
    - `grep -c "notifications:" backend/src/api/users/users.service.ts` ≥ 1 (getProfileStats returns notifications block — for mobile form init per Plan 07 fix)
    - app.ts integrity: Plan 02's mounts still present — `grep -c "app.use('/api/water'\\|app.use('/api/home'\\|app.use('/api/config'" backend/src/app.ts` outputs 3 (no regression of Plan 02 edits)
  </acceptance_criteria>
  <done>
    Three /api/users endpoints work with Zod-whitelisted PATCH bodies and authenticated reads. getProfileStats reuses existing getStreak (no streak logic reimplemented) AND returns the user's current notifications block so mobile Plan 07 can initialise its form. /api/users mounted in app.ts as an additive single-line edit after Plan 02's mounts. All 12 integration tests pass, TypeScript compiles. Mass-assignment defence proven by Test 7. Plan 02's app.ts mounts intact (no regression).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend PATCH endpoints | User-supplied body crosses trust boundary; must be whitelisted before reaching User schema |
| JWT → DB query | userId must always come from token, never body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | PATCH /api/users/profile mass assignment | mitigate | Zod schema `.strict()` rejects unknown keys; role/email/passwordHash not in whitelist. Asserted by Test 7. |
| T-05-03-02 | Tampering | PATCH /api/users/notifications HH:MM injection | mitigate | Regex `^([01]\d|2[0-3]):[0-5]\d$` rejects malformed time strings. Asserted by Test 8. |
| T-05-03-03 | Information Disclosure / IDOR | GET /api/users/profile/stats cross-user | mitigate | All queries filter by `userId: ObjectId(req.user.id)`. Asserted by Test 4. |
| T-05-03-04 | Spoofing | Unauthenticated profile access | mitigate | All 3 routes registered with `authenticate` middleware. Asserted by Tests 1, 5. |
| T-05-03-05 | Elevation of Privilege | role/admin escalation via PATCH | mitigate | role field absent from updateProfileSchema; even on .passthrough() fallback the service builds update object explicitly from whitelist keys. |
| T-05-03-06 | Tampering | Concurrent app.ts edits with Plan 02 | mitigate | Plan reordered to Wave 2 with explicit `depends_on: 05-02` (per checker BLOCKER 1). app.ts is edited serially: Plan 02 first, then this plan adds one additional `app.use('/api/users', ...)` line. No parallel write. |
</threat_model>

<verification>
- `cd backend && npm run test:users && npx tsc --noEmit` green
- 12 integration tests pass; mass-assignment Test 7 verifies role/email/passwordHash NEVER mutated
- HH:MM regex Test 8 returns 400 with Vietnamese error
- Cross-user IDOR Test 4 returns scoped data only
- Test 12 verifies notifications round-trip (form-init data path for mobile Plan 07)
- app.ts contains both Plan 02's and this plan's mounts (5 new mounts total across both plans, no regression)
</verification>

<success_criteria>
- GET /api/users/profile/stats returns { streakDays, totalWorkouts, totalKcalBurned, notifications } via delegation to getStreak + WorkoutLog aggregates + User.findById on notifications
- PATCH /api/users/profile accepts only whitelisted fields (name, heightCm, weightKg, goalType, waterGoal) via Zod .strict()
- PATCH /api/users/notifications accepts only whitelisted fields with HH:MM regex on time fields via Zod .strict()
- All routes mounted under /api/users with authenticate middleware
- All 12 integration tests pass
- Mass-assignment Test 7 proves role/email/passwordHash cannot be modified via these endpoints
- Notifications block in getProfileStats response enables mobile Plan 07 form initialisation from saved server state
</success_criteria>

<output>
Create `.planning/phases/05-home-dashboard/05-03-SUMMARY.md` with:
- 3 endpoint signatures
- Updated IProfileStats response shape (now includes notifications block)
- Zod schema snippets (updateProfileSchema + updateNotificationsSchema)
- Mass-assignment defence approach (.strict() rejection vs whitelist build)
- Tests added (12 total)
- Wave reassignment note: was Wave 1, moved to Wave 2 to serialize app.ts edit with Plan 02 (BLOCKER 1)
- Note: mobile Plan 07 must update IProfileStats type in mobile/src/lib/api/types.ts to include the notifications block, and call getProfileStatsApi to seed notifications.tsx form state (WARNING 3 fix)
</output>
</content>
</invoke>