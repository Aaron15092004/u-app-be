---
phase: 05-home-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/models/User.ts
  - backend/src/models/WaterLog.ts
  - backend/src/api/home/home.integration.test.ts
  - backend/src/api/water/water.integration.test.ts
  - backend/src/api/users/users.integration.test.ts
  - mobile/package.json
  - mobile/src/constants/colors.ts
  - mobile/tailwind.config.js
autonomous: true
requirements:
  - HOME-02
  - HOME-05
  - HOME-06
  - PRO-02
  - PRO-03
  - PRO-05
  - NOTIF-02
  - NOTIF-03

must_haves:
  truths:
    - "node-cron 4.2.1 + @types/node-cron 3.0.11 installed in backend/package.json"
    - "expo-linear-gradient and @react-native-community/datetimepicker installed via npx expo install in mobile/package.json"
    - "User.notifications schema replaces reminderTime with waterReminderTime (default '08:00') and workoutReminderTime (default '07:00')"
    - "User.profile schema includes waterGoal (Number, default 8)"
    - "WaterLog model exists with compound index { userId: 1, loggedAt: -1 } (non-unique)"
    - "Three integration test scaffolds exist and run (with placeholder assertions) so Wave 1 tasks can extend them"
    - "Three npm scripts added: test:home, test:water, test:users"
    - "Three new color tokens (BADGE_UNLOCKED_BG, BADGE_LOCKED, BADGE_LOCKED_BG) exported from mobile/src/constants/colors.ts and tailwind.config.js"
  artifacts:
    - path: "backend/src/models/User.ts"
      provides: "Migrated User schema (D-74, D-79)"
      contains: "waterGoal"
    - path: "backend/src/models/WaterLog.ts"
      provides: "WaterLog mongoose model (D-73)"
      contains: "WaterLogSchema.index"
    - path: "backend/src/api/home/home.integration.test.ts"
      provides: "Wave 0 test scaffold (HOME-02, HOME-05, HOME-06)"
    - path: "backend/src/api/water/water.integration.test.ts"
      provides: "Wave 0 test scaffold (D-73 WaterLog CRUD)"
    - path: "backend/src/api/users/users.integration.test.ts"
      provides: "Wave 0 test scaffold (PRO-02, PRO-03, PRO-05)"
  key_links:
    - from: "backend/package.json"
      to: "node_modules/node-cron"
      via: "dependency entry"
      pattern: "node-cron"
    - from: "mobile/package.json"
      to: "node_modules/expo-linear-gradient"
      via: "dependency entry"
      pattern: "expo-linear-gradient"
    - from: "backend/src/models/User.ts"
      to: "Mongoose Schema"
      via: "schema definition"
      pattern: "waterReminderTime"
---

<objective>
Wave 0 foundations for Phase 5: install required packages (backend node-cron, mobile linear-gradient + datetimepicker), migrate User schema per D-74 + D-79, create the WaterLog model per D-73, scaffold three integration test files so Wave 1 tasks can extend them, and add new color tokens for achievement badges per D-78 UI-SPEC.

Purpose: Every Wave 1 backend plan touches User.notifications.waterReminderTime / workoutReminderTime, WaterLog, or runs an integration test. None of those can begin until the schemas, the test files, and node-cron exist. This plan is the dependency unblock for the entire phase.

Output: Updated package.json (backend+mobile), migrated User schema, new WaterLog model, three test scaffolds with npm scripts, three new color tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-home-dashboard/05-CONTEXT.md
@.planning/phases/05-home-dashboard/05-RESEARCH.md
@.planning/phases/05-home-dashboard/05-PATTERNS.md
@.planning/phases/05-home-dashboard/05-VALIDATION.md
@.planning/phases/05-home-dashboard/05-UI-SPEC.md
@backend/src/models/User.ts
@backend/src/models/HabitLog.ts
@backend/src/models/WorkoutLog.ts
@backend/src/api/food/food.integration.test.ts
@backend/package.json
@mobile/package.json
@mobile/src/constants/colors.ts
@mobile/tailwind.config.js

<interfaces>
<!-- Existing User schema in backend/src/models/User.ts that MUST change per D-74 + D-79 -->

Current IUser.profile (lines 10-16):
  profile: {
    dateOfBirth?: Date;
    gender?: 'male' | 'female' | 'other';
    heightCm?: number;
    weightKg?: number;
    goalType?: 'lose' | 'maintain' | 'gain';
  };
  // ADD: waterGoal?: number (default 8)

Current IUser.notifications (lines 17-21):
  notifications: {
    waterReminder: boolean;
    workoutReminder: boolean;
    reminderTime: string;          // REMOVE
  };
  // ADD: waterReminderTime: string (default '08:00')
  // ADD: workoutReminderTime: string (default '07:00')

Existing schema definition pattern at lines 39-50 — modify in place.

<!-- HabitLog model pattern for WaterLog reference -->
From backend/src/models/HabitLog.ts: schema fields { userId, habitId, date, checkedAt } + compound index { userId: 1, date: -1, habitId: 1 } unique. WaterLog drops habitId and date, uses loggedAt, index NOT unique.

<!-- WorkoutLog non-unique compound index pattern (closer analog) -->
From backend/src/models/WorkoutLog.ts line 28: `WorkoutLogSchema.index({ userId: 1, date: -1 });` (non-unique). WaterLog mirrors this with `loggedAt` instead of `date`.

<!-- Existing integration test pattern -->
From backend/src/api/food/food.integration.test.ts lines 1-76: process.env stubs at top before imports, MongoMemoryServer setup in before/after hooks, beforeEach creates two users (userA, userB) for IDOR tests, signAccessToken for JWT.

<!-- npm script pattern -->
From backend/package.json line 16-21: existing test:* scripts use this exact form — `node --env-file=.env.test --require tsx/cjs --test src/api/<feature>/<feature>.integration.test.ts`. Add three more matching this template.

<!-- Tailwind color tokens existing pattern -->
From mobile/tailwind.config.js lines 9-29: colors map under theme.extend.colors. Add three keys: 'badge-unlocked-bg': '#E8F5E9', 'badge-locked': '#BDBDBD', 'badge-locked-bg': '#F5F5F5'.

<!-- Color constants existing pattern -->
From mobile/src/constants/colors.ts: each constant is `export const NAME = '#HEX';`. Add three new constants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install dependencies and migrate User schema</name>
  <files>backend/package.json, backend/src/models/User.ts, mobile/package.json</files>
  <read_first>
    - backend/package.json (current dependency list)
    - backend/src/models/User.ts (current schema — both interface AND schema definition blocks)
    - mobile/package.json (verify expo-linking already at ~7.0.5; do not modify)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Standard Stack section + Pattern 4 schema migration)
    - .planning/phases/05-home-dashboard/05-CONTEXT.md (D-74, D-79)
  </read_first>
  <action>
    Run two install commands. From `backend/`: `npm install node-cron@4.2.1` then `npm install --save-dev @types/node-cron@3.0.11`. From `mobile/`: `npx expo install expo-linear-gradient @react-native-community/datetimepicker`. These resolve to expo-linear-gradient ~55.0.14 and @react-native-community/datetimepicker ~9.1.0 (Expo SDK 54 compatible).

    Verify packages flagged as legitimate by RESEARCH.md `## Package Legitimacy Audit` table (all 4 entries marked Approved [VERIFIED: npm registry]). No `[ASSUMED]`/`[SUS]` packages — no human-verify checkpoint needed.

    Migrate `backend/src/models/User.ts` for D-74 + D-79:
    - In `IUser.profile` interface block, ADD `waterGoal?: number;` after `goalType?` line.
    - In `IUser.notifications` interface block, REMOVE the `reminderTime: string;` line and ADD `waterReminderTime: string;` and `workoutReminderTime: string;` in its place.
    - In schema `profile:` block (lines 39-45), ADD `waterGoal: { type: Number, default: 8 },` after the goalType line.
    - In schema `notifications:` block (lines 46-50), REMOVE the `reminderTime: { type: String, default: '08:00' }` line and ADD `waterReminderTime: { type: String, default: '08:00' },` and `workoutReminderTime: { type: String, default: '07:00' },` in its place.
    - Do NOT modify any other field on the User schema.

    Per RESEARCH.md Assumption A2: no production users exist — safe breaking change, no migration script needed.
  </action>
  <verify>
    <automated>cd backend && npm ls node-cron @types/node-cron 2>&1 | grep -E "(node-cron|@types/node-cron)" && cd ../mobile && npm ls expo-linear-gradient @react-native-community/datetimepicker 2>&1 | grep -E "(linear-gradient|datetimepicker)" && cd ../backend && npx tsc --noEmit src/models/User.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm ls node-cron` shows `node-cron@4.2.1` (or 4.2.x)
    - `cd backend && npm ls @types/node-cron` shows `@types/node-cron@3.0.11` (or 3.0.x)
    - `cd mobile && npm ls expo-linear-gradient` shows `expo-linear-gradient@~55.x.x`
    - `cd mobile && npm ls @react-native-community/datetimepicker` shows version `~9.x.x`
    - `grep -v '^//' backend/src/models/User.ts | grep -c 'reminderTime:'` outputs 0 (legacy field removed — match colon to exclude the new fields)
    - `grep -c 'waterReminderTime' backend/src/models/User.ts` outputs at least 2 (interface + schema)
    - `grep -c 'workoutReminderTime' backend/src/models/User.ts` outputs at least 2 (interface + schema)
    - `grep -c 'waterGoal' backend/src/models/User.ts` outputs at least 2 (interface + schema)
    - `cd backend && npx tsc --noEmit` returns exit code 0
  </acceptance_criteria>
  <done>
    Backend has node-cron + @types/node-cron installed at exact versions; mobile has expo-linear-gradient and @react-native-community/datetimepicker installed via expo install; User schema and IUser interface include waterGoal, waterReminderTime, workoutReminderTime; reminderTime field is gone; TypeScript compiles.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create WaterLog model and three integration test scaffolds</name>
  <files>backend/src/models/WaterLog.ts, backend/src/api/home/home.integration.test.ts, backend/src/api/water/water.integration.test.ts, backend/src/api/users/users.integration.test.ts, backend/package.json</files>
  <read_first>
    - backend/src/models/HabitLog.ts (lines 1-28 — schema + index pattern)
    - backend/src/models/WorkoutLog.ts (lines 1-30 — non-unique compound index)
    - backend/src/api/food/food.integration.test.ts (lines 1-76 — process.env stubs, MongoMemoryServer setup, beforeEach user setup with signAccessToken)
    - backend/package.json (existing test:* scripts at lines 16-21)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Pattern 3 WaterLog count snippet)
    - .planning/phases/05-home-dashboard/05-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Create `backend/src/models/WaterLog.ts` exporting an `IWaterLog` Document interface with fields `userId: mongoose.Types.ObjectId`, `loggedAt: Date`, plus `createdAt` and `updatedAt` from `{ timestamps: true }`. Schema fields: `userId` ref 'User' required, `loggedAt` type Date required default Date.now. Add `WaterLogSchema.index({ userId: 1, loggedAt: -1 })` — NOT unique (multiple glasses per day per D-73). Default export the model `mongoose.model<IWaterLog>('WaterLog', WaterLogSchema)`. Mirror the exact structure of `backend/src/models/WorkoutLog.ts`.

    Create three test scaffolds. Each test file:
    1. Starts with the EXACT same `process.env.*` stub block as `food.integration.test.ts` lines 4-19 (set JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, NODE_ENV='test', MONGODB_URI stub, CLOUDINARY_*, FIREBASE_*, OPENAI_API_KEY stub). Copy verbatim.
    2. Imports: `test, before, after, beforeEach` from `node:test`, `assert` from `node:assert/strict`, `request` from `supertest`, `mongoose`, `MongoMemoryServer` from `mongodb-memory-server`, `app` from `'../../app'`, `User` from `'../../models/User'`, `signAccessToken` from `'../../utils/jwt'`.
    3. Add MongoMemoryServer `before`/`after` hooks identical to food.integration.test.ts lines 43-51.
    4. Add `beforeEach` that deletes User docs and creates two users (userA, userB) and signs JWT for each, capturing `tokenA`, `tokenB`, `userIdA`, `userIdB` — identical pattern to food.integration.test.ts lines 53-76.
    5. Add at least ONE placeholder smoke test per file (Wave 1 plans will extend):
       - home.integration.test.ts: `test('placeholder: home test suite wired', () => { assert.equal(1, 1); });`
       - water.integration.test.ts: also import `WaterLog` from `'../../models/WaterLog'` and add deleteMany in beforeEach for WaterLog. Placeholder smoke test same shape.
       - users.integration.test.ts: placeholder smoke test same shape.

    Add three npm scripts to `backend/package.json` in the same `scripts` block (alphabetical / matching existing pattern). EXACT script values:
    - `"test:home": "node --env-file=.env.test --require tsx/cjs --test src/api/home/home.integration.test.ts"`
    - `"test:water": "node --env-file=.env.test --require tsx/cjs --test src/api/water/water.integration.test.ts"`
    - `"test:users": "node --env-file=.env.test --require tsx/cjs --test src/api/users/users.integration.test.ts"`

    Do NOT create routes/controllers/services for home/water/users — only the test files. Wave 1 plans will fill in the SUT (system under test).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run test:home &amp;&amp; npm run test:water &amp;&amp; npm run test:users</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/models/WaterLog.ts` exists
    - `grep -c "WaterLogSchema.index({ userId: 1, loggedAt: -1 })" backend/src/models/WaterLog.ts` outputs 1
    - `grep -c "unique: true" backend/src/models/WaterLog.ts` outputs 0 (index must NOT be unique)
    - `grep -c "loggedAt" backend/src/models/WaterLog.ts` outputs at least 3 (interface + schema field + index)
    - File `backend/src/api/home/home.integration.test.ts` exists and `npm run test:home` exits 0
    - File `backend/src/api/water/water.integration.test.ts` exists and `npm run test:water` exits 0
    - File `backend/src/api/users/users.integration.test.ts` exists and `npm run test:users` exits 0
    - All three test scripts present in backend/package.json: `grep -c 'test:home\|test:water\|test:users' backend/package.json` outputs at least 3
  </acceptance_criteria>
  <done>
    WaterLog model exists with correct non-unique compound index. Three integration test files exist with full MongoMemoryServer + JWT setup and at least one passing placeholder test each. Three npm scripts added matching the existing form. `npm run test:home && npm run test:water && npm run test:users` all pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add mobile achievement badge color tokens</name>
  <files>mobile/src/constants/colors.ts, mobile/tailwind.config.js</files>
  <read_first>
    - mobile/src/constants/colors.ts (existing tokens)
    - mobile/tailwind.config.js (existing colors map under theme.extend.colors)
    - .planning/phases/05-home-dashboard/05-UI-SPEC.md (## New Tokens Required section)
    - .planning/phases/05-home-dashboard/05-CONTEXT.md (D-78 unlocked = #E8F5E9 bg + #4CAF50 icon, locked = #F5F5F5 bg + #BDBDBD icon)
  </read_first>
  <action>
    In `mobile/src/constants/colors.ts`, append three new exports at the bottom of the file:
    - `export const BADGE_UNLOCKED_BG = '#E8F5E9';`
    - `export const BADGE_LOCKED = '#BDBDBD';`
    - `export const BADGE_LOCKED_BG = '#F5F5F5';`

    In `mobile/tailwind.config.js`, add three keys to the `theme.extend.colors` map (after the existing entries, before the closing brace):
    - `'badge-unlocked-bg': '#E8F5E9',`
    - `'badge-locked': '#BDBDBD',`
    - `'badge-locked-bg': '#F5F5F5',`

    Note: `BADGE_UNLOCKED` icon color (#4CAF50) and `BADGE_LOCKED_BG` (#F5F5F5) are intentional reuse — `#4CAF50` is already exported as `PRIMARY`, and `#F5F5F5` is already exported as `BACKGROUND`. The new tokens are semantic aliases for the badge feature only.
  </action>
  <verify>
    <automated>grep -E "BADGE_UNLOCKED_BG|BADGE_LOCKED|BADGE_LOCKED_BG" mobile/src/constants/colors.ts | wc -l &amp;&amp; grep -E "badge-unlocked-bg|badge-locked|badge-locked-bg" mobile/tailwind.config.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BADGE_UNLOCKED_BG = '#E8F5E9'" mobile/src/constants/colors.ts` outputs 1
    - `grep -c "BADGE_LOCKED = '#BDBDBD'" mobile/src/constants/colors.ts` outputs 1
    - `grep -c "BADGE_LOCKED_BG = '#F5F5F5'" mobile/src/constants/colors.ts` outputs 1
    - `grep -c "badge-unlocked-bg" mobile/tailwind.config.js` outputs 1
    - `grep -c "badge-locked-bg" mobile/tailwind.config.js` outputs 1
    - `grep -c "'badge-locked':" mobile/tailwind.config.js` outputs 1
  </acceptance_criteria>
  <done>
    Three new color constants exported from `mobile/src/constants/colors.ts` and three corresponding Tailwind tokens added to `mobile/tailwind.config.js`. Hex values match D-78 exactly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → backend/mobile | npm/expo packages installed cross trust boundary; supply chain risk |
| MongoDB driver → DB collection | WaterLog writes will be authenticated in Wave 1; Plan 01 only defines the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | npm installs (node-cron, @types/node-cron, expo-linear-gradient, @react-native-community/datetimepicker) | mitigate | All 4 packages marked [VERIFIED: npm registry] in RESEARCH.md Package Legitimacy Audit (Approved disposition, no SLOP/SUS). No human-verify checkpoint required. |
| T-05-01-01 | Tampering | User schema migration | accept | No production data exists per RESEARCH.md A2; breaking change is safe. No data loss risk. |
| T-05-01-02 | Information Disclosure | WaterLog model | mitigate | Model has no PII; only userId reference and timestamps. Wave 1 plans add authenticate middleware on routes. |
</threat_model>

<verification>
- All three integration test suites run green: `cd backend && npm run test:home && npm run test:water && npm run test:users`
- TypeScript compiles: `cd backend && npx tsc --noEmit`
- User schema migration is complete: legacy `reminderTime` field absent
- WaterLog model loaded without index conflicts
</verification>

<success_criteria>
- node-cron and @types/node-cron installed in backend at versions 4.2.x and 3.0.x respectively
- expo-linear-gradient and @react-native-community/datetimepicker installed in mobile at Expo SDK 54-compatible versions
- backend/src/models/User.ts contains waterGoal in profile and waterReminderTime + workoutReminderTime in notifications (legacy reminderTime removed)
- backend/src/models/WaterLog.ts exists with non-unique compound index { userId: 1, loggedAt: -1 }
- Three test files exist under backend/src/api/{home,water,users}/ each with a passing placeholder test
- Three npm scripts (test:home, test:water, test:users) added to backend/package.json
- Three new color tokens (BADGE_UNLOCKED_BG, BADGE_LOCKED, BADGE_LOCKED_BG) exported from mobile/src/constants/colors.ts and mobile/tailwind.config.js
</success_criteria>

<output>
Create `.planning/phases/05-home-dashboard/05-01-SUMMARY.md` when done with:
- Final installed package versions (exact)
- Diff summary of User.ts schema change
- WaterLog.ts contents (model definition)
- Locations of three new test files
- Note for downstream plans: "All Wave 1 tests must extend these scaffolds — do not create new test files; add tests to the existing files."
</output>
