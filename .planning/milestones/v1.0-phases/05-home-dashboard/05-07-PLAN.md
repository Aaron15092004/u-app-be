---
phase: 05-home-dashboard
plan: 07
type: execute
wave: 3
depends_on:
  - 05-05
files_modified:
  - mobile/src/app/(tabs)/profile/index.tsx
  - mobile/src/app/(tabs)/profile/edit.tsx
  - mobile/src/app/(tabs)/profile/notifications.tsx
  - mobile/src/app/(tabs)/profile/help.tsx
  - mobile/src/components/ui/AchievementBadge.tsx
  - mobile/src/components/ui/AchievementBadgesRow.tsx
  - mobile/src/components/ui/ProfileMenuCard.tsx
  - mobile/src/components/ui/ProfileMenuRow.tsx
  - mobile/src/components/ui/NotificationRationaleModal.tsx
  - mobile/src/components/ui/NotifToggleRow.tsx
  - mobile/src/components/ui/NotifTimeRow.tsx
  - mobile/src/components/ui/FAQItem.tsx
  - mobile/src/lib/storage/mmkv.ts
autonomous: false
requirements:
  - PRO-01
  - PRO-02
  - PRO-03
  - PRO-04
  - PRO-05
  - PRO-06
  - PRO-07
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "Profile tab (5th) renders avatar + name + email from auth.user, stats row (streak/workouts/kcal burned) from GET /api/users/profile/stats, achievement badges row from streak count, menu card with Edit/Notifications/Help, logout button at bottom"
    - "D-77: Edit Profile screen (push nav) has form fields (name, height, weight, goalType selector, waterGoal stepper) and PATCH /api/users/profile on submit"
    - "Notification Settings screen initialises form state from getProfileStatsApi().notifications (WARNING 3 fix — NO hardcoded defaults overwriting saved user settings on every screen open); has 2 toggle rows + 2 conditional time rows; time picker uses @react-native-community/datetimepicker; PATCH /api/users/notifications fires on toggle change immediately and on time change with 800ms debounce"
    - "Notification Rationale Modal shown once (MMKV flag 'notif_permission_asked'); 'Bật thông báo' triggers expo-notifications requestPermissionsAsync, then setNotificationChannelAsync (Android 13+ Pitfall 8), then getDevicePushTokenAsync, then registerTokenApi"
    - "Help & Support screen shows 4 FAQ accordion items + email/version rows"
    - "Logout button in Profile tab opens Alert.alert confirm and calls auth.logout() — replaces Home logout removed by Plan 06"
    - "D-78: Achievement badges row shows 4 badges (7/14/28/60 ngày), unlocked when streakDays >= milestone (#E8F5E9 bg + #4CAF50 icon) else gray (#F5F5F5 bg + #BDBDBD icon)"
    - "8 new UI components shipped in mobile/src/components/ui/"
  artifacts:
    - path: "mobile/src/app/(tabs)/profile/index.tsx"
      provides: "Profile tab — full implementation"
      min_lines: 90
    - path: "mobile/src/app/(tabs)/profile/edit.tsx"
      provides: "Edit Profile screen with form + PATCH"
      min_lines: 80
    - path: "mobile/src/app/(tabs)/profile/notifications.tsx"
      provides: "Notification Settings screen — initialises from getProfileStatsApi().notifications (WARNING 3 fix); rationale modal trigger"
      min_lines: 80
    - path: "mobile/src/app/(tabs)/profile/help.tsx"
      provides: "Help & Support screen with FAQ + email"
      min_lines: 60
    - path: "mobile/src/components/ui/NotificationRationaleModal.tsx"
      provides: "Modal with 3 bullets + 2 CTAs; integrates expo-notifications"
      contains: "requestPermissionsAsync"
  key_links:
    - from: "mobile/src/app/(tabs)/profile/index.tsx"
      to: "getProfileStatsApi + getStreakApi + auth.logout"
      via: "useQuery + Alert.alert"
      pattern: "auth.logout"
    - from: "mobile/src/app/(tabs)/profile/edit.tsx"
      to: "updateProfileApi"
      via: "TanStack useMutation"
      pattern: "updateProfileApi"
    - from: "mobile/src/app/(tabs)/profile/notifications.tsx"
      to: "getProfileStatsApi (for form init — WARNING 3) + updateNotificationsApi + NotificationRationaleModal"
      via: "useQuery (form init) + useMutation (PATCH) + permission flow"
      pattern: "getProfileStatsApi"
    - from: "mobile/src/components/ui/NotificationRationaleModal.tsx"
      to: "registerTokenApi"
      via: "FCM token registration after permission grant"
      pattern: "registerTokenApi"
  user_setup:
    - service: firebase-fcm
      why: "Push notification permission must be granted on a real device; Expo Go does not support FCM in SDK 54+"
      env_vars: []
      dashboard_config:
        - task: "Run on EAS development build (not Expo Go)"
          location: "Local dev: `eas build --profile development` or device simulator with FCM enabled"
---

<validation_note>
**Mobile behavioral test deferral (per checker BLOCKER 2 fix):**

This plan delivers Phase 5's behavioral verification gate via the blocking-human checkpoint in **Task 3** ("Phase 5 device verification"). That checkpoint executes 15 manual steps on a real EAS development build covering:

- Home dashboard render (covered for Plan 06)
- Water log +/- flow (covered for Plan 06)
- Profile tab navigation, edit profile PATCH (this plan)
- Notification settings, rationale modal, FCM water reminder, FCM workout reminder, FCM streak alert at 20:00 UTC+7 (this plan + Plan 04)
- Help screen FAQ + clipboard copy (this plan)
- Logout flow (this plan, replacing Plan 06's removed home logout)

Automated verification per task is `npx tsc --noEmit` (TypeScript strict compilation). React Native Testing Library behavioral tests are infeasible here because: `expo-notifications.requestPermissionsAsync` requires a real OS permission dialog, `getDevicePushTokenAsync` requires native FCM, MMKV requires a development build, and `@react-native-community/datetimepicker` UI is not exercisable in jsdom. Per STATE.md: "MMKV, Google Sign-In, Apple Sign-In mocked for Expo Go testing" — the project's mobile test infrastructure does not extend to these native modules.

**Task 1 independent validation (per checker WARNING 2 — Plan 07 scope):**
Plan 07 is large (8 UI components, 4 screens, MMKV helper, permission flow). The checker noted the size but did not require a split. To mitigate the size risk, Task 1 includes an explicit interim validation step: after writing `NotificationRationaleModal.tsx` (the highest-risk component — it integrates expo-notifications + FCM + MMKV), run `npx tsc --noEmit` on its own before moving to the other 7 components and to Task 2. This catches type contract failures early instead of at end of plan.

**Styling decision (per checker WARNING 5):** CLAUDE.md specifies NativeWind v4 but existing Phase 3-4 components (`WeeklyStatCard.tsx`, `StreakBadge.tsx`, `GoalCard.tsx`, `PrimaryButton.tsx`) use `StyleSheet.create`. Phase 5 continues `StyleSheet.create` for consistency with the established component pattern. NativeWind v4 migration is deferred to a dedicated UI refactor phase. All 8 new components here match the existing convention.
</validation_note>

<objective>
Wave 3 (parallel with Plan 06): Implement the Profile tab and its 3 sub-screens (Edit, Notifications, Help) plus the Notification Permission Rationale modal. Build 8 new UI components. Wire up GET /api/users/profile/stats (now including notifications block per Plan 03 WARNING 3 fix), PATCH /api/users/profile, PATCH /api/users/notifications, expo-notifications permission flow, and FCM token registration via POST /api/notifications/register-token.

Purpose: Delivers PRO-01 through PRO-07 and NOTIF-01 (rationale) + the mobile-side surface for NOTIF-02/03/04 (toggle/time UI; backend cron is owned by Plan 04). Pitfall 9 mitigation: this plan moves logout from Home (deleted in Plan 06) to Profile — both ship in the same wave so logout is never absent.

**WARNING 3 fix integration:** `notifications.tsx` initialises its form state from the user's saved `notifications` block returned by `getProfileStatsApi` (added by Plan 03). It does NOT use hardcoded defaults like `waterReminderTime='08:00'` for initial form state — that would overwrite the user's saved setting on every screen open, which is the bug the checker caught. Defaults are only used as fallback while the query is still loading on first paint.

Output: 4 screens + 8 components + MMKV helper update + permission flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-home-dashboard/05-UI-SPEC.md
@.planning/phases/05-home-dashboard/05-RESEARCH.md
@.planning/phases/05-home-dashboard/05-PATTERNS.md
@.planning/phases/05-home-dashboard/05-CONTEXT.md
@.planning/phases/05-home-dashboard/05-03-SUMMARY.md
@.planning/phases/05-home-dashboard/05-05-SUMMARY.md
@mobile/src/lib/api/types.ts
@mobile/src/lib/api/users.api.ts
@mobile/src/lib/api/notifications.api.ts
@mobile/src/lib/api/habits.api.ts
@mobile/src/providers/AuthProvider.tsx
@mobile/src/lib/storage/mmkv.ts
@mobile/src/app/(tabs)/bmi/index.tsx
@mobile/src/app/(tabs)/index.tsx
@mobile/src/components/ui/PrimaryButton.tsx
@mobile/src/components/ui/ScreenHeader.tsx
@mobile/src/components/ui/StreakBadge.tsx
@mobile/src/components/ui/GoalCard.tsx
@mobile/src/constants/colors.ts

<interfaces>
Mobile types (Plan 05 — POST checker fixes):
  IUserNotifications { waterReminder, workoutReminder, waterReminderTime, workoutReminderTime }
  IProfileStats { streakDays, totalWorkouts, totalKcalBurned, notifications: IUserNotifications }   // WARNING 3 fix — notifications added

API modules (Plan 05):
  getProfileStatsApi(): Promise of IProfileStats   // response now includes notifications block
  updateProfileApi(body: { name?, heightCm?, weightKg?, goalType?, waterGoal? }): Promise of { name, email, profile }
  updateNotificationsApi(body: Partial of IUserNotifications): Promise of { notifications }
  registerTokenApi(token: string, platform: 'ios'|'android'): Promise of void

Existing getStreakApi from habits.api.ts (REUSE — do not duplicate):
  getStreakApi(): Promise of { streakDays: number }

AuthProvider for logout (existing):
  const auth = useAuth();
  auth.user (IAuthUser { id, email, name, role, profileCompleted })
  await auth.logout();

MMKV helpers (existing storage/mmkv.ts):
  Existing exports: getOnboardingSeen, setOnboardingSeen, setCachedUser, getCachedUser, clearCachedUser, plus raw `storage` { get/set/delete }.
  ADD: getNotifAsked(): boolean and setNotifAsked(value: boolean): void for NOTIF_ASKED_KEY = 'notif_permission_asked'. Implement by calling existing `storage.getBoolean`/`storage.set`.

expo-notifications API (installed):
  import * as Notifications from 'expo-notifications';
  const { granted } = await Notifications.requestPermissionsAsync();
  await Notifications.setNotificationChannelAsync('default', { name: 'default', importance: Notifications.AndroidImportance.MAX });
  const tokenData = await Notifications.getDevicePushTokenAsync();
  // tokenData.data is the native FCM token

@react-native-community/datetimepicker (installed in Plan 01):
  import DateTimePicker from '@react-native-community/datetimepicker';
  <DateTimePicker value={date} mode="time" display={Platform.OS === 'ios' ? 'inline' : 'default'} onChange={(_, d) => { if (d) setTime(d); }} />

Color tokens (Plan 01 + existing):
  PRIMARY, BACKGROUND, SURFACE, TEXT, TEXT_SECONDARY, STREAK_BADGE = '#FF6B35'
  BADGE_UNLOCKED_BG = '#E8F5E9', BADGE_LOCKED = '#BDBDBD', BADGE_LOCKED_BG = '#F5F5F5' (added in Plan 01)

UI-SPEC Profile screen layout (Screen 3):
  Section order: Avatar+name+email -> Stats row -> Achievement badges -> Menu items -> Logout
  Menu rows (3): Chỉnh sửa hồ sơ -> /(tabs)/profile/edit, Cài đặt thông báo -> /(tabs)/profile/notifications, Trợ giúp & Hỗ trợ -> /(tabs)/profile/help

UI-SPEC Edit Profile (Screen 4):
  Form fields: Tên hiển thị (TextInput), Chiều cao cm (numeric), Cân nặng kg (numeric), Mục tiêu sức khỏe (3-option selector "Giảm cân"/"Duy trì"/"Tăng cân" maps to lose/maintain/gain), Mục tiêu nước mỗi ngày ly (stepper 4-16 via +/- buttons)
  Save button: PrimaryButton filled "Lưu thay đổi"
  Success: Alert.alert "Thành công" "Hồ sơ đã được cập nhật." then router.back()

UI-SPEC Notification Settings (Screen 5):
  2 toggle rows (waterReminder/workoutReminder) + 2 conditional time rows (visible only when toggle ON)
  Toggle change PATCH immediate; time change PATCH with 800ms debounce
  Info text at bottom: "Streak alert sẽ tự động gửi lúc 20:00 mỗi ngày nếu bạn chưa hoàn thành thói quen."

UI-SPEC Rationale Modal (Screen 6) — 3 bullets:
  water-outline + "Nhắc uống nước đúng giờ mỗi ngày"
  barbell-outline + "Nhắc bắt đầu buổi tập của bạn"
  flame-outline + "Cảnh báo khi bạn sắp mất streak"
  2 CTAs: "Bật thông báo" (filled), "Để sau" (outlined)

UI-SPEC Help screen (Screen 7) — 4 FAQ accordion:
  Q1: "Làm thế nào để cập nhật cân nặng?"
  Q2: "Dữ liệu của tôi có được lưu an toàn không?"
  Q3: "Cách tính streak là gì?"
  Q4: "Tôi có thể xóa tài khoản không?"
  Plus email row: support@u-app.vn (Clipboard.setString); version row from expo-constants
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build 8 Profile/Notification UI components + MMKV helper (with interim tsc validation after NotificationRationaleModal per WARNING 2)</name>
  <files>mobile/src/components/ui/AchievementBadge.tsx, mobile/src/components/ui/AchievementBadgesRow.tsx, mobile/src/components/ui/ProfileMenuCard.tsx, mobile/src/components/ui/ProfileMenuRow.tsx, mobile/src/components/ui/NotificationRationaleModal.tsx, mobile/src/components/ui/NotifToggleRow.tsx, mobile/src/components/ui/NotifTimeRow.tsx, mobile/src/components/ui/FAQItem.tsx, mobile/src/lib/storage/mmkv.ts</files>
  <read_first>
    - mobile/src/components/ui/PrimaryButton.tsx
    - mobile/src/components/ui/ScreenHeader.tsx
    - mobile/src/components/ui/StreakBadge.tsx
    - mobile/src/constants/colors.ts (with Plan 01 badge tokens)
    - mobile/src/lib/storage/mmkv.ts (existing helpers — append, do not rewrite)
    - .planning/phases/05-home-dashboard/05-UI-SPEC.md (Screens 3-7 — exact dimensions/copy/colors)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Pattern 7 expo-notifications permission flow + Pitfall 8 Android 13+ channel)
    - .planning/phases/05-home-dashboard/05-CONTEXT.md (D-78 milestones 7/14/28/60)
  </read_first>
  <action>
    **Styling:** Use `StyleSheet.create` per existing convention (NativeWind v4 migration deferred — see validation_note).

    **Build order (per WARNING 2 — validate the highest-risk component first):**

    1. First, append MMKV helpers to `mobile/src/lib/storage/mmkv.ts` (do NOT modify existing exports):
       - Add constant `const NOTIF_ASKED_KEY = 'notif_permission_asked';` near the other key constants.
       - Add exports: `export function getNotifAsked(): boolean { return storage.getBoolean(NOTIF_ASKED_KEY) ?? false; }` and `export function setNotifAsked(value: boolean): void { storage.set(NOTIF_ASKED_KEY, value); }`.

    2. **Build `NotificationRationaleModal.tsx` SECOND** (highest-risk: integrates expo-notifications + FCM + MMKV + PrimaryButton). Spec below.

    3. **Run `cd mobile && npx tsc --noEmit` after writing the modal alone, before proceeding to the other 7 components.** If tsc fails, fix in place — do NOT continue building components on top of a broken type contract. This interim validation is a checker-mandated mitigation for Plan 07's size (WARNING 2).

    4. After tsc passes for the modal, build the remaining 7 components in any order.

    **Component specs:**

    1. AchievementBadge.tsx — Props { milestone: number; unlocked: boolean }. Column layout, alignItems center. Circle (56x56 borderRadius 28, backgroundColor BADGE_UNLOCKED_BG when unlocked else BADGE_LOCKED_BG) containing Ionicons `medal-outline` size 28 color (PRIMARY when unlocked else BADGE_LOCKED). Below: Text "{milestone} ngày" 12px weight 700 color (PRIMARY when unlocked else BADGE_LOCKED).

    2. AchievementBadgesRow.tsx — Props { streakDays: number }. White card borderRadius 12 padding 16 elevation 2. Section title "Thành tích" 16px weight 700 TEXT marginBottom 12. Row with 4 AchievementBadge using milestones [7, 14, 28, 60]. For each milestone: unlocked = streakDays >= milestone. justifyContent space-around.

    3. ProfileMenuCard.tsx — Props { children: React.ReactNode }. White card borderRadius 12 padding 0. Wraps children with vertical separators (1px #E0E0E0) between each child via React.Children.map: render child, then if not last child render a divider View. Use overflow hidden on outer.

    4. ProfileMenuRow.tsx — Props { iconName: string; label: string; onPress: () => void }. Pressable row, height 52, paddingHorizontal 16, flexDirection row, alignItems center, justifyContent space-between. Left: row with Ionicons {iconName} size 20 TEXT_SECONDARY + Text label 16px weight 400 TEXT (gap 12). Right: Ionicons chevron-forward size 16 BADGE_LOCKED ('#BDBDBD'). Press feedback: backgroundColor #F5F5F5 when pressed.

    5. NotificationRationaleModal.tsx — Props { visible: boolean; onAccept: () => Promise of void; onDismiss: () => void }. Uses React Native Modal component with animationType 'slide', transparent false. Centered column paddingHorizontal 24 paddingTop 48 paddingBottom 32 backgroundColor SURFACE flex 1. Content top-to-bottom: Ionicons notifications-outline size 80 PRIMARY centered marginBottom 24, Text "Nhận nhắc nhở sức khỏe" 28px weight 700 TEXT textAlign center marginBottom 12, three bullet rows (each flexDirection row gap 8 marginBottom 8): water-outline size 16 '#64B5F6' + "Nhắc uống nước đúng giờ mỗi ngày" 16px TEXT, barbell-outline size 16 PRIMARY + "Nhắc bắt đầu buổi tập của bạn", flame-outline size 16 STREAK_BADGE + "Cảnh báo khi bạn sắp mất streak". Below bullets marginTop 32: PrimaryButton variant filled label "Bật thông báo" onPress=onAccept (which is async; show loading while in-flight). marginTop 12: PrimaryButton variant outlined label "Để sau" onPress=onDismiss.

    The modal's internal "Bật thông báo" handler runs the full expo-notifications flow before calling the prop `onAccept`:
    ```
    const handleEnable = async () => {
      setLoading(true);
      try {
        const { granted } = await Notifications.requestPermissionsAsync();
        if (granted) {
          if (Platform.OS === 'android') {
            await Notifications.setNotificationChannelAsync('default', {
              name: 'default',
              importance: Notifications.AndroidImportance.MAX,
            });
          }
          const tokenData = await Notifications.getDevicePushTokenAsync();
          if (tokenData?.data) {
            await registerTokenApi(tokenData.data, Platform.OS === 'ios' ? 'ios' : 'android');
          }
        }
      } catch (err) {
        console.warn('[NotificationRationaleModal] permission flow error:', err);
      } finally {
        setLoading(false);
        await onAccept();   // always call onAccept so MMKV flag is set regardless of permission outcome
      }
    };
    ```

    6. NotifToggleRow.tsx — Props { label: string; sublabel: string; value: boolean; onChange: (v: boolean) => void }. Row height 64 paddingHorizontal 16 flexDirection row alignItems center justifyContent space-between. Left column: label 16px weight 700 TEXT, sublabel 12px weight 400 TEXT_SECONDARY below. Right: React Native Switch with trackColor { false: '#E0E0E0', true: PRIMARY } thumbColor '#FFFFFF', value=value, onValueChange=onChange.

    7. NotifTimeRow.tsx — Props { label: string; sublabel: string; time: string; onTimeChange: (newTime: string) => void }. Row pressable to open time picker. Display similar layout to NotifToggleRow but right side shows: time 16px weight 700 PRIMARY + chevron-forward 16 '#BDBDBD'. On press: set local state showPicker=true and render DateTimePicker from '@react-native-community/datetimepicker' below the row (iOS inline display) or as a modal (Android default). Provide a helper at top: parseTimeString(timeStr) -> Date and formatTimeString(date) -> string ('HH:MM' zero-padded). On DateTimePicker onChange: format the date back to 'HH:MM' and call onTimeChange(formatted). Caller is responsible for debouncing PATCH calls.

    8. FAQItem.tsx — Props { question: string; answer: string }. Local state expanded:boolean. Pressable row: question 16px weight 700 TEXT + chevron-down (when expanded) / chevron-forward (collapsed) Ionicons. Below when expanded: answer 16px weight 400 TEXT_SECONDARY paddingTop 8 paddingBottom 8.

    All 8 components: import React, Ionicons from '@expo/vector-icons', color tokens from '../../constants/colors'. Follow StyleSheet.create pattern of WeeklyStatCard.tsx. NotificationRationaleModal also imports `* as Notifications from 'expo-notifications'`, `{ Platform }` from 'react-native', and `{ registerTokenApi }` from '../../lib/api/notifications.api'.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cd mobile && npx tsc --noEmit exits 0
    - All 8 component files exist under mobile/src/components/ui/
    - `grep -c "BADGE_UNLOCKED_BG\|BADGE_LOCKED" mobile/src/components/ui/AchievementBadge.tsx` ≥ 2 (uses new color tokens)
    - `grep -c "\\[7, 14, 28, 60\\]\|MILESTONES" mobile/src/components/ui/AchievementBadgesRow.tsx` ≥ 1 (D-78 milestones)
    - `grep -c "requestPermissionsAsync\|getDevicePushTokenAsync\|setNotificationChannelAsync" mobile/src/components/ui/NotificationRationaleModal.tsx` ≥ 3 (full permission flow per Pitfall 8)
    - `grep -c "registerTokenApi" mobile/src/components/ui/NotificationRationaleModal.tsx` ≥ 1
    - `grep -c "Modal" mobile/src/components/ui/NotificationRationaleModal.tsx` ≥ 1
    - `grep -c "Switch" mobile/src/components/ui/NotifToggleRow.tsx` ≥ 1
    - `grep -c "@react-native-community/datetimepicker" mobile/src/components/ui/NotifTimeRow.tsx` ≥ 1
    - `grep -c "getNotifAsked\|setNotifAsked" mobile/src/lib/storage/mmkv.ts` ≥ 2 (helpers added)
    - `grep -c "Bật thông báo\|Để sau\|Nhận nhắc nhở sức khỏe" mobile/src/components/ui/NotificationRationaleModal.tsx` ≥ 3 (UI-SPEC copy)
  </acceptance_criteria>
  <done>
    8 components built per UI-SPEC using StyleSheet.create. MMKV helper has getNotifAsked/setNotifAsked. NotificationRationaleModal integrates expo-notifications permission flow + setNotificationChannelAsync (Pitfall 8) + registerTokenApi. NotificationRationaleModal was validated with `npx tsc --noEmit` independently before the other 7 components were built (WARNING 2 mitigation). TypeScript compiles for the whole plan.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement Profile, Edit, Notifications, Help screens; notifications.tsx initialises form from getProfileStatsApi().notifications (WARNING 3 fix); add logout</name>
  <files>mobile/src/app/(tabs)/profile/index.tsx, mobile/src/app/(tabs)/profile/edit.tsx, mobile/src/app/(tabs)/profile/notifications.tsx, mobile/src/app/(tabs)/profile/help.tsx</files>
  <read_first>
    - mobile/src/app/(tabs)/profile/index.tsx (Plan 05 stub — replace)
    - mobile/src/app/(tabs)/profile/edit.tsx (Plan 05 stub — replace)
    - mobile/src/app/(tabs)/profile/notifications.tsx (Plan 05 stub — replace)
    - mobile/src/app/(tabs)/profile/help.tsx (Plan 05 stub — replace)
    - mobile/src/app/(tabs)/bmi/index.tsx (PATCH mutation pattern lines 75-90)
    - mobile/src/app/(tabs)/index.tsx (legacy logout Alert.alert pattern — pre-Plan-06; use the Alert.alert call structure)
    - mobile/src/providers/AuthProvider.tsx (auth.logout signature)
    - mobile/src/lib/api/users.api.ts (Plan 05 functions — getProfileStatsApi returns IProfileStats with notifications block)
    - mobile/src/lib/api/types.ts (IProfileStats now includes notifications: IUserNotifications per Plan 05 update)
    - mobile/src/lib/api/habits.api.ts (getStreakApi REUSE — do not duplicate)
    - mobile/src/lib/api/notifications.api.ts (registerTokenApi)
    - mobile/src/lib/storage/mmkv.ts (post-Task-1 — getNotifAsked + setNotifAsked)
    - .planning/phases/05-home-dashboard/05-03-SUMMARY.md (confirm getProfileStatsApi response includes notifications block)
    - .planning/phases/05-home-dashboard/05-UI-SPEC.md (Screens 3-7)
  </read_first>
  <action>
    **Styling:** All four screens use `StyleSheet.create` per existing convention (validation_note WARNING 5).

    Replace `mobile/src/app/(tabs)/profile/index.tsx` ENTIRELY:
    - Imports: React useState, SafeAreaView, ScrollView, View, Text, StyleSheet, Image, Alert from 'react-native'; useRouter from 'expo-router'; useQuery from '@tanstack/react-query'; useAuth from '../../../providers/AuthProvider'; getProfileStatsApi from '../../../lib/api/users.api'; getStreakApi from '../../../lib/api/habits.api'; PrimaryButton; Ionicons; AchievementBadgesRow, ProfileMenuCard, ProfileMenuRow from '../../../components/ui/'; PRIMARY, BACKGROUND, SURFACE, TEXT, TEXT_SECONDARY, STREAK_BADGE from '../../../constants/colors'.
    - Default function ProfileScreen.
    - Hooks: const auth = useAuth(); const router = useRouter(); const [logoutLoading, setLogoutLoading] = useState(false);
    - Queries: statsQuery = useQuery({ queryKey: ['users','profile','stats'], queryFn: getProfileStatsApi }); streakQuery = useQuery({ queryKey: ['habits','streak'], queryFn: getStreakApi }).
    - confirmLogout function: Alert.alert("Đăng xuất","Bạn có chắc muốn đăng xuất không?", [{text:'Hủy',style:'cancel'}, {text:'Đăng xuất',style:'destructive', onPress: async () => { setLogoutLoading(true); await auth.logout(); setLogoutLoading(false); }}]).
    - Layout: SafeAreaView backgroundColor BACKGROUND, ScrollView paddingHorizontal 16 paddingTop 24 paddingBottom 48:
      1. Avatar+name+email block: column alignItems center. Avatar 80x80 circular: if auth.user.avatar use Image with borderRadius 40, else View 80x80 backgroundColor PRIMARY borderRadius 40 alignItems center justifyContent center with Text auth.user.name.charAt(0) 28px weight 700 white. Name 20px weight 700 TEXT marginTop 12. Email 16px TEXT_SECONDARY marginTop 4.
      2. Stats row: white card marginTop 24, borderRadius 12, padding 16, elevation 2, flexDirection row. Three columns separated by 1px #E0E0E0 vertical dividers:
         - Col 1: flame-outline size 16 STREAK_BADGE + value (statsQuery.data?.streakDays ?? '—') 20px weight 700 TEXT, label "ngày streak" 12px TEXT_SECONDARY.
         - Col 2: value statsQuery.data?.totalWorkouts ?? '—' 20px weight 700, label "bài tập".
         - Col 3: value statsQuery.data?.totalKcalBurned ?? '—' 20px weight 700, label "kcal đốt".
      3. AchievementBadgesRow with streakDays = streakQuery.data?.streakDays ?? 0, marginTop 16.
      4. ProfileMenuCard marginTop 16 wrapping 3 ProfileMenuRow:
         - iconName="create-outline" label="Chỉnh sửa hồ sơ" onPress=()=>router.push('/(tabs)/profile/edit')
         - iconName="notifications-outline" label="Cài đặt thông báo" onPress=()=>router.push('/(tabs)/profile/notifications')
         - iconName="help-circle-outline" label="Trợ giúp & Hỗ trợ" onPress=()=>router.push('/(tabs)/profile/help')
      5. PrimaryButton variant outlined label "Đăng xuất" loading={logoutLoading} onPress=confirmLogout marginTop 24 full width.
    - Loading/error states for queries: render '—' placeholders in stats while loading; on error show inline error card with retry button.

    Replace `mobile/src/app/(tabs)/profile/edit.tsx` ENTIRELY:
    - Imports: React useState, KeyboardAvoidingView, ScrollView, SafeAreaView, View, Text, TextInput, Platform, Alert, StyleSheet; useRouter; useMutation, useQueryClient from '@tanstack/react-query'; useAuth; ScreenHeader; PrimaryButton; updateProfileApi.
    - Hooks: useAuth, useRouter, useQueryClient. Local state: name (default auth.user?.name ?? ''), heightCm (default null/empty string), weightKg, goalType ('lose'|'maintain'|'gain'|null default null), waterGoal (default 8).
    - Form fields per UI-SPEC Screen 4:
      - Tên hiển thị: TextInput (height 48, borderRadius 8, borderWidth 1, borderColor #E0E0E0 inactive / PRIMARY focused, paddingHorizontal 12, fontSize 16, placeholder "Nhập tên của bạn", returnKeyType "next", value=name, onChangeText=setName).
      - Chiều cao (cm): TextInput keyboardType "numeric" placeholder "170" value=heightCm onChangeText set as string then coerce to number on submit.
      - Cân nặng (kg): TextInput keyboardType "numeric" placeholder "65".
      - Mục tiêu sức khỏe: 3-option row (Pressable cards: "Giảm cân"->'lose', "Duy trì"->'maintain', "Tăng cân"->'gain'). Each card flex 1 height 80 borderRadius 12 borderWidth 2. Selected: borderColor PRIMARY backgroundColor 'rgba(76,175,80,0.1)'. Unselected: borderColor #E0E0E0 backgroundColor SURFACE.
      - Mục tiêu nước mỗi ngày (ly): row with two circular buttons (52x52 borderRadius 26; minus outlined PRIMARY, plus filled PRIMARY) + value display 20px weight 700 TEXT center. Range 4-16 (per UI-SPEC). Disable minus when waterGoal <= 4, disable plus when waterGoal >= 16.
    - mutation = useMutation with mutationFn = () => updateProfileApi({ name, heightCm: Number(heightCm) || undefined, weightKg: Number(weightKg) || undefined, goalType: goalType ?? undefined, waterGoal }), onSuccess: () => { qc.invalidateQueries({ queryKey: ['home','summary'] }); qc.invalidateQueries({ queryKey: ['users','profile','stats'] }); qc.invalidateQueries({ queryKey: ['water','today'] }); Alert.alert('Thành công','Hồ sơ đã được cập nhật.'); router.back(); }, onError: () => { Alert.alert('Lỗi','Có lỗi xảy ra. Vui lòng thử lại.'); }.
    - **Note (WARNING 4 chain):** invalidate `['water','today']` alongside `['home','summary']` and `['users','profile','stats']` so the water screen reflects an updated waterGoal immediately. (water.tsx in Plan 06 reads waterGoal from waterQuery; without this invalidation a user editing waterGoal in Profile would still see the old value on the water screen until next refetch.)
    - Save button: PrimaryButton variant filled label "Lưu thay đổi" loading={mutation.isPending} onPress=()=>mutation.mutate() marginTop 24.
    - Wrap content in KeyboardAvoidingView behavior Platform.OS === 'ios' ? 'padding' : 'height'.

    Replace `mobile/src/app/(tabs)/profile/notifications.tsx` ENTIRELY — **WARNING 3 FIX: initialise form state from server, not from hardcoded defaults**:
    - Imports: React useState useRef useEffect, SafeAreaView, ScrollView, View, Text, StyleSheet; useMutation, useQuery, useQueryClient; ScreenHeader, NotifToggleRow, NotifTimeRow, NotificationRationaleModal; updateNotificationsApi, getProfileStatsApi from '../../../lib/api/users.api'; getNotifAsked, setNotifAsked from '../../../lib/storage/mmkv'; TEXT_SECONDARY.
    - **Initialise form state from getProfileStatsApi** (the WARNING 3 fix):
      ```
      const qc = useQueryClient();
      const profileStatsQuery = useQuery({
        queryKey: ['users', 'profile', 'stats'],
        queryFn: getProfileStatsApi,
      });
      const serverNotif = profileStatsQuery.data?.notifications;

      const [waterReminder, setWaterReminder] = useState<boolean | null>(null);
      const [workoutReminder, setWorkoutReminder] = useState<boolean | null>(null);
      const [waterReminderTime, setWaterReminderTime] = useState<string | null>(null);
      const [workoutReminderTime, setWorkoutReminderTime] = useState<string | null>(null);

      // Seed local form state once the server data arrives — and only once (don't overwrite user-in-progress edits)
      useEffect(() => {
        if (serverNotif && waterReminder === null) {
          setWaterReminder(serverNotif.waterReminder);
          setWorkoutReminder(serverNotif.workoutReminder);
          setWaterReminderTime(serverNotif.waterReminderTime);
          setWorkoutReminderTime(serverNotif.workoutReminderTime);
        }
      }, [serverNotif, waterReminder]);
      ```
      The `=== null` guard ensures the screen does NOT overwrite the user's in-progress edits when the query refetches (e.g., on window focus). This is the core of the WARNING 3 fix.
    - **Loading guard:** if `waterReminder === null` (i.e., the seed has not happened yet), render a skeleton or loading indicator and return — do NOT render the toggle/time rows with bogus default values. Once the seed completes, render the real form.
    - Modal state: const [showRationale, setShowRationale] = useState(false). On mount via useEffect: if (!getNotifAsked()) setShowRationale(true).
    - mutation = useMutation with mutationFn = updateNotificationsApi, onSuccess: (res) => { qc.invalidateQueries({ queryKey: ['users','profile','stats'] }); }. Toggle change calls mutation.mutate({ waterReminder: newValue }) etc. immediately (no debounce).
    - Time change with debounce: useRef debounceTimer; clearTimeout previous; setTimeout 800ms then mutation.mutate({ waterReminderTime: newTime }).
    - Layout: ScreenHeader title "Cài đặt thông báo" showBack. White card (borderRadius 12 padding 0 elevation 2 marginTop 16) wrapping rows with 1px #E0E0E0 separators between:
      1. NotifToggleRow label "Nhắc uống nước" sublabel "Nhắc bạn uống nước mỗi ngày" value={waterReminder ?? false} onChange={(v) => { setWaterReminder(v); mutation.mutate({ waterReminder: v }); }}.
      2. Conditional (waterReminder === true): NotifTimeRow label "Giờ nhắc uống nước" sublabel={waterReminderTime ?? '08:00'} time={waterReminderTime ?? '08:00'} onTimeChange={(newTime) => { setWaterReminderTime(newTime); debouncedMutate({ waterReminderTime: newTime }); }}.
      3. Separator 1px #E0E0E0.
      4. NotifToggleRow label "Nhắc tập luyện" sublabel "Nhắc bạn bắt đầu tập mỗi ngày" value={workoutReminder ?? false} onChange similar.
      5. Conditional NotifTimeRow for workoutReminderTime similar.
    - Info text at bottom: Text "Streak alert sẽ tự động gửi lúc 20:00 mỗi ngày nếu bạn chưa hoàn thành thói quen." 12px TEXT_SECONDARY marginTop 16 textAlign center.
    - NotificationRationaleModal visible={showRationale} onAccept={handleAccept} onDismiss={handleDismiss}. handleAccept: setNotifAsked(true); setShowRationale(false). handleDismiss: setNotifAsked(true); setShowRationale(false). [Setting the asked flag in both branches matches UI-SPEC "shown once".] The modal's internal handler runs the permission/FCM flow before calling onAccept (per Task 1 spec).

    Replace `mobile/src/app/(tabs)/profile/help.tsx` ENTIRELY:
    - Imports: React, SafeAreaView, ScrollView, View, Text, StyleSheet, Pressable, Linking; *for clipboard*: try `import * as Clipboard from 'expo-clipboard'` (Expo SDK 54 ships this), with a fallback to `Alert.alert('Đã sao chép','support@u-app.vn')` if expo-clipboard is not yet installed; Ionicons; Constants from 'expo-constants'; ScreenHeader; FAQItem; SURFACE, TEXT, TEXT_SECONDARY.
    - Layout: SafeAreaView, ScrollView paddingHorizontal 16 paddingTop 16. ScreenHeader title "Trợ giúp & Hỗ trợ" showBack.
    - FAQ card (white borderRadius 12 padding 16 elevation 2 marginTop 16): Text "Câu hỏi thường gặp" 16px weight 700 TEXT marginBottom 12. Then 4 FAQItem:
      - Q "Làm thế nào để cập nhật cân nặng?" A "Vào tab BMI, dùng thanh trượt để chọn cân nặng và chiều cao mới, sau đó nhấn Lưu."
      - Q "Dữ liệu của tôi có được lưu an toàn không?" A "Có. Tất cả dữ liệu được lưu trữ trên MongoDB Atlas với mã hóa khi truyền và khi lưu. Mật khẩu được hash bằng bcrypt."
      - Q "Cách tính streak là gì?" A "Streak là số ngày liên tiếp bạn đánh dấu hoàn thành ít nhất 3 thói quen trong 6 thói quen mặc định."
      - Q "Tôi có thể xóa tài khoản không?" A "Hiện tại bạn có thể xóa tài khoản bằng cách liên hệ với chúng tôi qua email hỗ trợ. Tính năng tự xóa sẽ có trong bản cập nhật tiếp theo."
    - Contact card (white borderRadius 12 padding 16 elevation 2 marginTop 16): rows:
      1. mail-outline 20 TEXT_SECONDARY + "support@u-app.vn" 16px TEXT + copy-outline 20 TEXT_SECONDARY (Pressable copies email to clipboard).
      2. "Phiên bản ứng dụng" 16px TEXT + Constants.expoConfig?.version (or 'v1.0.0' fallback) 12px TEXT_SECONDARY right-aligned.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cd mobile && npx tsc --noEmit exits 0
    - All 4 screen files exist and were modified (no longer Plan 05 stubs).
    - `grep -c "auth.logout\|Đăng xuất" mobile/src/app/\\(tabs\\)/profile/index.tsx` ≥ 2 (logout button + Alert title)
    - `grep -c "getProfileStatsApi\|getStreakApi" mobile/src/app/\\(tabs\\)/profile/index.tsx` ≥ 2
    - `grep -c "AchievementBadgesRow\|ProfileMenuCard\|ProfileMenuRow" mobile/src/app/\\(tabs\\)/profile/index.tsx` ≥ 3
    - `grep -c "router.push\\('/(tabs)/profile/edit'\\)\|router.push\\('/(tabs)/profile/notifications'\\)\|router.push\\('/(tabs)/profile/help'\\)" mobile/src/app/\\(tabs\\)/profile/index.tsx` ≥ 3
    - `grep -c "updateProfileApi" mobile/src/app/\\(tabs\\)/profile/edit.tsx` ≥ 1
    - `grep -c "Lưu thay đổi\|Hồ sơ đã được cập nhật" mobile/src/app/\\(tabs\\)/profile/edit.tsx` ≥ 2
    - `grep -c "Giảm cân\|Duy trì\|Tăng cân" mobile/src/app/\\(tabs\\)/profile/edit.tsx` ≥ 3
    - `grep -c "queryKey: \\['water', 'today'\\]" mobile/src/app/\\(tabs\\)/profile/edit.tsx` ≥ 1 (invalidates water cache so waterGoal change propagates to water.tsx — WARNING 4 chain)
    - `grep -c "getProfileStatsApi" mobile/src/app/\\(tabs\\)/profile/notifications.tsx` ≥ 1 (WARNING 3 fix — form initialises from server)
    - `grep -c "useQuery" mobile/src/app/\\(tabs\\)/profile/notifications.tsx` ≥ 1 (WARNING 3 fix — uses TanStack query for init)
    - `grep -c "updateNotificationsApi\|NotificationRationaleModal\|NotifToggleRow\|NotifTimeRow" mobile/src/app/\\(tabs\\)/profile/notifications.tsx` ≥ 4
    - `grep -c "getNotifAsked\|setNotifAsked" mobile/src/app/\\(tabs\\)/profile/notifications.tsx` ≥ 2
    - `grep -c "Streak alert sẽ tự động gửi" mobile/src/app/\\(tabs\\)/profile/notifications.tsx` ≥ 1
    - `grep -c "FAQItem\|support@u-app.vn\|Câu hỏi thường gặp" mobile/src/app/\\(tabs\\)/profile/help.tsx` ≥ 3
    - `wc -l mobile/src/app/\\(tabs\\)/profile/index.tsx` outputs ≥ 90
    - `wc -l mobile/src/app/\\(tabs\\)/profile/edit.tsx` outputs ≥ 80
    - `wc -l mobile/src/app/\\(tabs\\)/profile/notifications.tsx` outputs ≥ 80
    - `wc -l mobile/src/app/\\(tabs\\)/profile/help.tsx` outputs ≥ 60
  </acceptance_criteria>
  <done>
    Profile tab renders avatar + stats + badges + menu + logout. Edit screen has form + PATCH on save with cache invalidation (including ['water','today'] for WARNING 4 chain). Notifications screen INITIALISES form state from getProfileStatsApi().notifications (WARNING 3 fix — saved settings preserved across screen opens) + 2 toggles + 2 conditional time rows + debounced PATCH + rationale modal trigger (once via MMKV flag). Help screen has 4 FAQ + email + version. TypeScript compiles. Styling uses StyleSheet.create per WARNING 5 / validation_note.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 5 device verification (NOTIF-01/02/03/04 manual flows + WARNING 3 settings persistence)</name>
  <files>none (device verification — no source files modified)</files>
  <what-built>
    All Phase 5 mobile + backend work complete. Manual verification required per VALIDATION.md for the 4 NOTIF flows + the visual UI states that cannot be automated. This checkpoint is the **phase-level behavioral verification gate** that compensates for mobile plans 05-07 using TypeScript-only automated verification (see validation_note for rationale).
  </what-built>
  <action>
    Execute the manual verification flow in how-to-verify. This task pauses execution until the human operator types 'approved' or describes issues. Do NOT auto-advance — workflow.auto_advance is ignored for blocking-human gates.
  </action>
  <how-to-verify>
    1. Build a development build (not Expo Go — push notifications require a dev build per SDK 54): cd mobile && npx expo prebuild then eas build --profile development --platform android (or iOS).
    2. Install dev build on device, log in.
    3. Verify Home Dashboard renders: greeting with your name, 3 today cards (kcal/water/workout 0 initially), green Ủ Shop banner with white 'Ủ Shop' text, 3 quick action buttons, BMI widget (or empty state if no BMI yet), 4 macro progress bars all at 0% with 'Chưa có bữa ăn nào hôm nay'.
    4. Tap Ủ Shop banner — system browser opens https://u-app.vn/shop (or test URL).
    5. Tap water summary card — Water Log screen pushes. Verify count/goal display reads correctly (e.g., "0 / 8"). Tap + several times — count increments, today list shows 'Ly 1', 'Ly 2', 'Ly 3' with HH:MM. Tap - — most recent log removed. Pop back to Home — water card shows updated count.
    6. **(WARNING 4 verification)** From Profile -> Edit Hồ sơ, change waterGoal from 8 to 12 and save. Navigate back to Home, tap water card. Water screen should now show "X / 12" (not "X / 8"). This proves the Edit Profile cache invalidation chain reaches water.tsx and that water.tsx reads waterGoal from its own /api/water/today response, not from a stale /api/home/today-summary call.
    7. Tap Quét bữa ăn / Bắt đầu tập / Thói quen — each navigates to the existing tab/route.
    8. Switch to Profile tab (5th tab, person icon 'Hồ sơ'). Verify avatar (initial letter if no avatar), name, email. Stats row shows streakDays/totalWorkouts/totalKcalBurned (probably 0 / 0 / 0 for fresh user). Achievement badges all gray. Menu rows tap to push.
    9. Tap Chỉnh sửa hồ sơ — form loads with current name. Change height to 175, save. Alert 'Thành công' then router.back. Verify next time you open BMI tab the slider reflects 175.
    10. Tap Cài đặt thông báo — rationale modal appears (first time only). Tap 'Bật thông báo' — OS permission prompt appears. Grant. Modal closes. Reopen Notifications screen — modal NOT shown again (MMKV flag).
    11. **(WARNING 3 verification)** On the Notification Settings screen, change waterReminderTime to '11:30'. Wait 1 second for debounced PATCH. Navigate BACK to Profile, then back IN to Cài đặt thông báo. Verify the screen shows waterReminderTime = '11:30' (not '08:00' default). This proves notifications.tsx initialises form state from getProfileStatsApi().notifications and does NOT overwrite saved settings with hardcoded defaults.
    12. Toggle water reminder OFF then ON — observe instant network call (Network tab or backend logs).
    13. Tap time row, change waterReminderTime to 2 minutes from now. Save. Wait — FCM notification should arrive at that minute (NOTIF-02 verify).
    14. Set workoutReminderTime similarly to 2 min from now (NOTIF-03).
    15. Set up a user with streak > 0 and < 3 habits today, wait until 20:00 UTC+7 — verify streak alert FCM arrives (NOTIF-04).
    16. Help screen: tap FAQ item, accordion expands. Tap email — clipboard copy with feedback.
    17. Logout from Profile — Alert confirm, then redirects to login.

    Expected results:
    - Home dashboard greeting, 3 cards, banner, actions, BMI, nutrition all render correctly
    - Water log +/- works, deletes most-recent on minus
    - **(WARNING 4)** waterGoal updates from Edit Profile propagate to water.tsx after navigation/refetch
    - Profile tab shows correct user info + stats
    - Edit profile PATCH works, cache invalidation propagates to Home + Water
    - Notification rationale shown ONCE
    - **(WARNING 3)** Saved notification times persist across screen close/open cycles
    - FCM water/workout reminders arrive at configured time (NOTIF-02, NOTIF-03)
    - Streak alert arrives at 20:00 if applicable (NOTIF-04)
    - No crashes, no Vietnamese typos, no missing icons
  </how-to-verify>
  <verify>
    <human-check>Manual device verification per the 17 steps in how-to-verify. Operator confirms by typing 'approved'.</human-check>
  </verify>
  <done>
    Operator has run the dev build on a real device, executed all 17 verification steps (including the new WARNING 3 settings persistence check and WARNING 4 waterGoal propagation check), and confirmed all expected results pass. NOTIF-01/02/03/04 flows verified end-to-end on device.
  </done>
  <resume-signal>Type 'approved' to mark Phase 5 verification complete, or describe issues for revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile UI -> backend PATCH /api/users/profile | name/height/weight/goalType/waterGoal cross trust boundary; backend Zod whitelist enforces |
| mobile UI -> backend PATCH /api/users/notifications | toggle/time values cross trust boundary; HH:MM regex enforced |
| Expo OS -> mobile (permission dialog) | OS-controlled; mobile only requests, never inspects user denial state for spying |
| MMKV (device) -> NOTIF_ASKED flag | local-only; not synced to server; resetting flag requires app data wipe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | Edit Profile field bypass | mitigate | Backend Zod .strict() schema rejects unknown fields (Plan 03 Test 7). Mobile sends only whitelisted keys via updateProfileApi typed param. |
| T-05-07-02 | Tampering | Notification time injection | mitigate | Backend HH:MM regex enforced (Plan 03 Test 8). Mobile builds the string via formatTimeString helper from DateTimePicker — always produces valid HH:MM. |
| T-05-07-03 | Information Disclosure | Logout leaves cached user | mitigate | auth.logout() clears MMKV cached user, Zustand auth store, secure-store refresh token (existing AuthProvider behavior). Profile tab is gated behind isAuthenticated. |
| T-05-07-04 | Spoofing | FCM token replay | mitigate | POST /api/notifications/register-token now authenticated (Plan 04). Token bound to req.user.id from JWT. |
| T-05-07-05 | Repudiation | NOTIF rationale flag tampered | accept | MMKV flag is device-local; worst case is rationale shown again. No security boundary crossed. |
| T-05-07-06 | DoS | Notification time PATCH spam | mitigate | 800ms debounce on time changes (UI-SPEC) prevents PATCH flood while user drags time picker. |
| T-05-07-07 | Tampering | Settings overwrite on screen open (WARNING 3) | mitigate | notifications.tsx initialises form state via useEffect guarded by `=== null` — server values are seeded once on first arrival, never overwritten by stale/initial defaults. Verified by Task 3 step 11. |
</threat_model>

<verification>
- cd mobile && npx tsc --noEmit green
- Manual checkpoint (Task 3) covers all NOTIF-01/02/03/04 device flows + WARNING 3 settings persistence + WARNING 4 waterGoal propagation
- Profile tab logout replaces removed home logout (Plan 06 + Plan 07 ship in same wave per Pitfall 9)
</verification>

<success_criteria>
- Profile tab renders avatar, name, email, stats, 4 achievement badges, 3 menu rows, logout — all from real data
- Edit Profile form PATCHes /api/users/profile and invalidates home/users/water caches
- Notification Settings INITIALISES form state from getProfileStatsApi().notifications (WARNING 3 fix; saved settings preserved across screen opens) + 2 toggles + 2 conditional time rows + debounced PATCH + once-only rationale modal
- Rationale modal triggers expo-notifications full flow (requestPermissionsAsync + setNotificationChannelAsync Android + getDevicePushTokenAsync + registerTokenApi)
- Help screen has 4 FAQ accordion items + email + version
- 8 new UI components shipped using StyleSheet.create (WARNING 5 / validation_note)
- MMKV helper extended with getNotifAsked/setNotifAsked
- TypeScript compiles
- Manual checkpoint confirms NOTIF-01/02/03/04 device flows + WARNING 3 settings persistence + WARNING 4 waterGoal propagation
</success_criteria>

<output>
Create .planning/phases/05-home-dashboard/05-07-SUMMARY.md with:
- Profile tab section order + components used
- Edit Profile field list + PATCH body shape + cache invalidation list (home/summary, users/profile/stats, water/today)
- Notification Settings flow:
  - Form initialisation from getProfileStatsApi().notifications via useEffect+null-guard (WARNING 3 fix; replaces previous hardcoded defaults approach)
  - PATCH timing (immediate on toggle, 800ms debounce on time)
- Permission flow sequence diagram (rationale -> requestPermissions -> setChannel -> getToken -> registerToken)
- Styling decision: continued StyleSheet.create (WARNING 5 / validation_note)
- Plan 07 size note (WARNING 2): Plan completed without splitting; interim tsc validation after NotificationRationaleModal mitigated risk
- Manual checkpoint outcome notes (NOTIF-01/02/03/04 + WARNING 3 step 11 + WARNING 4 step 6)
</output>
</content>
</invoke>