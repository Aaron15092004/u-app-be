---
phase: 05-home-dashboard
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - backend/src/api/water/water.validation.ts
  - backend/src/api/water/water.service.ts
  - backend/src/api/water/water.controller.ts
  - backend/src/api/water/water.routes.ts
  - backend/src/api/home/home.service.ts
  - backend/src/api/home/home.controller.ts
  - backend/src/api/home/home.routes.ts
  - backend/src/api/config/config.controller.ts
  - backend/src/api/config/config.routes.ts
  - backend/src/app.ts
  - backend/.env.example
  - backend/src/api/water/water.integration.test.ts
  - backend/src/api/home/home.integration.test.ts
autonomous: true
requirements:
  - HOME-02
  - HOME-04
  - HOME-05
  - HOME-06

must_haves:
  truths:
    - "POST /api/water authenticated; creates a WaterLog for req.user.id; userId from JWT never body"
    - "GET /api/water/today authenticated; returns { logs, count, waterGoal } for today (UTC+7) for the authenticated user — waterGoal embedded so mobile water screen does not need a second roundtrip to /api/home/today-summary (WARNING 4 fix)"
    - "DELETE /api/water/:id authenticated; IDOR-safe via deleteOne({ _id, userId })"
    - "GET /api/home/today-summary authenticated; returns kcalConsumed, macros, waterGlasses, waterGoal, workoutMinutes, bmi (or null) aggregated for today UTC+7"
    - "D-82: GET /api/config/shop-url returns { url: SHOP_URL env var or default 'https://u-app.vn/shop' }; URL never sourced from request body or query"
    - "All endpoints mounted in app.ts: /api/water, /api/home, /api/config — Plan 03's /api/users mount is intentionally deferred to Wave 2 (per checker BLOCKER 1) to avoid concurrent app.ts edits"
    - "Integration tests pass: cross-user IDOR for water delete (userB token cannot delete userA log), today-summary aggregates from FoodLog + WaterLog + WorkoutLog + BMIRecord, water/today returns user-specific waterGoal (default 8, or PATCHed value if set), shop-url returns env var value"
  artifacts:
    - path: "backend/src/api/water/water.routes.ts"
      provides: "Water log CRUD endpoints"
      exports: ["default Router"]
    - path: "backend/src/api/water/water.service.ts"
      provides: "WaterLog business logic (count, create, delete); getTodayWater returns waterGoal from User.profile (WARNING 4 fix)"
      exports: ["logWater", "getTodayWater", "deleteWaterLog"]
    - path: "backend/src/api/home/home.service.ts"
      provides: "Multi-collection aggregation for dashboard"
      exports: ["getTodaySummary"]
    - path: "backend/src/api/config/config.routes.ts"
      provides: "Shop URL config endpoint"
      exports: ["default Router"]
  key_links:
    - from: "backend/src/api/home/home.service.ts"
      to: "WaterLog + FoodLog + WorkoutLog + BMIRecord + User"
      via: "imports + aggregate"
      pattern: "vietnamDayStart"
    - from: "backend/src/app.ts"
      to: "three new routers (water, home, config) — NOT users (Plan 03 owns that mount)"
      via: "app.use mount"
      pattern: "app.use\\('/api/(home|water|config)'"
    - from: "backend/src/api/water/water.service.ts"
      to: "WaterLog.deleteOne"
      via: "IDOR-safe filter"
      pattern: "deleteOne\\(\\{ _id.*userId"
    - from: "backend/src/api/water/water.service.ts"
      to: "User.profile.waterGoal"
      via: "getTodayWater embeds waterGoal in response"
      pattern: "waterGoal"
---

<validation_note>
**Backend plan — fully automated verification.** All behaviors verified via `npm run test:water` (9 tests) and `npm run test:home` (7 tests). No mobile concerns apply.

**Wave 1 file ownership:** This plan owns the Phase 5 `backend/src/app.ts` edits in Wave 1. Plan 03 (users API) was reordered to Wave 2 by the checker (BLOCKER 1) and will add `/api/users` after this plan ships. No concurrent app.ts writes.
</validation_note>

<objective>
Wave 1 (parallel with Plan 04 only — Plan 03 moved to Wave 2 per checker BLOCKER 1): Implement the three backend modules that serve the Home Dashboard. Water log CRUD (POST/GET/DELETE) per D-73, with `getTodayWater` returning `waterGoal` in its response so the mobile water screen has a single source of truth (WARNING 4 fix — eliminates an unnecessary second TanStack query against `/api/home/today-summary`). Home today-summary aggregation across FoodLog + WaterLog + WorkoutLog + BMIRecord + User per HOME-02/04/05. Shop URL config endpoint per HOME-06 + D-82. Mount three routers (water, home, config) in app.ts. Extend the Wave 0 home and water integration test scaffolds with real assertions.

Purpose: This plan delivers all the read-side and write-side data needed by the Home Dashboard mobile screen and Water Log mobile screen. Plan 04 (cron/FCM) runs in parallel and does not touch any of these files. Plan 03 (users API) was moved to Wave 2 by the checker because both this plan and Plan 03 originally edited `backend/src/app.ts` in Wave 1 with no atomic coordination.

Output: 11 new/modified backend files, integration tests covering HOME-02/05/06 and D-73 IDOR plus waterGoal embedding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-home-dashboard/05-CONTEXT.md
@.planning/phases/05-home-dashboard/05-RESEARCH.md
@.planning/phases/05-home-dashboard/05-PATTERNS.md
@.planning/phases/05-home-dashboard/05-VALIDATION.md
@.planning/phases/05-home-dashboard/05-01-SUMMARY.md
@backend/src/models/WaterLog.ts
@backend/src/models/User.ts
@backend/src/models/FoodLog.ts
@backend/src/models/WorkoutLog.ts
@backend/src/models/BMIRecord.ts
@backend/src/api/food/food.service.ts
@backend/src/api/food/food.controller.ts
@backend/src/api/food/food.routes.ts
@backend/src/api/food/food.validation.ts
@backend/src/api/workouts/workouts.service.ts
@backend/src/api/habits/habits.service.ts
@backend/src/api/bmi/bmi.controller.ts
@backend/src/api/health/health.routes.ts
@backend/src/middleware/auth.middleware.ts
@backend/src/utils/date.ts
@backend/src/utils/response.ts
@backend/src/app.ts
@backend/.env.example

<interfaces>
<!-- Existing date utility — use for ALL today-range queries -->
From backend/src/utils/date.ts:
  export function vietnamDayStart(input: Date | string): Date
  export function lastNDaysRange(n: number): { start: Date; end: Date }

<!-- Established today date-range pattern from food.service.ts -->
  const todayStart = vietnamDayStart(new Date());
  const tomorrowStart = new Date(todayStart.getTime() + 86400000);
  // Use { $gte: todayStart, $lt: tomorrowStart } — Pitfall 3 (RESEARCH.md)

<!-- FoodLog totals shape (for kcal + macros aggregation) -->
From backend/src/models/FoodLog.ts lines 20-25:
  totals: {
    calories: number;
    protein: number;
    carbs: number;
    fat: number;
  };
  // Indexed by { userId: 1, date: -1 }

<!-- WorkoutLog shape (for workout minutes) -->
From backend/src/models/WorkoutLog.ts lines 3-13:
  durationMinutes: number;
  caloriesBurned: number;
  date: Date;       // vietnamDayStart bucket — matches today via { date: todayStart }
  // Indexed by { userId: 1, date: -1 }

<!-- BMIRecord shape (for BMI widget) -->
From backend/src/models/BMIRecord.ts:
  bmi: number;
  category: 'underweight' | 'normal' | 'overweight' | 'obese';
  recordedAt: Date;
  // Indexed by { userId: 1, recordedAt: -1 }

<!-- WaterLog shape (created in Plan 01) -->
From backend/src/models/WaterLog.ts:
  userId: mongoose.Types.ObjectId;
  loggedAt: Date;
  // Indexed by { userId: 1, loggedAt: -1 } (non-unique)

<!-- User profile.waterGoal (migrated in Plan 01) -->
From backend/src/models/User.ts:
  profile.waterGoal: number;   // default 8

<!-- Response helpers -->
From backend/src/utils/response.ts:
  success<T>(res, data, statusCode=200): void  // { success: true, data }
  error(res, message, statusCode=500): void    // { success: false, error: message }

<!-- Auth pattern -->
From backend/src/middleware/auth.middleware.ts:
  authenticate(req, res, next)
  AuthRequest interface adds .user = { id: string; role: 'user'|'admin' }
  // Controllers must read: const userId = (req as AuthRequest).user.id;

<!-- IDOR-safe delete reference (food.service.ts deleteFoodLog lines 72-81) -->
  await FoodLog.deleteOne({
    _id: new mongoose.Types.ObjectId(logId),
    userId: new mongoose.Types.ObjectId(userId),
  });
  if (result.deletedCount === 0) throw makeError('Không tìm thấy bữa ăn', 404);

<!-- Existing food.routes.ts mount pattern (analog for water.routes.ts) -->
From backend/src/api/food/food.routes.ts:
  router.post('/scan', authenticate, uploadSingle, scanFood);
  router.post('/logs', authenticate, saveFoodLogHandler);
  router.get('/logs', authenticate, getFoodLogs);
  router.delete('/logs/:id', authenticate, deleteFoodLogHandler);

<!-- app.ts mount order (lines 18-26) -->
  app.use('/api/health', healthRouter);
  app.use('/api/upload/test', uploadTestRouter);
  app.use('/api/notifications', notificationRouter);
  app.use('/api/auth', authRouter);
  app.use('/api/exercises', exercisesRouter);
  app.use('/api/workouts', workoutsRouter);
  app.use('/api/habits', habitsRouter);
  app.use('/api/bmi', bmiRouter);
  app.use('/api/food', foodRouter);
  // ADD: water, home, config — before errorMiddleware
  // (Plan 03 will append /api/users in Wave 2 after this plan ships.)
  app.use(errorMiddleware);

<!-- Wave 0 test scaffolds (created in Plan 01) -->
backend/src/api/water/water.integration.test.ts — has MongoMemoryServer, beforeEach with userA/userB tokens, deleteMany(WaterLog). Extend with real tests.
backend/src/api/home/home.integration.test.ts — same scaffold; needs FoodLog/WorkoutLog/BMIRecord deletes in beforeEach.

<!-- ITodayWater response shape (updated for WARNING 4) -->
  // Was: { logs: IWaterLog[]; count: number }
  // Now: { logs: IWaterLog[]; count: number; waterGoal: number }
  // waterGoal sourced from User.profile.waterGoal with fallback to 8.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Water API (validation + service + controller + routes) with IDOR-safe delete and embedded waterGoal</name>
  <files>backend/src/api/water/water.validation.ts, backend/src/api/water/water.service.ts, backend/src/api/water/water.controller.ts, backend/src/api/water/water.routes.ts, backend/src/api/water/water.integration.test.ts</files>
  <read_first>
    - backend/src/api/food/food.service.ts (deleteFoodLog IDOR pattern lines 72-81; checkScanRateLimit countDocuments lines 22-33)
    - backend/src/api/food/food.controller.ts (handler patterns: validation, try/catch, success/error)
    - backend/src/api/food/food.routes.ts (route registration with authenticate)
    - backend/src/api/workouts/workouts.validation.ts (zod schema with .strict() and no userId field)
    - backend/src/models/WaterLog.ts (created in Plan 01)
    - backend/src/models/User.ts (post-migration — profile.waterGoal field)
    - backend/src/api/water/water.integration.test.ts (Wave 0 scaffold to extend)
    - .planning/phases/05-home-dashboard/05-PATTERNS.md (water.* sections)
  </read_first>
  <behavior>
    - Test 1: POST /api/water without Authorization header → 401
    - Test 2: POST /api/water with userA token (empty body) → 201 + creates 1 WaterLog with userId=userIdA and loggedAt set
    - Test 3: POST /api/water with explicit loggedAt body field → uses that timestamp; POST with `userId` in body → ignored (must come from JWT only)
    - Test 4: GET /api/water/today with userA token after 3 POSTs → 200 + { logs: [3 docs], count: 3, waterGoal: 8 }, logs sorted by loggedAt desc, waterGoal defaults to 8
    - Test 5: GET /api/water/today with userB token after userA POSTs → 200 + { logs: [], count: 0, waterGoal: 8 } (per-user scoping; userB still gets default waterGoal)
    - Test 6: DELETE /api/water/:id with userA token deleting userA's log → 200 + log gone
    - Test 7 (IDOR): DELETE /api/water/:id with userB token attempting to delete userA's log → 404; userA's log still present (verify with WaterLog.findById)
    - Test 8: Today range correctness — create a WaterLog with loggedAt = vietnamDayStart(today) - 1 second (yesterday), then GET /api/water/today returns count=0 for that user, waterGoal=8
    - Test 9 (waterGoal embedding — WARNING 4 fix): Update userA via `User.findByIdAndUpdate(userIdA, { 'profile.waterGoal': 12 })`. Then GET /api/water/today with userA token → response.data.waterGoal === 12 (not 8, not 10). This proves the mobile water screen receives the user's saved waterGoal in a single query.
  </behavior>
  <action>
    Create `backend/src/api/water/water.validation.ts`:
    - `logWaterSchema = z.object({ loggedAt: z.string().datetime().optional() }).strict()` — NO userId field. Add header comment "userId always sourced from JWT (req.user.id), never body — IDOR protection."

    Create `backend/src/api/water/water.service.ts`:
    - Import `mongoose`, `WaterLog` from '../../models/WaterLog', `User` from '../../models/User', `{ vietnamDayStart }` from '../../utils/date'.
    - `makeError(message, statusCode)` helper identical to food.service.ts lines 12-16.
    - `logWater(userId: string, loggedAt?: string): Promise<object>` — create WaterLog with `userId: new mongoose.Types.ObjectId(userId)`, `loggedAt: loggedAt ? new Date(loggedAt) : new Date()`. Return `.toObject()`.
    - `getTodayWater(userId: string): Promise<{ logs: object[]; count: number; waterGoal: number }>` (WARNING 4 — return type extended with `waterGoal`):
      - Compute todayStart/tomorrowStart with vietnamDayStart pattern.
      - Run two queries in parallel via Promise.all:
        1. `WaterLog.find({ userId: ObjectId, loggedAt: { $gte: todayStart, $lt: tomorrowStart } }).sort({ loggedAt: -1 }).lean()` → logs array.
        2. `User.findById(ObjectId(userId)).select('profile.waterGoal').lean()` → user doc.
      - Resolve `waterGoal = user?.profile?.waterGoal ?? 8` (fallback handles legacy users pre-Plan-01 migration AND missing User edge case).
      - Return `{ logs, count: logs.length, waterGoal }`.
      - **Why this matters (per checker WARNING 4):** mobile `water.tsx` previously had to fire a second TanStack query against `/api/home/today-summary` just to read `waterGoal`, which triggered the full multi-collection aggregation (FoodLog + WorkoutLog + BMIRecord + User). Embedding `waterGoal` in `getTodayWater` reduces that to a single User.findById on the water route. The home summary still independently exposes waterGoal — both routes are now self-sufficient.
    - `deleteWaterLog(userId: string, logId: string): Promise<void>` — `WaterLog.deleteOne({ _id: new mongoose.Types.ObjectId(logId), userId: new mongoose.Types.ObjectId(userId) })`. If `deletedCount === 0` throw `makeError('Không tìm thấy bản ghi nước', 404)`.

    Create `backend/src/api/water/water.controller.ts`:
    - Import `Request, Response` from 'express', `AuthRequest` from '../../middleware/auth.middleware', `{ success, error }` from '../../utils/response', `logWaterSchema` from './water.validation', service functions.
    - `logWater(req, res)`: extract userId from AuthRequest, parse body with safeParse, call service.logWater(userId, parseResult.data.loggedAt), success(res, doc, 201). try/catch same as food.controller.ts lines 49-66.
    - `getTodayWater(req, res)`: extract userId, call service.getTodayWater(userId), success(res, result). try/catch. (Returns the full { logs, count, waterGoal } shape.)
    - `deleteWater(req, res)`: extract userId, `const logId = String(req.params.id);` (matches food.controller.ts line 97), call service.deleteWaterLog(userId, logId), success(res, { deleted: true }). try/catch.

    Create `backend/src/api/water/water.routes.ts`:
    - `import { Router } from 'express'; import { authenticate } from '../../middleware/auth.middleware'; import * as waterController from './water.controller';`
    - `router.post('/', authenticate, waterController.logWater);`
    - `router.get('/today', authenticate, waterController.getTodayWater);`
    - `router.delete('/:id', authenticate, waterController.deleteWater);`
    - `export default router;`

    Extend `backend/src/api/water/water.integration.test.ts` (the Wave 0 scaffold): remove the placeholder smoke test and add the 9 tests listed in `<behavior>`. Use `request(app).post('/api/water').set('Authorization', \`Bearer ${tokenA}\`)`. For Test 7 IDOR, after the failed DELETE, assert `await WaterLog.findById(logId)` returns non-null. For Test 8 (yesterday), insert directly via `WaterLog.create({ userId: new mongoose.Types.ObjectId(userIdA), loggedAt: new Date(vietnamDayStart(new Date()).getTime() - 1000) })`. For Test 9, import User model and do `await User.findByIdAndUpdate(userIdA, { 'profile.waterGoal': 12 })` before the GET — also clean it back to 8 in afterEach or skip cleanup if MongoMemoryServer drops between tests.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run test:water</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm run test:water` exits 0 with all 9 tests passing
    - `grep -c "deleteOne({ _id" backend/src/api/water/water.service.ts` ≥ 1 AND same line contains `userId` (IDOR-safe filter)
    - `grep -c "userId" backend/src/api/water/water.validation.ts` outputs 0 (schema must NOT accept userId from body)
    - `grep -c "authenticate" backend/src/api/water/water.routes.ts` outputs 3 (all 3 routes authenticated)
    - `grep -c "(req as AuthRequest).user.id" backend/src/api/water/water.controller.ts` outputs 3 (all 3 handlers read userId from JWT)
    - `grep -c "\$gte.*todayStart.*\$lt.*tomorrowStart\|gte: todayStart" backend/src/api/water/water.service.ts` ≥ 1 (Pitfall 3 — upper bound on date range)
    - `grep -c "waterGoal" backend/src/api/water/water.service.ts` ≥ 2 (User.findById select + return field — WARNING 4 fix)
    - `grep -c "User.findById\|User\\.findById" backend/src/api/water/water.service.ts` ≥ 1 (single-doc lookup for waterGoal, not full multi-collection aggregate)
  </acceptance_criteria>
  <done>
    Water API has POST/GET/DELETE all under `authenticate`. UserId from JWT only. Delete is IDOR-safe via { _id, userId } filter. Today scope uses { $gte, $lt } UTC+7 range. GET /api/water/today returns user-specific waterGoal in a single query (no /api/home/today-summary call needed from mobile). All 9 integration tests pass, including the new Test 9 that proves waterGoal embedding.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Home today-summary + Config shop-url APIs and app.ts mounts (water/home/config only — NOT users)</name>
  <files>backend/src/api/home/home.service.ts, backend/src/api/home/home.controller.ts, backend/src/api/home/home.routes.ts, backend/src/api/config/config.controller.ts, backend/src/api/config/config.routes.ts, backend/src/app.ts, backend/.env.example, backend/src/api/home/home.integration.test.ts</files>
  <read_first>
    - backend/src/api/workouts/workouts.service.ts (multi-aggregate pattern with $group $sum)
    - backend/src/api/bmi/bmi.controller.ts (read-only GET handler shape)
    - backend/src/api/health/health.routes.ts (simple route module)
    - backend/src/app.ts (lines 1-30 — import + mount order)
    - backend/.env.example (lines 1-42 — existing env var pattern)
    - backend/src/api/home/home.integration.test.ts (Wave 0 scaffold to extend)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Pattern 5 — Home Today-Summary Endpoint)
  </read_first>
  <behavior>
    - Test 1: GET /api/home/today-summary without auth → 401
    - Test 2: GET /api/home/today-summary with userA token (no data) → 200 + { kcalConsumed: 0, macros: { protein: 0, carbs: 0, fat: 0 }, waterGlasses: 0, waterGoal: 8, workoutMinutes: 0, bmi: null }
    - Test 3: Seed: 1 FoodLog (totals { calories: 500, protein: 30, carbs: 50, fat: 20 } with date=today UTC+7), 2 WaterLog (today), 1 WorkoutLog (durationMinutes=30, date=vietnamDayStart(today)), 1 BMIRecord (bmi=22.5, category='normal'). Updated User.profile.waterGoal=10. GET → kcalConsumed=500, macros={30,50,20}, waterGlasses=2, waterGoal=10, workoutMinutes=30, bmi={ value: 22.5, category: 'normal' }
    - Test 4 (per-user scoping): userB token after userA's seed data → all zeros, bmi=null
    - Test 5 (UTC+7 boundary): WaterLog from yesterday (loggedAt = vietnamDayStart(today) - 1ms) → waterGlasses=0
    - Test 6 (BMI latest): two BMIRecords (one 7 days ago, one today) → returns today's bmi
    - Test 7 (config): GET /api/config/shop-url → 200 + { url: process.env.SHOP_URL || 'https://u-app.vn/shop' }. Use `process.env.SHOP_URL = 'https://test-shop.example.com'` inside the test and assert url matches.
  </behavior>
  <action>
    Create `backend/src/api/home/home.service.ts`:
    - Imports: `mongoose`, `FoodLog`, `WaterLog`, `WorkoutLog`, `BMIRecord`, `User` from '../../models/*', `{ vietnamDayStart }` from '../../utils/date'.
    - Export `getTodaySummary(userId: string)` returning shape `{ kcalConsumed: number; macros: { protein: number; carbs: number; fat: number }; waterGlasses: number; waterGoal: number; workoutMinutes: number; bmi: { value: number; category: string } | null }`.
    - Compute `todayStart = vietnamDayStart(new Date())` and `tomorrowStart = new Date(todayStart.getTime() + 86400000)` and `userObjId = new mongoose.Types.ObjectId(userId)`.
    - Run 5 queries (can be Promise.all for performance, but sequential is fine):
      1. `FoodLog.aggregate([{ $match: { userId: userObjId, date: { $gte: todayStart, $lt: tomorrowStart } } }, { $group: { _id: null, kcal: { $sum: '$totals.calories' }, protein: { $sum: '$totals.protein' }, carbs: { $sum: '$totals.carbs' }, fat: { $sum: '$totals.fat' } } }])` → take first element, default each field to 0.
      2. `WaterLog.countDocuments({ userId: userObjId, loggedAt: { $gte: todayStart, $lt: tomorrowStart } })`.
      3. `WorkoutLog.aggregate([{ $match: { userId: userObjId, date: todayStart } }, { $group: { _id: null, minutes: { $sum: '$durationMinutes' } } }])` → take first, default 0. (WorkoutLog stores `date` as the UTC+7 day bucket per Phase 3 pattern, so exact match on todayStart.)
      4. `BMIRecord.findOne({ userId: userObjId }).sort({ recordedAt: -1 }).select('bmi category').lean()` → null-safe map to `{ value, category }`.
      5. `User.findById(userObjId).select('profile.waterGoal').lean()` → `waterGoal = user?.profile?.waterGoal ?? 8`.
    - Return the fully built summary object.

    Create `backend/src/api/home/home.controller.ts`:
    - Read-only GET handler mirroring `bmi.controller.ts getHistory` (lines 28-38). Function `getTodaySummary(req, res)`: extract userId from AuthRequest, call service.getTodaySummary(userId), success(res, summary). try/catch with typed error.

    Create `backend/src/api/home/home.routes.ts`:
    - `router.get('/today-summary', authenticate, homeController.getTodaySummary);`

    Create `backend/src/api/config/config.controller.ts`:
    - Single function `getShopUrl(req, res)`: `const url = process.env.SHOP_URL || 'https://u-app.vn/shop'; success(res, { url });`. No service layer (trivial). Do NOT read any value from req.body, req.query, or req.params — URL is server-side only per D-82 + threat model.

    Create `backend/src/api/config/config.routes.ts`:
    - `router.get('/shop-url', configController.getShopUrl);` — NO authenticate (shop URL is public per RESEARCH.md "Open Question 1"). Add header comment "Public endpoint — URL is non-sensitive."

    Modify `backend/src/app.ts` (Plan 02 owns the Wave 1 mount edit — Plan 03 will append /api/users in Wave 2):
    - Add 3 imports after the food router import (line 11): `import homeRouter from './api/home/home.routes';`, `import waterRouter from './api/water/water.routes';`, `import configRouter from './api/config/config.routes';`.
    - Add 3 mounts AFTER `app.use('/api/food', foodRouter);` and BEFORE `app.use(errorMiddleware);`:
      - `app.use('/api/water', waterRouter);`
      - `app.use('/api/home', homeRouter);`
      - `app.use('/api/config', configRouter);`
    - Do NOT add `/api/users` here — that mount is owned by Plan 03 in Wave 2 (BLOCKER 1 serialization).

    Modify `backend/.env.example`:
    - Add a new section `# Shop URL (Phase 5)` after the `# CORS` block, with `SHOP_URL=https://u-app.vn/shop`.

    Extend `backend/src/api/home/home.integration.test.ts`:
    - Add deleteMany calls in beforeEach for FoodLog, WaterLog, WorkoutLog, BMIRecord (import these models at top).
    - Remove the placeholder smoke test.
    - Implement the 7 tests in `<behavior>`.
    - For Test 7, save and restore `process.env.SHOP_URL` in before/after hooks at test scope, or use a dedicated `test('GET /api/config/shop-url ...', async () => { ... })` that sets `process.env.SHOP_URL = 'https://test-shop.example.com'` and asserts on the returned url. Restore the env var afterward.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run test:home &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm run test:home` exits 0 with all 7 tests passing
    - `cd backend && npx tsc --noEmit` exits 0
    - `grep -c "vietnamDayStart" backend/src/api/home/home.service.ts` ≥ 1 (UTC+7 boundary used)
    - `grep -c "process.env.SHOP_URL" backend/src/api/config/config.controller.ts` ≥ 1
    - `grep -c "req.body\|req.query\|req.params" backend/src/api/config/config.controller.ts` outputs 0 (shop URL never sourced from request)
    - `grep -c "app.use('/api/water'" backend/src/app.ts` outputs 1
    - `grep -c "app.use('/api/home'" backend/src/app.ts` outputs 1
    - `grep -c "app.use('/api/config'" backend/src/app.ts` outputs 1
    - `grep -c "app.use('/api/users'" backend/src/app.ts` outputs 0 (Plan 03 will add this in Wave 2; this plan must NOT add it)
    - `grep -c "SHOP_URL" backend/.env.example` outputs 1
    - `grep -c "authenticate" backend/src/api/home/home.routes.ts` outputs 1 (today-summary is authenticated)
  </acceptance_criteria>
  <done>
    Home today-summary aggregates kcal/macros/water/workout/bmi/waterGoal correctly per UTC+7 day. Config shop-url returns env var (or default) and never reads user input. Three routes mounted in app.ts (water/home/config — NOT users; that mount belongs to Plan 03 in Wave 2). .env.example documents SHOP_URL. All 7 home integration tests + 9 water integration tests pass; TypeScript compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (mobile) → backend API | All Phase 5 endpoints receive JWT-authenticated requests; userId must come from token |
| process.env (server config) → response body | Shop URL crosses server config → public response; must NOT accept user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering / IDOR | DELETE /api/water/:id | mitigate | service.deleteWaterLog uses `WaterLog.deleteOne({ _id: ObjectId(logId), userId: ObjectId(userId) })` — same pattern as food.service.ts. Asserted by Test 7. |
| T-05-02-02 | Tampering | POST /api/water body | mitigate | Zod schema `.strict()` rejects extra fields; controller reads userId from `(req as AuthRequest).user.id` not body. Asserted by Test 3. |
| T-05-02-03 | Information Disclosure | GET /api/home/today-summary | mitigate | All 5 aggregation queries filter by `userId: ObjectId(req.user.id)`. Cross-user scope asserted by Test 4. |
| T-05-02-04 | Tampering | GET /api/config/shop-url open redirect | mitigate | URL exclusively from `process.env.SHOP_URL`; controller never reads `req.body`/`req.query`/`req.params`. Grep gate enforces zero occurrences. |
| T-05-02-05 | Spoofing | Unauthenticated endpoints | mitigate | All water + home routes registered with `authenticate` middleware. Tested via Test 1 (401 without auth). |
| T-05-02-06 | Information Disclosure | Stale BMIRecord cross-user | accept | BMIRecord index `{ userId: 1, recordedAt: -1 }` already exists; findOne scoped by userId. Latest record query is correct per Test 6. |
| T-05-02-07 | Information Disclosure | waterGoal leak to other users | mitigate | getTodayWater's `User.findById` is keyed on `req.user.id` from JWT; no userId from body/query/params. Asserted indirectly by Test 5 (userB receives default 8, not userA's 10) and Test 9. |
</threat_model>

<verification>
- `cd backend && npm run test:water && npm run test:home && npx tsc --noEmit` all green
- All 16 integration tests across water + home pass (9 water + 7 home)
- IDOR test (Test 7 water) confirms userB cannot delete userA log
- waterGoal embedding (Test 9 water) confirms single-query water screen pattern
- All home aggregation queries scoped to req.user.id
- app.ts mounts water/home/config but NOT users (Plan 03 owns that)
</verification>

<success_criteria>
- POST /api/water authenticated, creates WaterLog with userId from JWT; loggedAt optional in body
- GET /api/water/today authenticated, returns { logs, count, waterGoal } scoped to today UTC+7; waterGoal from User.profile (WARNING 4 fix)
- DELETE /api/water/:id authenticated, IDOR-safe { _id, userId } filter
- GET /api/home/today-summary authenticated, returns the full ITodaySummary shape with FoodLog macros + waterCount + waterGoal + workout minutes + latest BMI (or null)
- GET /api/config/shop-url returns { url: SHOP_URL env or default }; never reads request input
- Three routers (water, home, config) mounted in app.ts before errorMiddleware; users mount intentionally deferred to Plan 03 / Wave 2
- .env.example documents SHOP_URL
- 16 integration tests pass; TypeScript compiles
</success_criteria>

<output>
Create `.planning/phases/05-home-dashboard/05-02-SUMMARY.md` when done with:
- ITodaySummary response shape (TypeScript type)
- ITodayWater response shape — updated to include `waterGoal: number` (WARNING 4 fix; mobile types in Plan 05 must reflect this)
- IWaterLog response shape
- List of 5 endpoints created and their auth status
- app.ts current state (water/home/config mounted; users mount deferred to Plan 03 / Wave 2)
- Key decisions logged (e.g., why config is public; aggregation done sequentially vs Promise.all; why waterGoal embedded in /api/water/today response)
- Tests added per test file (9 water + 7 home)
- Note for Plan 03 executor: when adding `app.use('/api/users', usersRouter);` in Wave 2, place it AFTER `app.use('/api/config', configRouter);` and BEFORE `app.use(errorMiddleware);`
- Note for Plan 05/06 executors: ITodayWater now includes waterGoal — mobile types and water screen must use it directly (no separate /api/home/today-summary query needed for waterGoal in water.tsx)
</output>
</content>
</invoke>