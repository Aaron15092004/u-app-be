---
phase: 05-home-dashboard
plan: 04
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - backend/src/services/fcm.service.ts
  - backend/src/cron/scheduler.ts
  - backend/src/api/notifications/notification.routes.ts
  - backend/src/api/notifications/notification.controller.ts
  - backend/src/api/notifications/notification.service.ts
  - backend/src/server.ts
autonomous: true
requirements:
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "fcm.service.ts exports sendBatchNotificationToUsers(userIds, notification) that resolves DeviceTokens via single $in query and sends in parallel with Promise.allSettled"
    - "FCM notification bodies for water/workout/streak alerts are generic strings — contain NO PII, NO health metrics, NO user-identifying data (per RESEARCH.md security domain)"
    - "D-81: backend/src/cron/scheduler.ts exports startScheduler() function which registers TWO node-cron jobs: per-minute reminder dispatcher and daily 20:00 streak alert"
    - "Per-minute job computes HH:MM in UTC+7 via manual offset math (now.getTime() + 7*60*60*1000), never via server local time (Pitfall 2)"
    - "Streak alert cron expression is '0 20 * * *' with timezone 'Asia/Ho_Chi_Minh' (D-80)"
    - "Streak alert logic: query users with streak > 0 AND today's distinct HabitLog count < 3 → send FCM"
    - "server.ts calls startScheduler() AFTER connectDB() AND loadFirebase() (Pitfall 1)"
    - "POST /api/notifications/register-token now requires authenticate middleware (RESEARCH.md Open Question 1 resolved)"
    - "register-token controller reads userId from JWT (req.user.id), not from request body"
  artifacts:
    - path: "backend/src/services/fcm.service.ts"
      provides: "sendBatchNotificationToUsers added; existing sendNotificationToUser + registerDeviceToken unchanged"
      exports: ["sendNotificationToUser", "sendBatchNotificationToUsers", "registerDeviceToken"]
    - path: "backend/src/cron/scheduler.ts"
      provides: "node-cron job bootstrap"
      exports: ["startScheduler"]
    - path: "backend/src/server.ts"
      provides: "Scheduler bootstrap after DB + Firebase"
      contains: "startScheduler"
  key_links:
    - from: "backend/src/server.ts"
      to: "startScheduler"
      via: "import + invocation"
      pattern: "startScheduler\\(\\)"
    - from: "backend/src/cron/scheduler.ts"
      to: "fcm.service.sendBatchNotificationToUsers"
      via: "import + call"
      pattern: "sendBatchNotificationToUsers"
    - from: "backend/src/api/notifications/notification.routes.ts"
      to: "authenticate middleware"
      via: "route registration"
      pattern: "authenticate"
---

<objective>
Wave 1 (parallel): Add server-side FCM scheduling per D-80/D-81. Extend `fcm.service.ts` with a batch send method. Create `backend/src/cron/scheduler.ts` with two node-cron jobs (per-minute reminder dispatcher and daily 20:00 streak alert). Bootstrap the scheduler from `server.ts` after DB + Firebase loaders. Add `authenticate` middleware to the existing `/api/notifications/register-token` endpoint and rewire its controller to use req.user.id from JWT.

Purpose: This plan delivers NOTIF-02 (water reminder), NOTIF-03 (workout reminder), and NOTIF-04 (streak alert) backend infrastructure. Runs in parallel with Plan 02 (water/home/config) and Plan 03 (users API) — different files. Mobile Plan 07 will trigger token registration; the cron jobs run unprompted on the server.

Output: 3 backend service files (1 new), 1 server bootstrap update, notification routes hardened with auth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-home-dashboard/05-CONTEXT.md
@.planning/phases/05-home-dashboard/05-RESEARCH.md
@.planning/phases/05-home-dashboard/05-PATTERNS.md
@.planning/phases/05-home-dashboard/05-UI-SPEC.md
@.planning/phases/05-home-dashboard/05-01-SUMMARY.md
@backend/src/services/fcm.service.ts
@backend/src/models/DeviceToken.ts
@backend/src/models/User.ts
@backend/src/models/HabitLog.ts
@backend/src/api/habits/habits.service.ts
@backend/src/api/notifications/notification.routes.ts
@backend/src/api/notifications/notification.controller.ts
@backend/src/server.ts
@backend/src/middleware/auth.middleware.ts
@backend/src/utils/date.ts
@backend/src/utils/response.ts

<interfaces>
<!-- Existing FCM service (lines 1-36) — DO NOT replace, only ADD -->
From backend/src/services/fcm.service.ts:
  import admin from 'firebase-admin';
  import DeviceToken from '../models/DeviceToken';

  export async function sendNotificationToUser(userId: string, notification: {...}): Promise<void>
  export async function registerDeviceToken(userId: string, token: string, platform: 'ios'|'android'): Promise<void>

  // ADD: export async function sendBatchNotificationToUsers(userIds: string[], notification: { title: string; body: string; data?: Record<string,string> }): Promise<void>

<!-- DeviceToken model -->
From backend/src/models/DeviceToken.ts:
  { userId: ObjectId, token: string (unique), platform: 'ios'|'android', updatedAt }
  // Index: { userId: 1 }

<!-- Existing notification.controller.ts has TODO comment to add auth -->
From backend/src/api/notifications/notification.controller.ts (lines 1-22):
  // TODO Phase 2: replace 'phase1-test-user' with req.user.id from JWT middleware
  await notificationService.saveDeviceToken('phase1-test-user', token, platform);
  // ↑ MUST CHANGE: read userId from (req as AuthRequest).user.id

<!-- Existing notification.routes.ts (lines 1-10) -->
From backend/src/api/notifications/notification.routes.ts:
  // TODO Phase 2: add JWT auth middleware to this route
  router.post('/register-token', notificationController.registerToken);
  // ↑ MUST CHANGE: router.post('/register-token', authenticate, notificationController.registerToken);

<!-- getStreak from habits.service.ts (delegation target) -->
  export async function getStreak(userId: string): Promise<{ streakDays: number }>

<!-- HabitLog count today pattern (existing habits.service.ts) -->
From backend/src/api/habits/habits.service.ts lines 25-32:
  HabitLog.find({ userId: ObjectId, date: vietnamDayStart(new Date()) }).select('habitId').lean()
  // Returns array; distinct habit count = result.length (because of unique compound index)

<!-- server.ts current bootstrap (lines 1-25) -->
From backend/src/server.ts:
  await connectDB();
  await loadFirebase();
  app.listen(config.PORT, ...);
  // INSERT: after loadFirebase() and before app.listen(): startScheduler();

<!-- node-cron API (RESEARCH.md Pattern 1) -->
  import cron from 'node-cron';
  cron.schedule('* * * * *', async () => {...}, { timezone: 'Asia/Ho_Chi_Minh' });
  cron.schedule('0 20 * * *', async () => {...}, { timezone: 'Asia/Ho_Chi_Minh' });

<!-- UTC+7 manual offset math (Pitfall 2 — DO NOT use server local time) -->
  const now = new Date();
  const utc7 = new Date(now.getTime() + 7 * 60 * 60 * 1000);
  const hh = String(utc7.getUTCHours()).padStart(2, '0');
  const mm = String(utc7.getUTCMinutes()).padStart(2, '0');
  const currentTime = `${hh}:${mm}`;

<!-- Notification copy from UI-SPEC.md Copywriting Contract — use these EXACT strings -->
  Water reminder:   title "Nhắc uống nước",     body "Đã đến giờ uống nước rồi! Hãy uống một ly nhé."
  Workout reminder: title "Nhắc tập luyện",     body "Đã đến giờ tập luyện. Bắt đầu ngay để giữ streak nhé!"
  Streak alert:     title "Sắp mất streak!",    body "Bạn sắp mất streak! Hoàn thành thói quen trước nửa đêm."
  // All bodies are GENERIC — no user name, no kcal, no health metrics (T-05-04-04)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend fcm.service with sendBatchNotificationToUsers and harden /api/notifications/register-token</name>
  <files>backend/src/services/fcm.service.ts, backend/src/api/notifications/notification.routes.ts, backend/src/api/notifications/notification.controller.ts, backend/src/api/notifications/notification.service.ts</files>
  <read_first>
    - backend/src/services/fcm.service.ts (lines 1-36 — existing exports must remain)
    - backend/src/models/DeviceToken.ts (schema + index)
    - backend/src/api/notifications/notification.routes.ts (TODO at line 4)
    - backend/src/api/notifications/notification.controller.ts (TODO at line 18; line 19 uses 'phase1-test-user' string)
    - backend/src/api/notifications/notification.service.ts (verify shape of saveDeviceToken)
    - backend/src/middleware/auth.middleware.ts (AuthRequest type)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Pattern 2 batch FCM send)
  </read_first>
  <action>
    Modify `backend/src/services/fcm.service.ts` — APPEND (do not replace existing exports):
    - Add `import mongoose from 'mongoose';` at top if not already present.
    - Add new exported function `sendBatchNotificationToUsers(userIds: string[], notification: { title: string; body: string; data?: Record<string, string> }): Promise<void>`:
      - Early return if `userIds.length === 0`.
      - Convert userIds to ObjectIds: `const objIds = userIds.map(id => new mongoose.Types.ObjectId(id));`
      - Single `$in` query: `const tokens = await DeviceToken.find({ userId: { $in: objIds } }).lean();`
      - Promise.allSettled over `tokens.map(t => admin.messaging().send({ notification: { title, body: notification.body }, data: notification.data, token: t.token }).catch(err => console.error(`FCM batch fail for token ${t.token}:`, err)))`.
      - Return void.
    - Do NOT modify `sendNotificationToUser` or `registerDeviceToken`.

    Modify `backend/src/api/notifications/notification.routes.ts`:
    - Add import: `import { authenticate } from '../../middleware/auth.middleware';`
    - Change `router.post('/register-token', notificationController.registerToken);` to `router.post('/register-token', authenticate, notificationController.registerToken);`
    - Remove the `// TODO Phase 2: add JWT auth middleware to this route` comment (replace with `// Authenticated per Phase 5 — RESEARCH.md Open Question 1`).

    Modify `backend/src/api/notifications/notification.controller.ts`:
    - Add import: `import { AuthRequest } from '../../middleware/auth.middleware';`
    - Replace `await notificationService.saveDeviceToken('phase1-test-user', token, platform);` with `const userId = (req as AuthRequest).user.id; await notificationService.saveDeviceToken(userId, token, platform);`
    - Remove the `// TODO Phase 2: replace 'phase1-test-user' with req.user.id from JWT middleware` comment.

    Verify `backend/src/api/notifications/notification.service.ts` `saveDeviceToken` signature accepts `(userId: string, token: string, platform: 'ios'|'android')`. If it currently uses a literal string userId or has a TODO, ensure it stores `new mongoose.Types.ObjectId(userId)` into DeviceToken (DeviceToken.userId is ObjectId-typed). If `notificationService.saveDeviceToken` is missing this cast, update it accordingly.

    Do NOT add any new endpoint to this file in this task.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx tsc --noEmit &amp;&amp; grep -c "sendBatchNotificationToUsers" backend/src/services/fcm.service.ts &amp;&amp; grep -c "authenticate" backend/src/api/notifications/notification.routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npx tsc --noEmit` exits 0
    - `grep -c "export async function sendBatchNotificationToUsers" backend/src/services/fcm.service.ts` outputs 1
    - `grep -c "Promise.allSettled" backend/src/services/fcm.service.ts` ≥ 1
    - `grep -c "\\$in" backend/src/services/fcm.service.ts` ≥ 1 (single-query token lookup)
    - `grep -c "export async function sendNotificationToUser\|export async function registerDeviceToken" backend/src/services/fcm.service.ts` outputs 2 (existing exports preserved)
    - `grep -c "authenticate" backend/src/api/notifications/notification.routes.ts` outputs 1
    - `grep -c "phase1-test-user" backend/src/api/notifications/notification.controller.ts` outputs 0 (placeholder removed)
    - `grep -c "(req as AuthRequest).user.id" backend/src/api/notifications/notification.controller.ts` ≥ 1
    - Existing tests still pass: `cd backend && npm run test:auth` exits 0 (auth unrelated, sanity check)
  </acceptance_criteria>
  <done>
    fcm.service.ts has 3 exports (sendNotificationToUser, sendBatchNotificationToUsers, registerDeviceToken). /api/notifications/register-token requires authenticate; controller uses req.user.id. TypeScript compiles.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create cron/scheduler.ts with reminder dispatcher + streak alert, bootstrap from server.ts</name>
  <files>backend/src/cron/scheduler.ts, backend/src/server.ts</files>
  <read_first>
    - backend/src/services/fcm.service.ts (sendBatchNotificationToUsers — added in Task 1)
    - backend/src/models/User.ts (post-Plan 01 schema — waterReminderTime, workoutReminderTime, waterReminder, workoutReminder)
    - backend/src/models/HabitLog.ts (date field uses vietnamDayStart bucket)
    - backend/src/api/habits/habits.service.ts (getStreak lines 78-121)
    - backend/src/utils/date.ts (vietnamDayStart)
    - backend/src/server.ts (current bootstrap)
    - .planning/phases/05-home-dashboard/05-RESEARCH.md (Pattern 1 + Pitfalls 1, 2)
    - .planning/phases/05-home-dashboard/05-UI-SPEC.md (Copywriting Contract — notification titles + bodies)
  </read_first>
  <action>
    Create `backend/src/cron/scheduler.ts`:
    - Imports: `import cron from 'node-cron';`, `import mongoose from 'mongoose';`, `import User from '../models/User';`, `import HabitLog from '../models/HabitLog';`, `import { sendBatchNotificationToUsers } from '../services/fcm.service';`, `import { getStreak } from '../api/habits/habits.service';`, `import { vietnamDayStart } from '../utils/date';`.

    - Export `startScheduler(): void`:
      - Log `console.log('[scheduler] Starting cron jobs (Asia/Ho_Chi_Minh)');`
      - Register per-minute job:
        ```
        cron.schedule('* * * * *', async () => {
          try {
            const now = new Date();
            const utc7 = new Date(now.getTime() + 7 * 60 * 60 * 1000);
            const hh = String(utc7.getUTCHours()).padStart(2, '0');
            const mm = String(utc7.getUTCMinutes()).padStart(2, '0');
            const currentTime = `${hh}:${mm}`;
            await dispatchWaterReminders(currentTime);
            await dispatchWorkoutReminders(currentTime);
          } catch (err) {
            console.error('[scheduler] per-minute job error:', err);
          }
        }, { timezone: 'Asia/Ho_Chi_Minh' });
        ```
        Reminder: the per-minute job uses MANUAL UTC+7 offset math (Pitfall 2) — do NOT rely on server local time. The `timezone` option is documented in node-cron v4 but the comparison string MUST be computed manually per RESEARCH.md.

      - Register daily 20:00 streak alert:
        ```
        cron.schedule('0 20 * * *', async () => {
          try { await dispatchStreakAlerts(); }
          catch (err) { console.error('[scheduler] streak alert error:', err); }
        }, { timezone: 'Asia/Ho_Chi_Minh' });
        ```

    - Module-private async function `dispatchWaterReminders(currentTime: string): Promise<void>`:
      - `const users = await User.find({ 'notifications.waterReminder': true, 'notifications.waterReminderTime': currentTime }).select('_id').lean();`
      - If `users.length === 0` return.
      - `const userIds = users.map(u => (u._id as mongoose.Types.ObjectId).toString());`
      - `await sendBatchNotificationToUsers(userIds, { title: 'Nhắc uống nước', body: 'Đã đến giờ uống nước rồi! Hãy uống một ly nhé.', data: { type: 'water_reminder' } });`

    - Module-private async function `dispatchWorkoutReminders(currentTime: string): Promise<void>`:
      - `const users = await User.find({ 'notifications.workoutReminder': true, 'notifications.workoutReminderTime': currentTime }).select('_id').lean();`
      - If empty return.
      - `await sendBatchNotificationToUsers(userIds, { title: 'Nhắc tập luyện', body: 'Đã đến giờ tập luyện. Bắt đầu ngay để giữ streak nhé!', data: { type: 'workout_reminder' } });`

    - Module-private async function `dispatchStreakAlerts(): Promise<void>`:
      - Fetch all users: `const users = await User.find({}).select('_id').lean();`
      - For each user (run sequentially or via Promise.all with reasonable concurrency — sequential is fine for v1):
        - `const { streakDays } = await getStreak(user._id.toString());`
        - If `streakDays === 0` skip.
        - Today's habit count: `const todayLogs = await HabitLog.find({ userId: user._id, date: vietnamDayStart(new Date()) }).select('habitId').lean();`
        - Distinct habit count = todayLogs.length (HabitLog has unique compound index per habit per day).
        - If `todayLogs.length < 3` → add `user._id.toString()` to the alert recipient list.
      - After loop, `await sendBatchNotificationToUsers(atRiskUserIds, { title: 'Sắp mất streak!', body: 'Bạn sắp mất streak! Hoàn thành thói quen trước nửa đêm.', data: { type: 'streak_alert' } });`

    - Do NOT include any user name, kcal value, BMI, or health metric in the notification body (T-05-04-04 PII).
    - All three dispatch functions: wrap in try/catch at the schedule callback level so a DB error never crashes the scheduler.

    Modify `backend/src/server.ts`:
    - Add import: `import { startScheduler } from './cron/scheduler';` (after the loadFirebase import line).
    - In `startServer()`, AFTER `await loadFirebase();` and BEFORE `app.listen(...)`, insert: `startScheduler();`
    - Critical: scheduler must NOT bootstrap at module import time (Pitfall 1) — only via `startScheduler()` call inside `startServer()` after DB is connected.

    Do NOT import `cron/scheduler.ts` from any other file.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx tsc --noEmit &amp;&amp; grep -c "startScheduler" backend/src/server.ts &amp;&amp; grep -c "'* * * * *'\\|'0 20 \* \* \*'" backend/src/cron/scheduler.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npx tsc --noEmit` exits 0
    - `grep -c "export function startScheduler" backend/src/cron/scheduler.ts` outputs 1
    - `grep -c "cron.schedule" backend/src/cron/scheduler.ts` outputs 2 (per-minute + daily 20:00)
    - `grep -c "'\\* \\* \\* \\* \\*'" backend/src/cron/scheduler.ts` outputs 1 (per-minute expression)
    - `grep -c "'0 20 \\* \\* \\*'" backend/src/cron/scheduler.ts` outputs 1 (daily 20:00 expression)
    - `grep -c "Asia/Ho_Chi_Minh" backend/src/cron/scheduler.ts` outputs 2 (both schedules tz-aware)
    - `grep -c "7 \\* 60 \\* 60 \\* 1000" backend/src/cron/scheduler.ts` ≥ 1 (manual UTC+7 offset math — Pitfall 2)
    - `grep -c "sendBatchNotificationToUsers" backend/src/cron/scheduler.ts` ≥ 3 (one call per dispatch function)
    - `grep -c "import { startScheduler" backend/src/server.ts` outputs 1
    - `grep -c "startScheduler()" backend/src/server.ts` outputs 1
    - server.ts has `startScheduler()` AFTER `loadFirebase()` line — verify by line number: `awk '/loadFirebase/{lf=NR} /startScheduler/{ss=NR} END { exit (ss > lf) ? 0 : 1 }' backend/src/server.ts`
    - Notification bodies match UI-SPEC exactly: `grep -c "Đã đến giờ uống nước" backend/src/cron/scheduler.ts` outputs 1; `grep -c "Đã đến giờ tập luyện" backend/src/cron/scheduler.ts` outputs 1; `grep -c "Bạn sắp mất streak" backend/src/cron/scheduler.ts` outputs 1
    - No PII in bodies: `grep -E "\\$\\{.*name|\\$\\{.*email|caloriesBurned|bmi" backend/src/cron/scheduler.ts` returns 0 matches
  </acceptance_criteria>
  <done>
    `backend/src/cron/scheduler.ts` exports startScheduler() with two cron jobs. Per-minute job dispatches water + workout reminders using manual UTC+7 offset math. Daily 20:00 job dispatches streak alerts to users with streak>0 and <3 habits today. Bodies match UI-SPEC copy verbatim and contain no PII. server.ts calls startScheduler() after connectDB() + loadFirebase(). TypeScript compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FCM service (firebase-admin) → mobile device | Server sends arbitrary notification body; must not leak PII |
| public mobile client → /api/notifications/register-token | Was unauthenticated (TODO); must be authenticated |
| node-cron worker → MongoDB | Internal trust; no HTTP exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | POST /api/notifications/register-token | mitigate | Add `authenticate` middleware; controller reads userId from req.user.id (not request body) — closes RESEARCH.md Open Question 1. |
| T-05-04-02 | Tampering | Cron job time comparison | mitigate | Manual UTC+7 offset math in per-minute callback (Pitfall 2); grep gate enforces `7 * 60 * 60 * 1000` presence. |
| T-05-04-03 | Denial of Service | Cron schedules before DB ready | mitigate | startScheduler() called only inside startServer() AFTER connectDB() + loadFirebase() (Pitfall 1); grep gate enforces line order. |
| T-05-04-04 | Information Disclosure | FCM notification body content | mitigate | Notification body strings are generic (UI-SPEC Copywriting Contract); no user name, kcal, BMI, email, or other PII interpolated. Grep gate forbids template literals referencing user fields. |
| T-05-04-05 | Repudiation | Failed FCM sends silently dropped | accept | Mirror existing fcm.service.ts pattern (console.error per failure); Promise.allSettled ensures one bad token doesn't abort the whole batch. Production observability deferred. |
| T-05-04-06 | Elevation of Privilege | Cron job authentication | accept | Cron runs in-process server-side; no HTTP exposure; no auth needed. |
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit` green
- All grep gates in Task 2 pass (cron expressions, timezone, UTC+7 math, no PII)
- server.ts shows startScheduler() AFTER loadFirebase() and BEFORE app.listen()
- `cd backend && npm run test:auth` still green (sanity check that auth flow not regressed)
- Manual verification deferred per VALIDATION.md (cron timing requires real device + FCM)
</verification>

<success_criteria>
- fcm.service.ts has sendBatchNotificationToUsers with $in query and Promise.allSettled
- /api/notifications/register-token requires authenticate; userId from JWT
- backend/src/cron/scheduler.ts exists with startScheduler() and two cron jobs
- Per-minute job uses manual UTC+7 math (not server local time)
- Daily 20:00 job uses 'Asia/Ho_Chi_Minh' tz + '0 20 * * *' expression
- server.ts bootstraps scheduler after connectDB() + loadFirebase()
- All notification bodies match UI-SPEC copy and contain no PII
- TypeScript compiles
</success_criteria>

<output>
Create `.planning/phases/05-home-dashboard/05-04-SUMMARY.md` with:
- New fcm.service.ts export signature
- scheduler.ts cron expressions and dispatch function signatures
- server.ts bootstrap order (note: must be after loadFirebase())
- Notification body strings (final, no PII, matching UI-SPEC verbatim)
- Manual test plan from VALIDATION.md (deferred to device testing)
- Note for downstream Plan 07: mobile must call registerToken after auth + permission grant
</output>
